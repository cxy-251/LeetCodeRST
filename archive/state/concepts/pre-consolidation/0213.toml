[meta]
range = "0213"
kind = "generation"
reference_problems = [50, 76, 198]
static_review_only = true

[problems."0213"]
title = "House Robber II"
primary_pattern = "circular_path_to_two_linear_paths"
concepts = [
  "circular adjacency constraint",
  "first-last mutual exclusion",
  "two linear interval decomposition",
  "take-or-skip recurrence",
  "rolling dynamic programming state",
  "wide integer accumulation",
]
proof_obligations = [
  "every valid circular selection excludes the first or last house",
  "both linear subproblem solutions remain valid in the circle",
  "rolling states equal the previous two full-DP values",
]
language_risks = [
  "n=1 must return before constructing two intervals",
  "fixed-width adapters widen the accumulated sum",
  "Julia and R translate zero-based interval endpoints to one-based indices",
  "R uses a while loop instead of potentially reversed colon ranges",
]

[workflow]
planned_scope = [213, 214]
completed_scope = [213]
scope_shrunk = true
scope_reason = "0213 已完整通过质量门；0214 为 Hard，KMP border 充要证明、安全分隔符、十语言字符串构造和 C 失败清理需要独立批次，继续同批会压缩证明与资源说明。"
