# AGENTS.md

本文件规定 ChatGPT/Codex 在本仓库中生成、审查、整改和接续 LeetCode 题目 RST 的长期规则。仓库简介看 `README.rst`；每轮具体任务以 `PROGRESS.rst` 为准。

## 1. 接手与任务判定

每次接手任务按以下顺序开始：

1. 读取根 `PROGRESS.rst`，确认目标题号、目标文件、工作重点和完成后入口；
2. 读取根 `AGENTS.md`，确定文件类型、题解结构和任务闭环；
3. 确认目标题目的类型与公开状态；
4. 检查最新 `main` 上的目标 RST 是否存在；
5. 目标 RST 已存在时读取全文，再按本文件规则处理。

题目类型决定文件内容：

- 公开算法题使用完整题解 RST；
- Premium、Database、Shell 和题面暂时无法可靠确认的题目使用状态 RST。

公开算法题按目标文件状态处理：

- 目标 RST 已存在：审查并整改现有题解；
- 目标 RST 尚未建立：创建完整的分析型多语言题解。

状态题目按目标文件状态处理：

- 目标 RST 已存在：核对并整理状态信息；
- 目标 RST 尚未建立：创建对应题号的状态 RST。

## 2. 任务范围与提交

正常一轮处理一道题，并修改两个文件：

1. `PROGRESS.rst` 指定的目标题号 RST；
2. 根 `PROGRESS.rst`。

目标题号 RST 与新的 `PROGRESS.rst` 组成一个原子提交，直接写入 `main`。

用户明确指定多题批次时，按题号连续处理，并保持每道公开算法题的分析完整度一致。批次完成后把下一入口写入 `PROGRESS.rst`。

规则维护轮次根据实际问题同步更新 `README.rst`、`AGENTS.md` 和 `PROGRESS.rst`，相关控制文件组成一个原子提交。

## 3. 文件与目录

题目文件按百题范围放置：

- `0001-0100/`
- `0101-0200/`
- `0201-0300/`

每个题号使用一个 `<题号>-<slug>.rst` 文件：

- 公开算法题文件包含题目信息、题目重述、自建示例、C++ 多解法、题解正文和九语言实现；
- 状态题目文件包含题号、题名、类型、原题链接和处理状态。

## 4. 状态 RST

Premium、Database、Shell 和题面暂时无法可靠确认的题目使用以下结构：

```rst
0156. Binary Tree Upside Down
=============================

:题号: 0156
:题名: Binary Tree Upside Down
:类型: Premium
:原题: `LeetCode 0156 <https://leetcode.com/problems/binary-tree-upside-down/>`_
:处理状态: Premium 题目，当前以状态页保留题号位置。
```

状态 RST 在处理状态之后结束。后续取得完整公开题面并决定纳入题解时，在原文件路径上扩展为完整题解。

## 5. 公开算法题定位

公开算法题形成分析型题解。每篇题解完成四件事：

1. 用自己的话讲清题目要求、平台接口和实现边界；
2. 从原始搜索空间或直接模拟出发，推导重复工作和优化方向；
3. 把题面抽象为明确的算法模型，讲清状态、数据结构、排除依据以及算法为何成立；
4. 用 C++ 展示代表性解法，再给出主解法的九语言实现。

代码先给出完整实现，题解正文再围绕代码中的函数、状态和更新顺序展开。题解既解释代码如何运行，也解释算法为什么由前一种方法演化而来。

## 6. 固定题解结构

公开算法题按以下顺序组织：

1. **题目信息**；
2. **题目重述**；
3. **自建示例**；
4. **C++ 实现**；
5. **题解**；
6. **九语言实现**。

九语言实现是本题正文的结束位置。标题根据题目机制微调，整体顺序保持一致。题解内部围绕实际算法设置小节。

## 7. 题目信息、题目重述与自建示例

### 7.1 题目信息

题目信息包括：

- 题号；
- 难度；
- 主题；
- 原题链接；
- 教学重点。

教学重点写明本题真正需要理解的算法机制、状态设计或实现边界。

### 7.2 题目重述

题目重述使用自己的话提取决定实现的契约，按题目说明：

- 输入与输出；
- 返回下标、节点、对象或结果集合的语义；
- 长度范围、数值范围、字符域或节点模型；
- 平台保证的条件；
- 输入修改方式；
- 对象状态的跨调用关系；
- 返回顺序、唯一性、去重或原地修改要求。

实现自身选择的行为放入对应解法说明，例如复制输入、保留原始下标、复用节点或新建返回结构。

### 7.3 自建示例

自建示例来自自行设计的输入，通常选择 1 至 3 个能够暴露核心机制或实现边界的场景，例如：

- 重复值或重复字符；
- 边界下标与空结果；
- 同坐标事件；
- 对象状态变化；
- 回退、恢复或去重过程；
- 能体现关键更新顺序的反例。

每个示例承担一个明确的解释任务，并在后续题解中继续使用其中至少一个示例展示状态演化。

## 8. C++ 多解法实现

C++ 是本仓库的主语言。C++ 实现放在题解分析之前，用一个完整代码块展示本题具有独立思想价值的代表性解法。

### 8.1 解法集合

通常选择 2 至 4 种代表性方法：

- 最直接的基准解法；
- 综合最优的主解法；
- 具有不同核心思想或实际取舍的替代解法。

解法集合聚焦搜索空间、算法模型、数据结构或复杂度具有实质差异的方法。仅有一种独立思路的题目保留该主解法，并完整展示其辅助结构和状态变化。

### 8.2 代码组织

一个 C++ 代码块中使用不同函数表示不同解法，函数名直接表达算法，例如：

- `bruteForce`；
- `sortAndTwoPointers`；
- `onePassHash`。

LeetCode 标准入口保留题目要求的函数名，并调用最终选定的主解法。代码块包含必要的头文件、类型和辅助函数，使每种方法形成完整实现。

### 8.3 轮子与辅助函数

算法依赖且标准库没有直接提供的轮子在代码块中完整体现，例如：

- 自建哈希表、堆、队列或并查集；
- Trie 节点与资源释放；
- 二分边界、图遍历或字符串解析辅助函数；
- 平台类型之外需要定义的适配结构。

标准库提供合适容器时直接使用。题解正文说明辅助函数的输入输出、局部状态、调用前提、保持的性质、复杂度和资源关系。

## 9. 题解正文深度

题解正文位于 C++ 代码之后，围绕前面已经出现的函数展开。小节名称按算法实际机制设置，并覆盖以下推导链。

### 9.1 原始搜索空间与基准方法

先说明最直接的方法枚举或模拟什么，例如：

- 下标对、子数组、子序列、分割位置；
- 路径、状态组合、操作序列；
- 每个位置的左右边界；
- 对象操作后的候选状态。

给出候选数量、遍历范围或递归分支，并指出基准方法重复计算、重复查询或重复维护的信息。

### 9.2 等价转换与优化来源

写出把题面条件转换为可查询、可递推或可维护关系的关键等式与逻辑，例如：

- 固定和值转为补数查询；
- 区间答案转为前缀差；
- 全局最优转为前缀状态；
- 无序搜索转为排序后的单调移动；
- 重复子问题转为记忆化或动态规划；
- 操作历史转为可维护的数据结构状态。

说明新方法具体消除了基准方法中的哪部分重复工作。

### 9.3 状态与数据结构推导

每个关键状态说明：

1. 它保存什么信息；
2. 为什么主算法需要这项信息；
3. 键、值、索引、节点或边分别表示什么；
4. 什么时候读取；
5. 什么时候更新；
6. 更新后覆盖哪些已处理输入。

数据结构选择从所需操作推导，例如成员查询、最值维护、顺序维护、区间聚合、最近位置、父子关系或对象身份。

### 9.4 排除、转移与更新依据

每个关键动作说明它处理了哪些候选，并给出信息依据：

- 双指针移动排除的下标范围；
- 二分边界舍弃的区间；
- 滑动窗口扩张或收缩后恢复的条件；
- 单调栈弹出时已经确定的答案；
- 动态规划转移对应的最后一步；
- 贪心选择保留的最优可能性；
- 回溯选择、撤销与可行状态恢复；
- 图搜索入队、出队和访问标记的时机。

### 9.5 状态演化展示

状态变化是理解重点时，使用一种或多种表示展示执行过程：

- 跟踪表；
- 状态图；
- 指针移动示意；
- DP 小表；
- 栈或队列变化；
- 对象操作序列；
- 递归树的局部展开。

展示内容使用自建示例，并让表格或图中的状态名与 C++ 代码一致。

### 9.6 用具体结论说明算法为何成立

题解使用本题需要说明的具体结论作为小节标题，例如：

- 为什么历史前缀查询不会重复位置或遗漏答案；
- 为什么移动左端或右端能够排除整组候选；
- 为什么窗口收缩后重新满足约束；
- 为什么状态转移覆盖所有合法的最后一步；
- 为什么元素离开单调结构时答案已经确定；
- 为什么局部操作保持对象的不变量。

正文围绕算法的合法性、完整性、状态保持和终止过程展开，并让论证直接对应代码中的状态与关键动作。简单题使用直接论证；状态较复杂的题目使用不变量、引理、归纳或局部操作证明。

### 9.7 复杂度来源

复杂度从实际操作次数推导：

- 外层与内层遍历范围；
- 指针总移动次数；
- 元素入栈、出栈、入队、出队次数；
- 哈希查询和写入次数；
- 排序、堆操作和树操作成本；
- DP 状态数量与每个状态的转移数量；
- 递归深度、工作空间和返回结果空间；
- 字符串、数组、节点或排序副本的物化成本；
- 平均复杂度成立所依赖的容器假设。

### 9.8 算法条件与实现选择

题解区分算法成立所需条件和当前代码采用的具体策略，例如：

- 哈希表保存任意合法历史下标、第一次下标或最近下标；
- 排序直接修改输入或排序副本；
- 链表节点复用或新建；
- DP 使用完整表、双行或单行压缩；
- 图搜索在入队或出队时标记访问。

说明当前实现选择带来的接口、空间、顺序和所有权影响。

## 10. 常见算法类型的分析重点

### 10.1 哈希与集合

说明反复查询的问题、键和值的语义、历史范围、重复值策略、查询与写入顺序，以及平均复杂度依赖的哈希假设。

### 10.2 双指针与滑动窗口

说明单调性或窗口约束、每次移动排除的候选、左右边界的覆盖范围，以及两个指针的总移动次数。

### 10.3 二分查找

说明搜索区间、答案单调性、循环边界、取中点方式、分支保留的半区间和最终返回位置。

### 10.4 单调栈、队列与堆

说明容器中的顺序性质、元素进入与离开的条件、离开时确定的答案，以及每个元素的总操作次数。

### 10.5 动态规划

说明状态维度、前缀或区间语义、边界状态、转移对应的最后一步、遍历顺序和空间压缩时的覆盖关系。

### 10.6 图搜索与并查集

说明节点和边的含义、方向、访问状态、入队或合并时机、连通性变化和重复访问控制。

### 10.7 回溯与搜索

说明搜索树中的层、选择集合、剪枝条件、状态写入与恢复，以及答案收集时机。

### 10.8 贪心

说明局部选择、交换或支配关系、保留的最优解集合，以及当前选择与后续选择之间的兼容性。

### 10.9 链表、树和对象设计

说明节点身份、结构拓扑、辅助操作、对象历史、字段关系、资源归属和每次公开操作后的状态。

## 11. 解法对比与主解法选择

使用表格或紧凑段落比较 C++ 中的代表性解法：

- 时间复杂度；
- 工作空间；
- 输入修改；
- 返回结构；
- 实现复杂度；
- 适用条件；
- 语言移植成本。

明确九语言采用的主解法及选择原因。比较结果与实际 C++ 实现保持一致。

## 12. 九语言实现

C++ 在前文完整展示。九语言固定顺序为：

1. C
2. Python
3. Java
4. Rust
5. Go
6. TypeScript
7. C#
8. Julia
9. R

九语言实现题解中最终选定的主解法，并保持同一题意、同一关键状态和同一更新顺序。平台接口采用该语言常见的 LeetCode 提交形式。

某种语言因平台接口、标准库、所有权、索引或对象模型采用不同实现时，在对应代码附近说明：

1. 适配原因；
2. 相比 C++ 主解法发生的变化；
3. 该语言实现的实际时间和空间复杂度。

语言实现重点：

- C：必要头文件、容量计算、返回数据所有权、资源释放和必需轮子；
- Python：容器语义、可变对象与递归边界；
- Java、C#：标准容器、对象模型和平台接口；
- Rust：所有权、借用、节点表示和必要的内部可变性；
- Go：切片、字符串和底层存储关系；
- TypeScript：`number` 精度、位运算和查询结果的真假判断；
- Julia、R：一基索引、范围、返回接口和可变对象表达；
- 字符串题：字节、代码单元或题目限定字符域。

## 13. 代码注释

代码块在关键位置使用中文注释，优先采用简短的中文行尾注释，使注释紧邻对应语句。

注释集中解释：

- 非显然的状态含义；
- 关键更新顺序；
- 排除候选的依据；
- 边界哨兵与索引转换；
- 溢出、精度和字符编码限制；
- 节点复用、所有权和资源释放；
- 语言特有的容器查询语义；
- 自建轮子的冲突处理、容量和生命周期。

较长说明使用前置的一至两行中文注释。

## 14. 对象身份与修改语义

链表、树、图和对象设计题说明：

1. 节点或对象按身份区分，还是只按值区分；
2. 实现采用复用、复制、重连节点或字段修改；
3. 输入结构的最终状态和调用者可观察的修改；
4. 对象状态的构造与跨方法调用关系；
5. 返回结构的所有权、共享关系和生命周期。

正文中的对象语义与 C++ 多解法及九语言代码保持一致。

## 15. 现有文件审查与整改

公开算法题 RST 已存在时，读取全文并完成以下审查：

1. 组织为“题目信息 → 题目重述 → 自建示例 → C++ 实现 → 题解 → 九语言实现”；
2. 题目契约、平台接口、输入范围和返回语义准确；
3. 自建示例能够承接后续状态演化说明；
4. C++ 在一个代码块中覆盖具有独立思想价值的代表性解法；
5. 题解从原始搜索空间推导到优化方法；
6. 问题抽象解释等价转换、状态和数据结构来源；
7. 各解法说明写清重复工作、排除依据和实际取舍；
8. 主解法的状态演化、判断顺序、关键结论成立依据和复杂度映射到代码；
9. 辅助函数和轮子的局部性质与主算法关系清楚；
10. 九语言完整实现同一主解法，并包含必要轮子和语言适配；
11. 关键位置使用简洁的中文代码注释；
12. 正文以九语言实现结束；
13. RST 标题、章节、代码块、链接、表格和数学表达完整可读。

状态 RST 已存在时，核对题号、题名、类型、原题链接和处理状态，并整理为第 4 节规定的结构。

已有正确内容继续保留并整理。整改结果直接写回目标 RST，形成该题号的最终文件。

## 16. 篇幅控制

简单题完整讲清搜索空间、等价转换、优化来源、关键状态、更新顺序、算法为何成立和复杂度，并使用紧凑的小节组织。

中等题完整展示状态推导、代表性解法、状态演化、排除或转移依据、关键结论成立依据和复杂度。

困难题围绕决定实现的关键机制展开，例如惰性删除、同坐标整体处理、状态压缩、图搜索层次、回溯恢复或数据结构不变量，并使用引理、状态图或跟踪表组织论证。

每段正文解决一个具体问题：搜索了什么、重复工作在哪里、为什么这样转换、为什么选择当前状态、为什么能排除候选、为什么更新顺序正确、为什么结果完整、为什么复杂度成立，或为什么某种语言需要当前适配。

## 17. 验证方式

仓库默认交付分析和代码文本。用户明确指定验证或运行代码时，按指定范围执行。

## 18. 完成与接力

完成每个题号后：

1. 根据实际完成结果完整重写根 `PROGRESS.rst`；
2. 在新的 `PROGRESS.rst` 中写明下一轮题号、目标文件、工作重点和完成后入口；
3. 让 `PROGRESS.rst` 保持为面向下一次对话的当前任务快照；
4. 将目标题号 RST 与新的 `PROGRESS.rst` 组成一个原子提交。

下一次接手时先读新的 `PROGRESS.rst`，再按本文件规则继续。
