# AGENTS.md

本文件规定 ChatGPT/Codex 在本仓库中生成、修改和接续 LeetCode 多语言分析题解的统一规则。仓库简介看 `README.rst`；每轮具体任务以 `PROGRESS.rst` 为准。

## 1. 接手流程

每次新对话按以下顺序开始：

1. 读取根 `PROGRESS.rst`，确认本轮题号、目标文件、参考文件和完成条件；
2. 读取根 `AGENTS.md`，确定题解写法和语言实现规则；
3. 读取当前题号所属范围的 `README.rst`；
4. 读取 `PROGRESS.rst` 指定的参考题解；
5. 按当前题本身完成分析和十语言实现。

`PROGRESS.rst` 决定“这轮做什么”，`AGENTS.md` 决定“题解怎么写”。

## 2. 任务粒度

默认一个对话处理一道题。用户指定多题批次时，按题号连续完成该批次，并保持每道题的分析完整度一致。

常见任务类型：

- 单题生成：创建下一道公开算法题的完整分析题解；
- 单题修改：修改指定题解中的分析、代码、接口、路径、索引或文字；
- 进度接续：完成 `PROGRESS.rst` 指定的任务并生成下一份接力说明。

## 3. 文件和目录

题解按百题范围放置：

- `0001-0100/`
- `0101-0200/`
- `0201-0300/`

每道公开算法题使用一个 `<题号>-<slug>.rst` 文件。题目分析、语言说明和全部代码直接写入该文件。

Premium、Database、Shell 或暂时无法可靠确认题面的题目，在当前范围索引和下一份 `PROGRESS.rst` 中登记处理结果。

## 4. 题解定位

本仓库产出分析型题解。每篇题解要完成三件事：

1. 讲清题目要求和实现边界；
2. 讲清算法模型、关键状态以及为什么成立；
3. 给出与分析一致的十语言代码。

分析服务于代码理解，代码承接前文分析。正文保持具体、完整和紧凑。

## 5. 题解结构

普通算法题按实际需要组织以下内容，标题可以调整，相邻内容可以合并：

1. **题目信息**：题号、难度、主题、原题链接、访问状态、教学重点；
2. **题目重述或精确契约**：输入域、返回语义、平台接口、特殊前提、输入修改和对象状态；
3. **示例与边界**：选择能够暴露核心机制或常见错误的例子；
4. **问题抽象**：说明题面被转化成什么算法模型；
5. **解法选择**：说明主解法，并比较真正相关的替代方案；
6. **核心状态与不变量**：定义关键变量、状态含义、更新时机以及它们与代码的对应关系；
7. **正确性理由**：围绕算法的关键动作说明合法性、完整性、最优性、无重复或终止性；
8. **复杂度**：说明时间、工作空间、递归栈、返回结果空间和必要语言适配成本；
9. **十语言实现**：按固定顺序给出代码，并补充当前题需要的语言差异说明。

## 6. 分析写法

题目契约写到足以决定实现。长度范围、数值范围、字符域、节点模型、返回下标、输入是否可修改以及对象操作历史，应在相关题目中明确。

示例用于解释算法。优先选择重复值、重复字符、重复边、空结果、边界下标、同坐标事件、对象状态变化等能够区分正确实现和错误实现的输入。

问题抽象要直接指出模型，例如：

- 哈希题：前缀状态、集合查询或最近位置；
- 滑动窗口题：窗口有效条件、扩张和收缩规则；
- 图题：边方向、入度、访问状态或层次关系；
- 动态规划题：状态语义、转移来源和遍历顺序；
- 数据结构题：对象状态、结构表示和操作后的不变量。

状态分析落到代码变量。正文使用的状态名、更新顺序和判断条件应与十语言实现保持一致。

正确性分析围绕当前算法真正需要证明的部分展开：

- Easy 题围绕核心状态和返回条件写清即可；
- Medium 题写完整的状态保持、覆盖范围和结果正确性；
- Hard 题按关键机制拆分论证，并合并重复内容。

复杂度反映真实实现。根据题目说明哈希平均与最坏情况、排序、堆操作、递归栈、输出规模、字符串或数组物化以及语言适配成本。

## 7. 十语言实现

普通算法题固定语言顺序：

1. C
2. C++
3. Python
4. Java
5. Rust
6. Go
7. TypeScript
8. C#
9. Julia
10. R

各语言使用符合语言习惯的写法，并保持同一题意和同一主算法语义。某种语言需要不同适配方式时，在对应代码前后用短段说明接口和成本差异。

平台接口贴近 LeetCode。类名、函数名、参数类型、返回类型和平台节点类型采用该语言常见提交形式。

语言实现重点：

- C：必要头文件、容量计算、返回数据所有权和资源释放；
- C++、Java、C#：标准容器和平台接口；
- Rust：所有权、借用和节点表示；
- Go：切片、字符串和底层存储关系；
- TypeScript：`number` 精度和位运算语义；
- Julia、R：一基索引、范围和可变对象表达；
- 字符串题：字节、代码单元或题目限定字符域；
- 图、树和链表题：节点身份、复用、重连和返回对象语义。

## 8. 篇幅控制

简单题围绕一个核心状态写清，保持紧凑。

中等题保留完整分析链：契约、抽象、状态、正确性、复杂度和代码。

困难题展开决定实现的关键机制，例如惰性删除、同坐标整体处理、状态压缩、图搜索层次或数据结构不变量，并合并同义证明和重复语言说明。

每段正文应解决一个具体问题：为什么这样建模、为什么这样更新、为什么结果正确、为什么复杂度成立，或为什么某种语言需要当前写法。

## 9. 验证方式

仓库交付分析和代码文本。运行、编译、调试和 LeetCode 判题由读者完成。用户明确指定验证、审查或修复代码时，按指定范围执行。

## 10. 完成与接力

完成本轮任务后：

1. 更新当前范围目录的 `README.rst`；
2. 根据实际完成结果，完整重写根 `PROGRESS.rst`；
3. 在新的 `PROGRESS.rst` 中写明下一轮目标、目标文件、参考文件、工作范围和完成后应推进到的入口；
4. 让 `PROGRESS.rst` 保持为面向下一次对话的当前任务快照。

下一次对话只需先读新的 `PROGRESS.rst`，即可知道接下来做什么。

## 11. Git 工作方式

用户要求修改仓库时，直接在 `main` 上完成。每次修改按用户限定的文件范围执行，并在写入后报告实际修改文件和提交 SHA。