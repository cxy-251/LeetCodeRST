# AGENTS.md

本文件是本仓库题解工作的长期执行规范。仓库目标不是收集可提交答案，而是把每道题写成一篇独立的解题思维训练材料：读者应能从题意出发，看到朴素方法为何正确、重复工作在哪里、题目结构如何缩小搜索空间，以及新的认识怎样落实为更好的代码。

## 1. 工作边界

- 一次只处理一道题，完成后停止，等待用户审阅。
- 默认直接修改 `main`，不新建分支。
- 只修改当前指定题目文件；控制文件只有在用户明确要求时才能修改。
- 不修改无关题目，不把一题的正文、示例、代码或结论机械复制到另一题。
- 只基于当前题目事实写作。处理每题前重新阅读当前 RST，并在需要时核对当前 LeetCode 题面、约束、函数签名、返回顺序和保证条件。
- 无法可靠确认的内容必须明确保留不确定性，不得编造题面、约束或结论。

## 2. 题目信息

每篇题目开头只保留以下五项：

- `题号`（题号后紧接中文题名，例如 `0101. 对称二叉树`）
- `难度`
- `主题`
- `原题`
- `重点`

字段名称必须简短，保证首列不换行。中文题名属于 `题号` 字段的值，不单独增加 `题名` 字段。Premium 题直接在难度后标注，例如 `Medium（Premium）`，不得单独增加“访问状态”“题面说明”“题名”“类型”等字段。

Premium、Database、Shell 与普通算法题一样都必须写成完整题解；题目类型只决定实现语言和推导重点，不得因会员标记或访问状态降级为状态页。写作前仍需尽力核对题意、表结构、函数签名和约束；不确定的事实必须如实处理，不得编造。

## 3. 题目重述

题目重述必须让读者不依赖原站也能理解当前问题，至少明确：

- 输入对象及其含义；
- 允许执行的操作；
- 需要判断、计算或返回的内容；
- 关键定义和统计口径；
- 会影响解法的保证条件与约束；
- 容易误读的语义，例如是否允许重复、是否修改输入、是否要求原始下标、是否存在唯一答案。

不得把题目重述压缩成一句模糊概括，也不得提前把核心结论直接当作题意写入。

## 4. 自建示例

`自建示例` 必须紧跟在题目重述之后，先于算法分析和实现出现。

示例应覆盖本题真正重要的情况，而不是只重复官方普通样例。根据题目选择普通情况、失败情况、最小规模、重复值、负数、零值、空集合、单元素、边界端点、溢出风险或特殊结构等用例。

每个示例都必须能够独立复算，并直接服务于后面的推导。只有一行的输入、输出、状态或代码使用行内代码；只有多行示例、表格、推演过程和完整程序才使用块级代码。

## 5. 实现组织

普通算法题以 C++ 为主；Database 题使用 SQL；Shell 题使用对应命令或脚本。后续不新增或维护“九语言实现”“十语言实现”等多语言章节。处理旧题时，只有用户明确要求精简，或多语言区域妨碍当前正文结构时，才删除该题已有的多语言实现区。

同一道算法题存在多种有教学价值的方法时，优先在一个 `Solution` 中组织：

- 不同方法写成命名清楚的私有函数；
- 公共入口只调用最终选定的主解法；
- 辅助函数只承担明确职责；
- 方法名能够反映算法差异，而不是使用含糊编号；
- 代码必须与正文中的推导顺序一一对应。

多种实现必须形成真实的思路演进，例如暴力枚举、利用结构减少搜索、建立索引或状态、空间压缩、最终主解法。不得为了凑数量放置本质相同的代码。

若题型不适合放在同一个类中，可以分开呈现，但仍要明确主解法及各方法之间的演进关系。

代码排版遵循以下规则：

- 每行代码最多 120 个字符；
- 函数签名、循环头、条件表达式、三元表达式和短返回表达式在不超过 120 个字符时保持单行；
- 只有超过 120 个字符或拆分后能显著增强结构表达时才换行，不为追求纵向对齐机械拆行；
- 函数内部尽量少用空行，只在初始化、主循环、结果整理等不同逻辑阶段之间保留必要分隔；
- 连续声明、赋值、边界更新和相邻控制语句之间不插入无意义空行。

## 6. 题解推导

题解必须从问题本身的原始搜索空间或直接模拟开始，而不是从最终技巧倒推结论。通常按下面的逻辑推进：

1. 给出最直观且能够保证正确的朴素方法；
2. 说明它枚举或维护了什么，以及为什么不会漏解；
3. 找出真正的瓶颈、重复计算或不必要状态；
4. 从题目结构中提取可利用的单调性、等价关系、不变量、状态、索引、转移、剪枝或数据结构；
5. 说明这个新认识如何直接删除、替换或压缩上一版代码中的工作；
6. 给出下一版实现，并继续检查是否还能优化；
7. 明确最终主解法的选择理由。

不同方法不必强行排成单线。若它们从同一个等价转换分叉，例如排序双指针与哈希索引，应先讲清共同转换，再分别说明各自利用的结构。

简单题可以短，但不能省略决定正确性的关键转换；难题应讲清状态定义、转移来源、不变量、搜索边界和剪枝依据。

## 7. 代码分析

`代码分析` 或 `代码演进` 的任务不是逐行翻译语句，而是解释思路变化如何改变代码：

- 哪一层循环或搜索被删除；
- 哪些状态不再需要维护；
- 为什么新增某个数据结构；
- 为什么查询顺序、写入顺序或遍历方向必须如此；
- 如何从保存全部中间结果缩减为只保存目标统计量；
- 时间和空间复杂度的变化来自哪一项工作被消除。

推导过程中已经解释清楚的正确性依据，不再单独写“正确性说明”、定理或引理。只有复杂算法确实需要独立不变量时，才用短语标题就地说明，避免重复全文。

章节标题必须是简短短语，不能使用问句。相同结论只解释一次，避免在推导、代码分析和正确性章节中反复复述。

## 8. 状态推演与表格

只有当状态变化、指针移动、哈希前缀、动态规划转移、栈队列内容或操作序列不容易仅凭文字理解时，才增加状态表或逐步推演。

表格必须服务于一个具体疑点，例如：

- 当前状态在处理元素前还是处理后定义；
- 为什么先查询再写入；
- 某次指针移动排除了哪些候选；
- 某个 DP 状态从哪些前驱转移而来。

RST 表格首列内容应保持简短，避免换行；列数和内容应控制在可阅读范围内。

## 9. 复杂度与边界

复杂度不能只写结论，要说明主要代价来自哪里。多种方法应集中对比时间复杂度、工作空间和优化依据。

需要区分：

- 期望复杂度与严格最坏复杂度；
- 工作空间与返回结果空间；
- 原地修改与复制输入；
- 数据库查询的逻辑中间结果规模与实际执行计划成本。

边界处理只写会影响算法或代码的情况，并说明当前实现如何处理。不要把题目约束重新抄写一遍充当边界分析。

## 10. Database 与 Shell

Database 题同样遵循思路演进：

- 先明确关系、统计单位和重复记录语义；
- 展示直接写法为什么遗漏、重复计数或改变权重；
- 推导关系展开、连接、分组、窗口、子查询或聚合结构；
- 给出主 SQL；
- 分析查询结构如何对应推导；
- 说明执行代价时避免武断地套用固定算法复杂度。

Shell 题应从输入流、字段、匹配范围和边界开始，逐步推导命令组合，并说明每个管道阶段改变了什么数据。

## 11. RST 与文字规范

- 使用中文说明，技术名词和代码标识保持准确。
- 标题使用短语，层级稳定，不使用问句标题。
- 一行输入、输出、状态或代码使用行内代码，不为这些单行内容创建块级代码。
- 简短数学关系可以使用行内形式；需要强调、推导或多行排版的公式可以使用 `.. math::`。
- 完整 C++、SQL、Shell 程序和多行推演使用对应代码块。
- 不设置独立总结章节，不在末尾重复正文结论。
- 文章篇幅由题目复杂度决定，不追求固定行数或固定小节数量。
- 每篇文章必须能够独立阅读，不依赖其他题目、旧版本或对话上下文。

## 12. 检查与提交

完成单题后至少进行以下静态检查：

- 只修改了目标文件；
- 题号、标题、原题链接和方法签名一致；
- 题目信息只有规定的五项；
- 自建示例位于题目重述之后；
- 没有不必要的单行块级代码；
- 算法题的 C++ 主入口调用明确的主解法；
- 正文方法与代码实现一一对应；
- 没有新增或修改与当前任务无关的多语言章节；
- 代码行不超过 120 个字符，短签名、短条件和短表达式没有机械拆行；
- 函数内部没有无意义的连续空行或过度分段；
- RST 标题、缩进、代码块和表格结构有效；
- 没有把静态阅读声称为编译、运行或测试结果。

默认只做文本、差异和 RST 结构等静态检查，不编译、不运行、不对拍；用户明确要求时再执行相应验证。

默认直接提交到 `main`。提交后核对差异只包含当前目标，并在对话中简洁报告修改了哪些文件和必要说明，不重复粘贴 RST 正文；只有用户明确询问时才提供提交信息。
