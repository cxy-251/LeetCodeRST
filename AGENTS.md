# AGENTS.md

## 1. 项目目标

本仓库按 LeetCode 题号顺序建设多语言算法学习资料。每道公开可访问题目对应且只对应一个自包含 RST，目标是：

1. 原创重述题目与示例；
2. 讲清问题抽象、算法状态、不变量、正确性证明、真实复杂度和边界；
3. 在同一题目 RST 中提供固定十语言的可靠实现；
4. 通过冻结质量基线、批次合同、风险触发静态审查和漂移记录阻止长期任务质量衰减。

Premium 或信息不足的题目登记到状态文件，材料完整后继续。

## 2. 当前状态

题号进度保持为：完成 `0001–0155`、`0160`、`0162` 和 `0164–0165`；`0156–0159`、
`0161` 与 `0163` 按 Premium 规则登记。当前处于质量返工阶段，新题 `0166` 暂停。

`0127`、`0129–0134` 已达到教学质量基线，剩余范围记录在 `state/PROGRESS.toml`。
当前批次由 `state/BATCH_CONTRACT.toml` 唯一指定。

## 3. 权威来源与优先级

出现重复或冲突时按以下顺序解释：

1. `state/PROGRESS.toml`：当前阶段、完成范围和下一步；
2. `state/BATCH_CONTRACT.toml`：当前批次范围、风险、样本角色和完成条件；
3. `state/REVIEW_INDEX.toml`：active corrections 与审查策略；
4. `docs/TUTORIAL_QUALITY_BASELINE.rst`：冻结的结构、解释深度和证明质量标准；
5. 当前生效的前向规则与 `docs/AUTOMATION_QUALITY_GATE.rst`；
6. 当前范围 README、知识账本和合同列出的事实来源；
7. `state/DRIFT_LOG.toml`、`archive/`、历史提交与对话报告。

低优先级材料不得覆盖高优先级规则。黄金题中的技术事实若与 active correction 冲突，以 correction 为准。

## 4. 样本权限

最近完成的题目不自动拥有格式示范权。

最近题目只能用于：

- 术语和知识衰减连续性；
- 平台类型与接口连续性；
- 关联题目事实；
- 当前范围和进度核对。

最近题目不得用于推导：

- 章节结构和标题密度；
- 正文、证明或注释应有多长；
- 哪些质量门可以省略；
- 文件拆分方式；
- 静态审查标准。

格式、解释深度和证明质量只能来自 `docs/TUTORIAL_QUALITY_BASELINE.rst` 及当前批次合同选定的黄金题。
新完成题目不会自动加入黄金样本；修改黄金样本必须经过明确质量审查，并形成独立规则提交。

## 5. 新对话接手

每次工作从最新 `main` 开始，只读取完成当前批次所需的权威入口：

1. `AGENTS.md` 与根 `README.rst`；
2. `state/PROGRESS.toml` 与 `state/BATCH_CONTRACT.toml`；
3. `state/REVIEW_INDEX.toml` 的 active corrections；
4. `docs/TUTORIAL_QUALITY_BASELINE.rst` 与合同列出的黄金题；
5. 当前前向规则和 `docs/AUTOMATION_QUALITY_GATE.rst`；
6. 当前范围 README、知识账本和合同明确列出的事实来源。

禁止用“最近三题”替代冻结基线，禁止为了确认格式而递归扫描最近产物。
若 `PROGRESS` 与批次合同不一致，先在当前原子提交中修复状态，不得自行选择另一批。

## 6. 对话驱动工作流

所有生成、返工、抽查、规则维护和归档由用户在当前对话中的明确指令触发。每轮：

1. 读取最新状态、当前合同、冻结基线和 active corrections；
2. 检查算法分数、证明风险、语言语义风险、平台类型风险和输出体积风险；
3. 按题号逐题完成正文、代码和必要静态审查；
4. 每题写完立即执行质量门，不把单题检查推迟到批次末尾；
5. 高风险叠加或产物开始压缩时，缩小批次，只提交已经完整达标的连续前缀；
6. 同步题目、索引、README、进度、知识账本和下一批合同；
7. 清除 marker、草稿、分片和一次性报告；
8. 显式暂存本轮文件，使用标准 `git commit` 形成一个原子提交并非强制推送到 `origin/main`；
9. 重新读取本地与远端 `main`，核对提交范围、状态、合同和下一步。

不得为了满足 4–6 分预算压缩复杂题。质量优先于单批题数。

## 7. 批次范围与风险预算

基础算法分数仍为 Easy=1、Medium=2、Hard=4，目标总分通常为 4–6，同批不安排两道 Hard。
算法分数只决定初始候选范围，最终范围还必须考虑：

- `proof_risk`；
- `language_semantics_risk`；
- `platform_type_risk`；
- `output_volume_risk`。

两个或以上维度为 `high` 时，允许把合同范围缩小为连续前缀。缩批必须同步
`PROGRESS.toml` 和下一份 `BATCH_CONTRACT.toml`，不得通过减少证明、语言说明或静态审查来维持原范围。

## 8. 单题交付

每道公开题只能有一个 `<题号>-<slug>.rst`，正文、证明、静态审查记录和全部语言代码直接内联。
禁止 `.inc`、`.. include::`、代码分片和临时组装工作流。

普通算法题按实际需要覆盖：

- 精确契约与自建示例；
- 问题抽象和真实解法取舍；
- 状态、转移、不变量和完整正确性证明；
- 与实现一致的复杂度及语言适配成本；
- C、C++、Python、Java、Rust、Go、TypeScript、C#、Julia、R；
- 平台接口、数值、所有权、字符单位和失败路径；
- 可核实的静态审查证据；
- 关键边界、易错点、学习链和带答案自检。

简单题可以合并章节，核心信息不能消失。复杂题不得为保持批次大小而写成摘要。

## 9. 静态审查

题解生产默认且统一采用静态审查：不得运行题解代码，不得调用编译器或解释器检查题解，不做官方示例执行、
随机对拍、穷举、属性测试、sanitizer 或目标语言最小程序。示例与关键边界只做人工推演。

逐语言核对平台接口、导入、索引、容器、所有权、位宽、字符单位、语法和正文到代码的映射。高风险只增加
针对性静态推导或官方语义资料核对；无法静态确认的结论必须标记为未运行、未编译、存在剩余风险，不得写成
“测试通过”。允许执行 RST、TOML、文件结构、Git 对象和状态一致性检查，因为这些检查不执行题解代码。

## 10. 审查与漂移控制

固定题数全量审查已经取消。出现高风险语义、静态审查异常、规则冲突、结构漂移或用户明确要求时，
启动针对性抽查。

历史质量事故记录在 `state/DRIFT_LOG.toml`。每轮开始检查当前题是否命中既有事故的回归条件；
发现新的通用漂移时，同一原子提交中更新漂移记录、对应永久规则和质量门，不能只修单题。

## 11. Git 与交接

所有生产、抽查、修复、规则维护和归档只允许直接更新 `main`。禁止分支、PR、上传分片分支和临时工作流。
开始前使用标准 Git 同步并确认最新 `origin/main`；提交前只对本轮明确文件执行 `git add`，复查暂存差异后使用
一次 `git commit`，再执行非强制 `git push origin main`。禁止 `git add .`、force push 和用额外中间提交补救。
若远端在工作期间变化或推送被拒绝，停止并基于最新 `main` 重新核对，不覆盖并发修改。

每轮结束确认：

- `PROGRESS.toml` 与 `BATCH_CONTRACT.toml` 一致；
- 黄金样本和事实来源的权限没有混用；
- 每道公开题只有一个自包含 RST；
- README、范围索引和知识账本同步；
- 活动目录没有已消费的中间产物；
- 对话报告与最新 `main` 一致。
