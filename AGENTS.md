# 根目录代理规则

本文件定义整个仓库的通用开发约束。任何代码修改前，都必须先阅读本文件。

# 允许内容

- 仓库级开发规则
- Codex 协作约束
- 目录 README 继承规则
- 与 `PROJECT_MAP.md` 配套的导航说明

# 禁止内容

- 业务实现细节
- 临时调试记录
- 与目录职责无关的产品方案
- 把 README 当作代码或配置替代品

# 修改前必须阅读

- `./AGENTS.md`
- `./PROJECT_MAP.md`
- 目标文件所在目录的 `README.md`
- 目标文件所有父目录中的 `README.md`

# 命名规则

- 根目录规则文件使用固定名称：`AGENTS.md`
- 项目映射文件使用固定名称：`PROJECT_MAP.md`
- 目录规则文件统一使用 `README.md`

# 边界说明

本文件只定义跨目录协作规则。
具体目录能放什么、不能放什么，由对应目录的 `README.md` 继续收窄。

# Codex 规则

- 在修改任何代码前，必须先阅读根目录 `AGENTS.md`。
- 在修改任何代码前，必须先阅读 `PROJECT_MAP.md`。
- 在修改任何代码前，必须阅读目标文件所在目录的 `README.md`。
- 在修改任何代码前，必须阅读目标文件所有父目录中的 `README.md`。
- 如果 `README.md` 规则与当前任务冲突，先停止并说明冲突，不要直接修改代码。
- 如果目标目录没有 `README.md`，先创建 `README.md`，再继续修改代码。
- 禁止把代码放入 `README.md` 未允许的目录。
- 禁止删除空目录中的 `README.md`。
- 不允许跨目录顺手重构；如需跨目录修改，先说明原因。
- 默认不维护以下生成或外部目录：`node_modules/`、`build/`、`dist/`、`output/`、各类 `generated/` 产物目录。
- 如果任务必须修改生成目录中的文件，先说明原因，并先为该目录补充 `README.md` 规则。

# 任务执行规则

- 如果根目录存在 `AI_TODO.md`，Codex 每次开始任务前必须先阅读 `AI_TODO.md`。
- Codex 只执行 `AI_TODO.md` 中状态为 `[TODO]` 的第一项任务，除非用户明确指定其他任务。
- 执行任务前，先将任务状态改为 `[DOING]`。
- 执行完成后，将任务状态改为 `[DONE]`，并填写完成记录。
- 如果任务无法完成，将任务状态改为 `[BLOCKED]`，并写明原因。
- 不要删除历史任务。
- 不要擅自重写整个 `AI_TODO.md`。
- 不要顺手执行其他任务。
- 如果发现新问题，只能追加到 `AI_TODO.md` 的「发现的问题」区域，或新增一个 `[TODO]` 任务。

# 文档创建规则

- 如果已有文档可以更新，不要创建重复职责的新文档。
- 新增文档前，先确认它不与 `AGENTS.md`、`PROJECT_MAP.md`、目录 `README.md`、`AI_TODO.md` 的职责重复。
- `AGENTS.md` 负责仓库级 AI / Codex 协作规则。
- `PROJECT_MAP.md` 负责项目结构导航。
- 目录 `README.md` 负责目录边界和放置规则。
- `AI_TODO.md` 负责用户交给 Codex 的任务清单。
- 不要把临时调试记录、业务实现细节、产品方案写入 `AGENTS.md`。