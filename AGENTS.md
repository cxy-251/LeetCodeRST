# AGENTS.md

本文件是本地 AGY 接手本仓库时必须执行的唯一任务规范。每次开始工作前先读取根目录 `PROGRESS.rst`，只处理其中写明的当前目标，不根据旧提交、旧完成范围或文件现状自行推断进度。

## 1. 当前任务

按 LeetCode 题号顺序，逐题整理仓库中已经存在的题目 RST。

对每道公开算法题，只允许重写以下三个小节：

1. `题目信息`；
2. `题目重述`；
3. `自建示例`。

文件主标题保持原样。若文件中存在 `C++ 实现`，可修改范围到 `C++ 实现` 标题之前结束；`C++ 实现` 标题及其后的全部内容必须逐字保持原样。

若目标文件只是元数据占位页，可以把主标题之后的占位内容替换为上述三个小节，但不得新增 `C++ 实现`、题解、代码或其他后续小节。

## 2. 绝对约束

- 只在 `main` 分支直接修改和提交；
- 不创建分支，不创建 Pull Request；
- 不新增任何文件或目录；
- 不删除任何文件或目录；
- 不重命名或移动任何文件或目录；
- 只修改当前目标题目 RST 与根目录 `PROGRESS.rst`；
- 不修改 `README.rst`、配置、工作流、脚本或其他控制文件；
- 不修改题目文件主标题；
- 不修改 `C++ 实现` 及其后的任何字符、空行、缩进、注释或标题；
- 不顺手修复代码、题解、复杂度、其他语言实现或后续章节中的错误；
- 不使用脚本、正则、固定模板或批量替换生成多道题的正文；
- 辅助工具只能用于查找文件、检查小节边界、查看差异和验证 RST，不得代替逐题理解；
- 发现缺失文件时不得补建，只在 `PROGRESS.rst` 记录缺失状态；
- 无法可靠确认题目契约时不得凭记忆补写，也不得标记为可信完成。

本文件的约束高于仓库中其他旧说明。旧 README、旧提交信息或旧进度记录与本文件冲突时，以本文件和当前 `PROGRESS.rst` 为准。

## 3. 可信状态规则

仓库的可信状态已经重置。

- 旧的批量完成声明不再作为可信依据；
- 文件中已经存在完整前三节，也不能自动视为可信；
- 只有按照本文件逐题读取、核对、重写或确认，并在当前 `PROGRESS.rst` 中明确记录的题号，才属于新的可信完成范围；
- 不得根据文件数量、提交信息或连续题号直接声明整段范围可信；
- 每个被标记为可信的公开题都必须实际打开目标 RST，并核对当前 LeetCode 原题；
- 每批只能记录本次实际逐题完成的连续范围、状态页和缺失文件；
- 某题没有完成全部检查时，任务指针必须停在该题，不得静默跳过。

## 4. 每道题必须独立写作

每一道题都是一篇独立文章。处理新题时必须清空上一题的写作上下文，只根据以下三项重新建立理解：

1. 当前 LeetCode 原题；
2. 当前目标 RST；
3. 本 `AGENTS.md` 规定的字段、边界和质量要求。

前一道题、相邻题目、同一批次中的其他题目以及旧提交中的文章，不得作为当前题目的内容来源或写作范本。

只允许跨题保持以下形式一致：

- 三个小节的名称和顺序；
- `题目信息` 的字段名称和顺序；
- 合法的 RST 标题、代码块和缩进格式；
- 本文件统一规定的检查流程。

不得跨题继承或模仿以下内容：

- 题目重述的句式、段落顺序、开头和结尾；
- `:重点:` 的措辞、关键词组合和表达节奏；
- 示例的数据形状、数字、字符串、对象操作序列和解释方式；
- 前题的限制、保证条件、返回规则、边界判断或术语；
- 为了提高批量速度而形成的固定段落模板、固定句长或固定示例套路；
- “与上一题相同”“同前”“继续沿用”等依赖其他文章才能理解的表达。

每次开始下一题时必须重新阅读原题，不得仅凭题号相近、主题相似或记忆中的 LeetCode 内容继续写。即使连续题目属于同一主题，也必须分别确认参数、约束、保证条件、特殊返回语义和示例结果。

当前题目的三节必须能够脱离仓库中的其他题目单独阅读。读者只打开这一份 RST，也应能准确理解题目契约和自建示例。

不得因为连续处理多题而逐渐缩短、概括或弱化后续文章。后面的题与前面的题采用相同质量门槛，每一题都必须重新完成全部核对。

完成单题前进行独立性检查：若当前文字明显沿用了前题句式、示例结构或不属于本题的条件，必须依据当前原题重新改写，不能把这种累积偏差带到下一题。

## 5. 题目类型处理

### 5.1 公开算法题或设计题

完整重写或校正前三个小节，并满足本文件后续全部要求。

### 5.2 Premium、Database、Shell 或其他状态页

- 不猜测未公开或无法确认的题面；
- 不把状态页改造成算法题正文；
- 只核对现有题号、题名、类型、难度和原题链接是否明显一致；
- 没有可靠依据时保持文件原样；
- 在 `PROGRESS.rst` 中记录该题属于状态页并已检查，然后推进到下一题。

### 5.3 缺失文件

- 不创建文件；
- 在 `PROGRESS.rst` 记录题号和缺失状态；
- 继续到下一个已经存在的题号，但不得把缺失题号记为内容可信完成。

## 6. `题目信息` 小节要求

公开题统一使用以下字段，字段顺序固定：

- `:题号:`；
- `:难度:`；
- `:主题:`；
- `:原题:`；
- `:重点:`。

具体要求：

- 题号使用四位格式，并与文件名和主标题一致；
- 难度与当前 LeetCode 题目一致；
- 主题只列与题目本身直接相关的数据结构、问题类型或核心机制；
- 原题链接指向该题当前 LeetCode 页面；
- 重点只概括理解题目契约时最容易遗漏或误解的条件；
- 重点不得提前写完整算法、复杂度或实现步骤；
- 删除占位页中的 `题名`、`类型`、`数据源`、`处理状态` 等旧字段，不把它们带入公开题的正式前三节。

## 7. `题目重述` 小节要求

必须在阅读当前原题后，用独立中文重新表达题目契约，不复制或机械改写官方题面。

根据具体题目准确写清：

- 输入对象、参数和数据结构分别表示什么；
- 需要返回什么，或需要怎样修改对象；
- 下标、坐标、层级、时间或操作序列从哪里开始计数；
- 数组、字符串、矩阵、链表、树、图、集合或对象的结构语义；
- 长度、数值、字符和调用次数等会影响实现的约束；
- 是否保证答案存在、是否唯一、是否允许多种合法返回；
- 是否要求不同位置、去重、特定顺序、原地修改或保持输入不变；
- 空结果、无解、并列、重复值、溢出边界、对象初始状态和特殊返回值；
- 设计题中构造函数、每个方法、调用顺序及跨调用共享状态。

本节只写题目契约和必要说明，不写完整算法方案、正确性证明、复杂度分析或代码实现。

不得遗漏原题的重要保证，也不得加入原题不存在的限制、排序要求、唯一性保证或返回规则。

## 8. `自建示例` 小节要求

每道公开算法题或设计题建立 `2` 至 `3` 个独立设计的示例。

示例组合必须至少包含：

1. 一个体现题目核心机制、主要输出或对象状态变化的普通场景；
2. 一个满足原题约束的边界或特殊语义场景。

边界或特殊语义可以包括：

- 最小合法规模；
- 空结果或无解；
- 重复值或去重；
- 并列选择；
- 多种答案均合法；
- 极端但合法的数值；
- 首尾位置；
- 对象初始状态；
- 多次调用后的共享状态；
- 题目规定的特殊返回值。

每个示例必须包含：

- `输入`；
- `输出`；
- `解释`。

示例要求：

- 不复制官方示例；
- 不仅仅替换官方示例中的数字、字符或名称；
- 不参考前一道题或同批其他题的示例来设计当前示例；
- 输入必须满足原题全部约束；
- 输出必须由该输入真实得到；
- 解释必须能够复算输出，并明确说明该示例体现的机制或边界；
- 不能为了制造边界使用原题不允许的空数组、空字符串、非法下标或非法操作；
- 多种答案均合法时，说明所给输出只是其中一种；
- 设计题使用清楚的操作序列、参数序列和逐次输出，解释对象状态如何变化；
- 示例只帮助理解题意，不提前展开完整算法；
- RST 代码块、列表和缩进必须合法。

## 9. 单题执行流程

处理 `PROGRESS.rst` 中的当前目标时，按以下顺序执行：

1. 确认当前分支是 `main`；
2. 读取目标 RST 全文；
3. 判断它是公开题、状态页还是缺失文件；
4. 对公开题读取当前 LeetCode 原题，逐项确认题目契约；
5. 主动丢弃上一题的措辞、示例和条件，只保留本文件统一规定的结构；
6. 记录 `C++ 实现` 标题及其后的原始内容，确保最终完全不变；
7. 独立重写或校正 `题目信息`、`题目重述`、`自建示例`；
8. 独立复算所有示例的输入、输出和解释；
9. 检查当前文章能否脱离其他题目独立理解；
10. 检查 diff，确认没有改动主标题、后续小节或其他文件；
11. 检查 RST 标题、下划线、代码块和缩进；
12. 更新 `PROGRESS.rst`，记录本次真实完成情况和下一目标；
13. 将目标 RST 与 `PROGRESS.rst` 直接提交到 `main`。

可以连续处理一个小批次，但批次中的每道公开题都必须从原题重新开始并逐题完成上述检查。不得用一次抽查代表整批，不得把第一题形成的写法套用到后续题目。

## 10. 单题可信完成条件

公开题只有同时满足以下条件，才能写入新的可信完成范围：

1. 已实际读取当前原题；
2. 已逐项确认输入、输出、约束、保证条件和特殊语义；
3. 没有引用、继承或机械模仿前一道题及同批其他题的正文；
4. 当前文章能够脱离其他题目单独理解；
5. `题目信息` 五个字段准确；
6. `题目重述` 独立、完整，没有新增错误条件；
7. 至少两个原创示例，其中至少一个覆盖合法边界或特殊语义；
8. 所有示例输入、输出和解释已经复算且一致；
9. 只修改了前三个小节；
10. 主标题和 `C++ 实现` 及其后的内容完全未变；
11. RST 结构有效；
12. 没有新增、删除、重命名或移动任何文件。

任一条件不满足，该题都不能标记为可信完成。

## 11. `PROGRESS.rst` 接力要求

`PROGRESS.rst` 是本地 AGY 的唯一任务指针。每次提交后必须清楚记录：

- 可信状态是否已重置；
- 新规则下已经逐题完成的精确题号或连续范围；
- 本批实际修改的公开题；
- 本批只检查未改写的状态页；
- 发现的缺失文件或阻塞项；
- 当前下一题的题号、题名和路径；
- 下一题需要特别核对的语义；
- 仍然只允许修改前三节。

不得写入未经本次实际核对的批量完成声明。任务指针必须始终指向第一道尚未按本文件完成的题目。