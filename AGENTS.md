# AGENTS.md

本文件是本仓库后续整理工作的执行合同。项目总目标是为仓库中已经存在的每一道 LeetCode 题目形成一份独立、完整、可单独阅读的题解。每次接续工作先读取根目录 `PROGRESS.rst`，以其中的当前目标和已确认状态为准。

## 1. 目标与文件边界

- 每个已有题号文件都是一篇独立文章，不依赖前后题、相邻题、旧文章或同批次其他文件才能理解。
- 每道题从文件开头到第一个多语言实现标题之前都属于审核范围，通常是“九语言实现”；仓库已有“十语言实现”等同义标题时同样以该标题为边界。前置正文重点审核题目信息、题目重述或分析、自建示例、C++ 代表性代码及其题解小结，要求这些内容合起来能够独立解释当前题目，但不要求每篇文章机械设置同名栏目或逐项套用固定结构。长篇文章优先审核多语言区之前的题解小结和主算法说明；该标题及其后已有的多语言实现可以保留，不作为本项目后续审核、修改或完成判断的要求。
- Premium、Database、Shell 或当前题面无法可靠确认的题目保留为状态页；不得凭记忆猜测题面、算法、示例或代码。
- 只修改仓库中已经存在的文件。不得新增、删除、重命名或移动任何文件或目录。
- 不为了追求数量修改无关题目，不把一题的正文、示例、限制条件或代码机械套用到另一题。

## 2. 单题独立性

处理每道题时，必须重新阅读当前 LeetCode 原题和当前 RST，只保留题目本身的事实。先判断读者是否能只凭当前文件理解输入、输出、约束和容易误判的语义，再根据算法类型补充真正有用的内容：例如双指针的不变量、动态规划的转移来源、搜索的剪枝依据、数据结构的维护规则或设计题的跨调用状态。

题解应从当前题目的定义或搜索空间出发，说明足以复核代码的关键转换和正确性理由，并在确有必要时交代边界、复杂度、输入是否修改和节点/资源语义。自建示例服务于理解，不追求每篇都使用相同的例题结构；示例输入、输出和解释必须能够独立复算。

“完整”不等于固定栏目齐全。简单题可以自然简短，难题应把决定正确性的部分讲透；不能用一句“遍历即可”代替关键推理，也不能让代码成为没有解释的答案。通过标准是独立、准确、针对性强，而不是固定小节数量或固定句式。

## 3. 代码与分析

- 代码、题目分析和题解小结都属于题解正文的一部分；发现不完整、过时、与题意不符或解释与实现不一致时，可以直接修改已有内容。
- C++ 通常作为主解法，代表性代码应与后文分析一一对应；辅助函数、入口函数、输入不变性、节点所有权和边界处理需要讲清楚。
- 从第一个多语言实现标题（“九语言实现”或现有同义标题“十语言实现”）开始的现有多语言实现默认保留原状，不纳入后续逐题审核范围；只有用户明确要求时才处理，不因它们的状态阻塞 C++ 题解完成。
- 不运行代码测试。可以进行文本、差异、RST 结构、题号和链接等静态检查；静态检查不能冒充运行验证。

## 4. 题目来源与时效

需要确认题目契约时优先读取当前 LeetCode 页面，特别是约束、返回顺序、保证条件、方法签名和设计题调用语义。访问受限或题面不可靠时，保持状态页并在 `PROGRESS.rst` 记录阻塞原因，不凭记忆补写。

## 5. 执行与进度

每次工作按以下顺序进行：

1. 读取 `PROGRESS.rst`，确认当前目标和可信完成范围；
2. 检查当前分支、目标文件和工作区状态；
3. 阅读目标 RST 全文并确认题目来源；
4. 独立完善目标文件中第一个多语言实现标题之前的题目分析、示例、C++ 代码和题解小结；长篇文章以已有小结和主算法说明为重点，不把多语言区的内容扩展成审核任务；
5. 做静态差异和 RST 结构检查，不运行代码测试；
6. 更新 `PROGRESS.rst`，只记录本次实际检查或完成的题号；
7. 在当前工作范围完成后汇报修改文件、状态和下一目标。

题号按每 100 道划分为一个交付批次，例如 ``0001–0100``、``0101–0200``；只有当前批次的全部已有题目按新范围完成并经过静态检查后，才将该批次的修改统一提交并推送一次。批次中途只保留本地修改，不提前提交或推送；最后不足 100 道的尾批次按实际已有题目完成后推送。

`PROGRESS.rst` 必须记录：总目标、可信完成的精确题号或范围、本次实际修改的文件、状态页或缺失文件、未完成原因、下一目标及其需要特别核对的语义。不得根据文件数量、旧提交或连续题号宣称未经逐题处理的范围已经完成。
