# AGENTS.md

本文件是 ChatGPT/Codex 处理本仓库时唯一的题解书写规则入口。仓库简介看 `README.rst`，当前进度看 `PROGRESS.rst`。

## 1. 控制文件边界

本仓库只使用三个控制文件：

1. `README.rst`：仓库目标和目录说明；
2. `AGENTS.md`：题解书写、审查和修改规则；
3. `PROGRESS.rst`：当前进度、跳过题目、当前任务和下一入口。

范围目录里的 `README.rst` 只维护题目索引，不新增全局规则。

不要创建、引用或恢复以下旧体系：

- `state/`
- `docs/`
- `archive/`
- `concepts/`
- 批次合同、概念账本、漂移日志、前向规则、质量门等拆分控制文件

如果发现旧路径残留引用，优先把它改回 `README.rst`、`AGENTS.md`、`PROGRESS.rst` 三文件模型。

## 2. 新对话读取顺序

每次接手任务时，只读取完成当前任务必要的文件：

1. 根 `README.rst`；
2. 根 `AGENTS.md`；
3. 根 `PROGRESS.rst`；
4. 当前题号所属范围的 `README.rst`；
5. 当前要生成、审查或修复的题解 RST。

不要为了确认格式而递归扫描最近题目。最近题目只能用于核对术语、平台接口、关联题目和目录进度，不能决定当前题的章节密度、证明长度或是否省略质量内容。

## 3. 任务粒度

默认一个对话只处理一道题。

允许的任务类型：

- 单题生成：创建下一道公开算法题的完整 RST；
- 单题审查：审查并修复一篇已有题解；
- 小修补丁：只修明确问题，例如路径、索引、include、错别字或进度记录；
- 进度同步：只更新 `PROGRESS.rst` 和必要范围索引。

除非用户明确要求，不连续生成下一题，不顺手审查其他题，不把规则维护和题解生成混在一起。

## 4. 目录和文件形态

题解按百题范围放置：

- `0001-0100/`
- `0101-0200/`
- `0201-0300/`

每道公开算法题只能有一个 `<题号>-<slug>.rst`。正文、证明、静态审查记录和全部语言代码必须直接内联在该文件中。

禁止：

- `.inc`；
- `.. include::`；
- 代码分片；
- 正文分片；
- 为 Premium、Database、Shell 或题面信息不足的题目创建空壳教程。

Premium、Database、Shell 或暂时无法可靠确认题面的题目，只在 `PROGRESS.rst` 和范围索引中登记。

## 5. 单题质量要求

普通算法题按实际需要覆盖以下内容。标题可以调整，相邻内容可以合并，但核心答案不能消失。

1. 精确契约：输入输出、值域、字符域、结构前提、平台接口、返回语义和副作用；
2. 自建示例：至少包含能解释算法选择或暴露典型错误的例子；
3. 问题抽象：说明题目被转化成什么算法模型；
4. 解法选择：解释为什么使用当前方法，必要时比较替代方法；
5. 状态与不变量：说明关键变量的含义、合法范围、更新时机和代码映射；
6. 正确性证明：覆盖当前题需要的合法性、完整性、最优性、无重复或终止性；
7. 复杂度：说明时间、核心空间、输入物化、输出载荷、递归栈和语言适配成本；
8. 十语言实现：固定提供 C、C++、Python、Java、Rust、Go、TypeScript、C#、Julia、R；
9. 语言边界：说明数值宽度、索引、字符单位、所有权、可变对象、资源释放或失败路径；
10. 边界和易错点：列出真正会影响正确性的边界，而不是泛泛而谈；
11. 静态审查：明确人工推演了什么、逐语言核对了什么、哪些结论未运行未编译未测试；
12. 自检问题：给出 2 到 3 个带答案要点的问题。

只有“算法摘要 + 正确性一句话 + 复杂度 + 十语言代码”的骨架不能通过。

## 6. 十语言顺序

普通算法题固定顺序如下：

1. C
2. C++
3. Python
4. Java
5. Rust
6. Go
7. TypeScript
8. C#
9. Julia
10. R

各语言可以采用符合语言习惯的写法，但必须保持同一题意和同一主算法语义。若某语言因平台限制采用不同适配方式，正文必须单独说明差异、成本和剩余风险。

## 7. 静态审查规则

题解代码默认且统一只做静态审查。

禁止：

- 运行题解代码；
- 调用编译器或解释器检查题解；
- 对拍；
- 穷举；
- 属性测试；
- sanitizer；
- 用目标语言最小程序验证题解。

允许：

- 人工推演官方示例；
- 人工推演关键边界；
- 静态核对接口、导入、索引、容器、所有权、位宽、字符单位、语法和正文到代码的映射；
- 检查 RST、目录索引和进度文件是否一致。

不得把静态审查写成“测试通过”“编译通过”或“运行通过”。

## 8. 常见高风险点

写题解时优先检查这些问题：

- C/C++：必要头文件、容量乘加溢出、资源失败清理、二维返回所有权；
- Rust：所有权移动、借用范围、`Option<Box<_>>`、`take()`、克隆成本；
- Go：切片和字符串是否共享底层存储；
- TypeScript：`number` 安全整数范围和 32 位位运算陷阱；
- Julia：一基索引、范围方向、字符串 `codeunits` 与 `collect` 的成本；
- R：一基索引、`seq.int` 方向、向量复制、environment 引用语义；
- 字符串题：明确字节、UTF-16 代码单元、Unicode 码点或题目限定字符域；
- 图和节点题：区分节点身份和值，说明平台对象是否复用、克隆或重连；
- 滑动窗口、二分、贪心、DP：证明排除候选或状态转移的安全性，不只复述代码。

## 9. 进度同步

完成单题生成、审查或修复后，根据实际改动同步：

- 当前范围目录的 `README.rst`；
- 根 `PROGRESS.rst`；
- 根 `README.rst` 中确实需要更新的总体说明。

不要在题解任务中新增控制文件。不要把临时分析、草稿、日志、marker 提交进仓库。

## 10. Git 工作方式

用户明确要求改仓库时，直接在 `main` 修改。不要创建分支、PR、上传分片分支或临时工作流。

每次修改只碰用户允许的文件。若用户限定文件范围，不得顺手修改其他文件。

提交或写入后，报告实际修改文件、提交 SHA 和仍需用户确认的点。
