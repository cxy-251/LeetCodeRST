# AGENTS.md

## 1. 项目目标

本仓库按 LeetCode 题号顺序建设多语言算法学习资料。每道公开可访问题目对应一个 RST 文件，目标是同时完成：

1. 原创重述题目与示例；
2. 讲清算法状态、正确性依据、复杂度和边界；
3. 提供固定核心语言的可靠实现；
4. 通过解释衰减与周期复现形成持续学习路径。

仓库处理公开题目和用户合法提供的材料。Premium 或信息不足的题目记录到状态文件，后续在材料完整时继续。

## 2. 新对话接手

每次工作从最新 `main` 开始，依次读取：

1. `AGENTS.md`；
2. `README.rst`；
3. `docs/PROJECT_VISION.rst`；
4. `docs/LANGUAGE_SCOPE.rst`；
5. `docs/SOLUTION_AND_TYPES_POLICY.rst`；
6. `docs/RST_STYLE_GUIDE.rst`；
7. 当前阶段对应的审查或生成规则；
8. `state/PROGRESS.toml`；
9. `state/REVIEW_INDEX.toml`；
10. `state/CONCEPT_LEDGER.toml`；
11. 本轮直接相关的题目与最近完成内容。

仓库文件是跨对话共享状态。接手后先说明当前阶段、已完成范围、本轮起点和下一步。

## 3. 对话驱动工作流

所有审查、生成和修复从用户在当前对话中的明确指令开始。

每轮按以下顺序完成：

1. 读取最新状态和相关内容；
2. 根据用户指令与状态文件确定本轮范围；
3. 一次性准备正文、代码、记录、索引和状态更新；
4. 按当前质量规则完成检查；
5. 将完整结果形成一个原子提交写入 `main`；
6. 重新读取 `main` 验证结果；
7. 在当前对话中报告完成内容、主要发现和下一步。

详细执行流程见 `docs/AUTOMATION_DIRECT_MAIN_POLICY.rst`。该文件名为历史兼容路径，实际内容是对话驱动执行流程。

## 4. 当前审查阶段

当 `state/PROGRESS.toml` 中 `phase = "review_extracting_rules"` 时：

1. 从 `state/REVIEW_INDEX.toml` 的 `review.current` 开始；
2. 按用户指定范围审查，未指定时默认连续五题；
3. 完整读取每题正文、证明、复杂度和十语言代码；
4. 将单题观察写入 `state/reviews/NNNN-NNNN.toml`；
5. 将详细证据写入 `docs/review-findings/NNNN-NNNN.rst`；
6. 将稳定的跨题模式提升到 `docs/FORWARD_RULES_0051_0100.rst`；
7. 同步 `REVIEW_INDEX.toml`、`PROGRESS.toml` 和两个 README；
8. 复查审查位置、完成数量和下一批范围。

`0001–0050` 在此阶段作为只读学习样本，审查结果用于提升 `0051–0100`。用户明确指定修复某道历史题时，再进入该题的修复流程。

## 5. 新题生成阶段

规则提炼完成后，从 `state/PROGRESS.toml` 的 `next_problem` 开始生成：

1. 按题号递增确定连续范围；
2. 使用 Easy=1、Medium=2、Hard=4 的难度预算控制单批工作量；
3. 每道题完成原创重述、自建示例、问题抽象、解法选择、正确性证明、复杂度、边界和十语言实现；
4. 应用 `docs/FORWARD_RULES_0051_0100.rst` 与质量门；
5. 同步题目 RST、范围 README、根 README、`PROGRESS.toml` 和 `CONCEPT_LEDGER.toml`；
6. 复查完成范围和新的 `next_problem`。

## 6. 内容结构

每道题根据实际教学需要组织以下内容：

1. 标题与题目信息；
2. 原创题目重述；
3. 自建示例；
4. 问题抽象；
5. 解法选择与取舍；
6. 主解法过程、状态或不变量；
7. 正确性依据；
8. 时间与空间复杂度；
9. 多语言实现；
10. 关键边界与易错点；
11. 新增与强化知识；
12. 关联题目；
13. 最小自检与答案要点。

简单题保持紧凑，复杂题提供足够推导。章节服务于理解，不为模板数量扩写内容。

## 7. 固定核心语言

普通算法题默认提供：

1. C；
2. C++；
3. Python；
4. Java；
5. Rust；
6. Go；
7. TypeScript；
8. C#；
9. Julia；
10. R。

数据库题使用 SQL；Shell 题使用 Bash 或 POSIX Shell。语言接口、基础类型和适配器约定见 `docs/LANGUAGE_SCOPE.rst` 与 `docs/SOLUTION_AND_TYPES_POLICY.rst`。

## 8. 解法与代码原则

### 8.1 主解法

选择在正确性、复杂度、可读性和通用性之间最平衡的方法，十语言实现保持同一问题语义。

### 8.2 对照解法

对照解法用于展示实质差异，例如复杂度变化、状态设计变化、暴力到优化、递归到动态规划或语言特化实现。普通风格差异直接吸收到主解法中。

### 8.3 标准库

优先使用可靠的标准库容器和算法，并说明关键 API 的语义、复杂度与前提。当标准库完全隐藏本题核心时，同时提供工程写法和教学写法。

### 8.4 平台类型

`ListNode`、`TreeNode`、`Node` 等平台类型首次出现时解释结构，后续复用统一约定。Julia 使用 `mutable struct` 表达可变节点，R 使用 `environment` 表达引用语义。

## 9. 教学模型

语言语法和常用库按以下状态推进：

- `introduced`：首次完整解释；
- `reinforcing`：后续保留关键提醒；
- `familiar`：普通使用直接出现；
- `refresh`：长时间未出现或出现新边界时复习；
- `mastered`：聚焦特殊语义、性能和风险。

动态规划、单调结构、并查集、图算法、字符串算法、高级数据结构、数学推导和回溯剪枝采用周期复现，每次聚焦当前题的新状态与新边界。

代码注释靠近对应语句，重点解释状态变化、原因和易错边界。代码行优先控制在 88 个显示列以内，硬上限为 100。

## 10. 图与文档形式

RST 是主要内容格式。Mermaid 用于链表改向、树图结构、状态转移、递归回溯等文字难以清楚表达的过程。每张图只承担一个核心解释目标，并配简短说明。

仓库以直接阅读源文件为主，检查重点包括：

- RST 标题层级与指令缩进；
- 代码块语言标记；
- Mermaid 语法与可读性；
- 相对链接和文件路径；
- 题号、文件名和状态账本一致；
- 代码、证明、复杂度与讲解一致；
- 行宽和注释密度符合规则。

## 11. Git 工作方式

日常对话采用直接 `main` 工作流：

1. 写入前读取最新 `main`；
2. 一轮工作准备全部目标文件；
3. 形成一个完整原子提交；
4. 提交后重新读取并验证；
5. 提交说明写清工作类型、题号范围和状态变化。

用户选择代码评审、多人协作或隔离实验时，本轮采用用户指定的分支与 PR 流程。

## 12. 来源与正确性

技术事实和语言版本信息优先使用官方资料。题目讲解、代码与示例保持原创。公开题目以可确认的题意和接口为依据；信息不足时在状态文件登记，等待材料完整后继续。

## 13. 交接结果

每轮结束时确认：

- `PROGRESS.toml` 指向准确阶段和下一步；
- `REVIEW_INDEX.toml` 反映真实审查位置；
- `CONCEPT_LEDGER.toml` 反映本轮知识状态；
- README、题目索引和规则文件已经同步；
- 最近提交能够独立说明本轮范围与结果；
- 下一轮从用户新的对话指令开始。
