# AGENTS.md

本文件规定 ChatGPT/Codex 在本仓库中生成、修改和接续 LeetCode 多语言代码题解的工作方式。仓库简介看 `README.rst`，下一次接续任务看 `PROGRESS.rst`。

## 1. 接手读取顺序

每次接手任务时，按顺序读取：

1. 根 `README.rst`；
2. 根 `AGENTS.md`；
3. 根 `PROGRESS.rst`；
4. 当前题号所属范围的 `README.rst`；
5. 当前要生成或修改的题解 RST。

最近题解只用于核对文件命名、语言顺序、平台接口和目录进度。当前题按本文件规则生成。

## 2. 任务粒度

默认一个对话处理一道题。

常见任务类型：

- 单题生成：创建下一道公开算法题的代码优先 RST；
- 单题修改：修改一篇已有题解中的代码、接口、路径、索引或文字问题；
- 小修补丁：修正明确的小问题，例如 include、错别字、目录链接或进度记录；
- 进度同步：更新 `PROGRESS.rst` 和必要范围索引。

用户明确要求多题批次时，按题号连续处理，并在输出开始压缩时收束到已完成的连续前缀。

## 3. 目录和文件形态

题解按百题范围放置：

- `0001-0100/`
- `0101-0200/`
- `0201-0300/`

每道公开算法题使用一个 `<题号>-<slug>.rst` 文件。全部代码和必要说明直接写入该文件。

Premium、Database、Shell 或暂时无法可靠确认题面的题目，在 `PROGRESS.rst` 和范围索引中登记。

## 4. 单题最低内容

普通算法题以代码为主。标题和章节可以简化，至少保留：

1. 题号和英文题名；
2. 必要的平台接口或输入输出说明；
3. 固定十语言代码；
4. 必要时保留很短的思路、复杂度或语言差异说明。

题解文字保持短，重点交付可读、可复制、可由读者自行运行验证的代码。

## 5. 十语言顺序

普通算法题固定顺序如下：

1. C
2. C++
3. Python
4. Java
5. Rust
6. Go
7. TypeScript
8. C#
9. Julia
10. R

各语言采用符合语言习惯的写法，并尽量保持同一题意和同一主算法语义。某语言需要不同适配方式时，用短句说明差异。

## 6. 验证责任

仓库交付代码文本。运行、编译、调试和判题验证由读者完成。

用户明确要求验证、审查或修 bug 时，按用户指定范围处理。

## 7. 代码生成注意点

生成代码时优先照顾这些常见问题：

- C/C++：必要头文件、数组边界、容量和资源释放；
- Rust：所有权移动、借用范围、`Option<Box<_>>` 和 `take()`；
- Go：切片和字符串共享；
- TypeScript：`number` 安全整数范围；
- Julia/R：一基索引和范围方向；
- 字符串题：字符单位与题目限定字符域；
- 图和节点题：节点身份和值的区别。

这些注意点用于提升代码可用性，题解中保持短说明。

## 8. 进度同步

完成单题生成、修改或进度任务后，根据实际改动同步：

- 当前范围目录的 `README.rst`；
- 根 `PROGRESS.rst`；
- 根 `README.rst` 中确实需要更新的总体说明。

临时分析、草稿和日志留在对话中，最终仓库只保存题解、索引和三个控制文件。

## 9. Git 工作方式

用户明确要求改仓库时，直接在 `main` 修改。

每次修改只碰用户允许的文件。若用户限定文件范围，按该范围执行。

写入后报告实际修改文件、提交 SHA 和仍需用户确认的点。
