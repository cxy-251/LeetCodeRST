# AGENTS.md

## 1. 项目使命

本仓库用于建设一套按 LeetCode 题号顺序推进的多语言算法学习资料。每道可访问题目对应一个 reStructuredText（RST）文件，内容同时承担三项任务：

1. 解释题目与算法；
2. 展示多种编程语言的可靠实现；
3. 通过有节制、会衰减、会复现的注释帮助读者逐步掌握各语言。

本项目不是题库镜像。题目描述必须使用原创重述，并保留原题链接；不得复制 LeetCode 的完整题干、官方题解、Premium 内容或测试数据。

## 2. 任意助手接手时的必读顺序

开始任何工作前，依次读取：

1. `AGENTS.md`；
2. `README.rst`；
3. `docs/PROJECT_VISION.rst`；
4. `docs/RST_STYLE_GUIDE.rst`；
5. `state/PROGRESS.toml`；
6. `state/CONCEPT_LEDGER.toml`；
7. 最近至少 5 道已完成题目；
8. `main` 最近的相关提交。

不得只根据当前对话记忆继续生成。仓库文件是跨对话、跨助手的进度依据。

## 3. 不可违背的核心规则

### 3.1 严格按题号推进

题目按 LeetCode 编号递增处理。编号顺序让数组、链表、树、图、动态规划、字符串等知识自然交错，形成间隔复习。

除修复历史文件外，不得跳过可访问的普通题目。遇到无法访问的会员题时，登记为 `premium_skipped`，继续下一个编号。

### 3.2 一题一个 RST 文件

路径格式：

```text
problems/0001-0100/0001-two-sum.rst
problems/0101-0200/0101-symmetric-tree.rst
```

文件名使用四位题号和英文 slug。题号超过四位后自然扩展位数。

题目正文、项目说明、教学规范以 `.rst` 为主。`AGENTS.md` 保留 Markdown，状态账本保留 TOML。

### 3.3 会员题处理

助手不得绕过付费墙、借用用户登录态、抓取会员题干或复制 Premium 官方内容。

可用信息不足时：

- 不猜题意；
- 不伪造示例；
- 不根据同名文章拼凑内容；
- 在 `state/PROGRESS.toml` 中登记题号和原因；
- 后续获得用户提供的合法材料时再补写。

### 3.4 不建立编译或构建系统

本仓库以直接阅读 RST 源文件为主，不需要 Sphinx、文档站点、CI 构建、HTML/PDF/EPUB 生成或自动发布流程。

检查重点是：

- RST 标题层级和下划线长度合理；
- 指令缩进正确；
- 代码块语言标记正确；
- 内部链接和文件路径存在；
- 题号、文件名和状态账本一致；
- 代码与讲解逻辑一致。

不得为了“工程完整”添加与学习内容无关的配置、依赖或脚本。

### 3.5 多语言覆盖采用渐进策略

长期目标是覆盖 LeetCode 支持的主要语言。每道题可以分阶段补齐语言，正确性优先于数量。

任何语言实现都必须：

- 与该题当前接口相符；
- 复杂度与正文一致；
- 使用该语言自然、常见的写法；
- 不机械逐字翻译另一种语言；
- 不为凑语言数量提交未经检查的代码。

## 4. 教学模型

### 4.1 语法知识采用解释衰减

语言语法和常用库会高频重复，解释强度按状态变化：

- `introduced`：第一次出现，完整解释用途、语义和本题原因；
- `reinforcing`：后续一到数次出现，只保留一句关键提醒；
- `familiar`：普通使用不再解释；
- `refresh`：长时间未出现、出现新边界或容易混淆时重新提醒；
- `mastered`：仅在特殊语义、性能或错误风险下解释。

不得在每道题中重复解释 `for`、普通变量声明、常规条件分支等基础内容。

### 4.2 疑难算法采用周期性复现

算法知识与语法知识的衰减策略不同。下列内容即使曾经出现，也需要在再次成为核心时重新解释：

- 动态规划状态设计与转移来源；
- 单调栈、单调队列；
- 并查集；
- 拓扑排序；
- 最短路、最小生成树；
- 字典树、KMP、滚动哈希；
- 回溯剪枝；
- 树形 DP、区间 DP、状态压缩 DP；
- 线段树、树状数组；
- 数学推导、组合计数；
- 任何依赖不变量、证明或复杂状态定义的解法。

再次解释时聚焦本题的新结构、状态变化、失败边界，以及它与旧题的差异。

### 4.3 注释预算

代码注释只用于以下情况：

1. 该语言特性首次出现；
2. 写法具有语言特有语义；
3. 该行容易写对语法却写错逻辑；
4. 该行承载算法关键状态或不变量；
5. 当前写法与此前写法存在重要差异。

禁止把代码逐行翻译成中文。注释应解释“为什么这样写”“状态发生了什么变化”“哪里容易错”。

### 4.4 每题只投入必要精力

简单题可以短，复杂题需要充分解释。文档长度由知识密度决定，不追求固定字数。

每道题优先讲清：

- 问题抽象；
- 核心不变量或状态；
- 为什么当前算法成立；
- 复杂度；
- 容易失败的边界；
- 本题真正新增或需要强化的语言知识。

## 5. 单题推荐结构

题目文件通常包含：

1. 标题与题目信息；
2. 原创题目重述；
3. 自建示例；
4. 问题抽象；
5. 解法与正确性依据；
6. 复杂度；
7. 多语言实现；
8. 易错点；
9. 本题新增知识；
10. 本题强化知识；
11. 关联题目；
12. 最小自检。

结构可以随题目调整。不得为了模板完整制造无价值章节。

## 6. 状态管理

### 6.1 `state/PROGRESS.toml`

记录：

- 下一题号；
- 已完成范围；
- 当前批次；
- 跳过的会员题；
- 阻塞项和待修订项；
- 最近一次提交及其说明。

### 6.2 `state/CONCEPT_LEDGER.toml`

记录算法和语言知识点的教学状态。每个知识点至少包含：

- 唯一 ID；
- 类型：`syntax`、`library`、`algorithm`、`data_structure`、`proof`；
- 首次出现题号；
- 最近出现题号；
- 出现次数；
- 当前教学状态；
- 是否属于需要周期复现的疑难内容。

每批题目完成时，必须同步更新题目文件、进度和知识账本。

## 7. 每批工作流程

1. 读取状态文件和最近至少 5 道已完成题目；
2. 从 `next_problem` 开始确定连续题号范围；
3. 判断题目是否公开可访问；
4. 对会员锁定题登记跳过；
5. 为可访问题编写原创 RST；
6. 选择有教学价值的解法，不强行堆砌低价值解法；
7. 编写并检查多语言实现；
8. 根据知识账本决定注释密度；
9. 更新进度与知识账本；
10. 检查 RST 结构、题号、路径和代码逻辑；
11. 直接提交到 `main`。

项目早期每批建议 3～5 题，用于稳定模板和教学密度。规范稳定后可扩大到 10～20 题。

## 8. Git 提交规则

默认直接修改 `main`，不得自行创建新分支或 PR。只有用户明确要求时才使用分支或 PR。

每个提交应当是可理解的完整批次，提交说明需要体现：

- 题号范围或规则变更；
- 完成题数；
- 跳过的会员题；
- 状态账本是否同步更新。

不要把大量互不相关的变更塞进同一提交。

## 9. 正确性与来源边界

- 技术事实和语言版本信息优先查官方文档；
- 题目讲解和代码必须原创；
- 不复制 LeetCode 官方题解；
- 不大段复制其他题解网站；
- 题目链接可以保留；
- 输入输出示例自行构造；
- 无法确认题意或接口时登记阻塞，不自行猜测。

## 10. 交接协议

每次停止工作前，确保：

- `state/PROGRESS.toml` 指向准确的下一题；
- 知识账本反映本批实际内容；
- 最近提交说明本批范围和遗留事项；
- 未完成内容不伪装为完成；
- 新助手仅阅读仓库即可理解下一步。

新的对话接手时，应先报告：当前完成范围、下一题号、最近引入的知识点、需要复现的疑难算法和当前阻塞项，然后继续工作。