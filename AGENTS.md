# AGENTS.md

本文件规定 ChatGPT/Codex 在本仓库中持续整理 LeetCode 题目 RST 的当前长期任务。每次接续先读取 `PROGRESS.rst`，再按本文件执行。

## 1. 当前长期任务

从第 0001 题开始，按题号顺序处理仓库中的全部 LeetCode 题目。

当前阶段只负责每个题目 RST 的前三个小节：

1. `题目信息`；
2. `题目重述`；
3. `自建示例`。

现有文件中的 `C++ 实现`、`题解` 以及其后的全部内容不属于当前任务，不得修改。

第 0001 题的结构是本阶段的小节边界参考：允许修改的范围从 `题目信息` 标题开始，到 `C++ 实现` 标题之前结束。

## 2. 分支、文件与提交约束

- 只允许在 `main` 分支直接修改和提交；
- 不创建工作分支，不创建 Pull Request；
- 只能修改或删除 `main` 上已经存在的文件；
- 严禁创建任何新文件或新目录，包括题目 RST、README、工作流、临时数据、脚本、配置、状态页或中间产物；
- 不得借助 GitHub Actions、临时文件或新建脚本间接完成修改；
- 已有题目必须在原 RST 文件上修改；
- 发现缺失题号或缺失文件时不得补建，只在 `PROGRESS.rst` 中记录实际状态并继续；
- 全仓库只允许根目录存在一个 README，即 `README.rst`；任何子目录中的 `README.rst`、`README.md` 或其他大小写变体都必须删除，且不得重新创建；
- 每完成一道题，同步更新根目录 `PROGRESS.rst`，使下一次对话能够从下一题继续；
- 题目 RST 与对应的 `PROGRESS.rst` 应组成同一项连续任务，提交信息直接说明完成的题号与前三节整理。

## 3. 必须逐题理解，禁止机械替换

题目内容的删改必须建立在对该 LeetCode 题目本身的理解上。

处理每道题时必须：

1. 阅读目标 RST 当前内容；
2. 确认题目的输入、输出、限制、保证条件和特殊语义；
3. 判断现有表述和示例是否正确；
4. 针对该题独立重写或修正前三个小节；
5. 验证自建示例的输入、输出和解释能够互相对应。

禁止使用脚本、正则、固定模板或批量替换方式直接生成、改写或覆盖多道题的题目重述和示例。不得把同一套句式机械套到连续题目中。

脚本只可用于辅助列出文件、检查缺失题号、定位小节边界或检查 RST 格式；脚本不得代替题意理解，不得批量生成题目文案、示例或解释。由于本仓库禁止新建文件，辅助脚本只能使用仓库外已有工具执行，不能写入仓库。

## 4. 现有 RST 的修改边界

对于已经存在的完整题目 RST：

- 不修改文件主标题；
- 只修改 `题目信息`、`题目重述`、`自建示例` 三个小节；
- `C++ 实现` 标题及其后的内容必须保持原样；
- 即使后续代码或题解存在错误，本阶段也不顺手修正；
- 不调整后续小节标题下划线、空行、代码缩进、注释或措辞；
- 若前三个小节缺失或顺序错误，只在 `C++ 实现` 之前补齐和整理；
- 现有前三节中的错误、遗漏、抄录式内容和无效示例可以直接删除、替换或重写；
- 已经正确且符合本规则的内容可以保留，不为制造改动而重写。

对于仓库中不存在的题目文件，无论题目是否公开、是否属于算法题，都不得创建。只在 `PROGRESS.rst` 中记录缺失状态，并继续处理下一个已经存在的文件。

对于 Premium、Database、Shell 或无法可靠确认完整题目契约的题目，不得凭记忆编造内容。保留现有状态文件，并在 `PROGRESS.rst` 中记录实际处理状态后继续推进。

## 5. 题目信息

`题目信息` 采用第 0001 题的字段形式：

- `:题号:`；
- `:难度:`；
- `:主题:`；
- `:原题:`；
- `:重点:`。

要求：

- 题号、难度、主题和链接与当前题目一致；
- `:重点:` 只概括理解题目所需的关键机制、接口边界或容易误解之处；
- 不在本节提前展开算法题解；
- 不保留与实际题目无关的模板词。

## 6. 题目重述

题目重述必须使用独立中文表达，不直接复制 LeetCode 官方题面。

重述应根据具体题目准确覆盖会影响实现的契约，包括但不限于：

- 输入对象及每个参数的含义；
- 需要返回或修改的结果；
- 下标从零还是从一开始；
- 是否要求不同位置、唯一答案、去重或特定顺序；
- 是否允许修改输入、是否要求原地完成；
- 数组、字符串、矩阵、链表、树、图或对象的结构语义；
- 长度范围、数值范围、字符范围和平台保证；
- 多次调用之间是否共享对象状态；
- 存在多个合法答案时允许返回什么。

只写题目契约和理解所需说明，不在本节讲完整算法、复杂度或代码实现。

不得为了简短而遗漏关键条件，也不得加入原题没有的限制或保证。

## 7. 自建示例

每道公开算法题建立 2 至 3 个原创示例。示例必须在理解题意后单独设计，不直接复制官方示例、官方说明或提示中的场景，也不能只对它们机械更换数字或字符。

示例组合至少覆盖：

- 一个体现题目核心机制、主要输出或状态变化的场景；
- 一个满足原题约束的边界场景，例如最小规模、空结果、无解、重复值、并列选择、极端合法取值或对象状态边界。

不能为了制造边界而使用原题不允许的空数组、空字符串、非法下标或其他越界输入。复杂题可建立第三个示例，补充另一种关键语义或容易误解的条件。

每个示例至少包含：

- 输入；
- 输出；
- 解释。

示例要求：

- 输入满足题目全部约束；
- 输出确实由该输入得到；
- 解释说明为什么得到该结果，并把示例要表达的边界条件写清楚；
- 多种答案均合法时，说明当前输出只是其中一种；
- 根据题目选择能够体现核心语义、边界、重复值、顺序、对象变化或容易误解条件的场景；
- 解释服务于理解题目，不提前展开完整算法题解；
- RST 代码块、列表、表格和缩进保持合法。

## 8. 单题完成条件

一道题只有同时满足以下条件，才可在 `PROGRESS.rst` 中推进到下一题：

1. 已逐项确认题目契约；
2. `题目信息` 与实际题目一致；
3. `题目重述` 独立、完整且没有新增错误条件；
4. 至少两个自建示例均为独立设计，其中至少一个明确覆盖合法边界条件；
5. 示例输入、输出和解释一致；
6. 仅修改了允许的前三个小节；
7. `C++ 实现` 及其后的原有内容没有变化；
8. RST 结构仍然有效；
9. 没有创建任何新文件或目录。

## 9. PROGRESS.rst 接力规则

`PROGRESS.rst` 是下一次对话的唯一当前任务指针，应持续写明：

- 当前目标题号和文件路径；
- 当前阶段只修改前三个小节；
- 本题需要重点核对的题目语义；
- 完成条件；
- 下一题入口。

长期顺序从 `0001` 开始，按题号连续推进。发现缺失文件时只记录状态，不创建文件，也不静默跳号。
