# AGENTS.md

本文件规定 ChatGPT/Codex 在本仓库中生成、审查、整改和接续 LeetCode 多语言分析题解的长期规则。仓库简介看 `README.rst`；每轮具体任务以 `PROGRESS.rst` 为准。

## 1. 接手与任务判定

每次接手任务按以下顺序开始：

1. 读取根 `PROGRESS.rst`，确认目标题号、目标文件、工作重点和完成后入口；
2. 读取根 `AGENTS.md`，确定文件类型、题解结构和任务闭环；
3. 确认目标题目的类型与公开状态；
4. 读取最新 `main` 上的目标 RST；
5. 按题目类型和目标文件状态执行本轮任务。

题目类型决定文件内容：

- 公开算法题使用完整题解 RST；
- Premium、Database、Shell 和题面暂时无法可靠确认的题目使用状态 RST。

公开算法题按目标文件状态处理：

- 目标 RST 已存在：读取全文，审查并整改现有题解；
- 目标 RST 尚未建立：创建完整的分析型多语言题解。

状态题目按目标文件状态处理：

- 目标 RST 已存在：核对并整理状态信息；
- 目标 RST 尚未建立：创建对应题号的状态 RST。

## 2. 任务范围

正常一轮处理一道题，并修改两个文件：

1. `PROGRESS.rst` 指定的目标题号 RST；
2. 根 `PROGRESS.rst`。

用户明确指定多题批次时，按题号连续处理，并保持每道公开算法题的分析完整度一致。批次完成后把下一入口写入 `PROGRESS.rst`。

## 3. 文件与目录

题目文件按百题范围放置：

- `0001-0100/`
- `0101-0200/`
- `0201-0300/`

每个题号使用一个 `<题号>-<slug>.rst` 文件：

- 公开算法题文件包含题目信息、题目重述、自建示例、C++ 多解法、题解正文和九语言实现；
- 状态题目文件包含题号、题名、类型、原题链接和处理状态。

## 4. 状态 RST

Premium、Database、Shell 和题面暂时无法可靠确认的题目使用以下结构：

```rst
0156. Binary Tree Upside Down
=============================

:题号: 0156
:题名: Binary Tree Upside Down
:类型: Premium
:原题: `LeetCode 0156 <https://leetcode.com/problems/binary-tree-upside-down/>`_
:处理状态: Premium 题目，当前以状态页保留题号位置。
```

状态 RST 在处理状态之后结束。后续取得完整公开题面并决定纳入题解时，在原文件路径上扩展为完整题解。

## 5. 公开算法题定位

本仓库的公开算法题产出分析型题解。每篇题解完成三件事：

1. 用自己的话讲清题目要求、平台接口和实现边界；
2. 把题面抽象为明确的算法模型，并讲清代表性解法之间的取舍；
3. 用 C++ 展示代表性解法，再给出主解法的九语言实现。

代码先给出可落地的实现，后续分析围绕代码中的函数、状态和更新顺序展开。正文保持具体、完整和紧凑。

## 6. 固定题解结构

公开算法题按以下顺序组织：

1. **题目信息**；
2. **题目重述**；
3. **自建示例**；
4. **C++ 实现**；
5. **题解**；
6. **九语言实现**。

九语言实现是本题正文的结束位置。标题可以根据题目微调，整体顺序保持一致。简单题合并题解内部的小节；困难题围绕关键机制继续拆分。

## 7. 题目信息、题目重述与自建示例

### 7.1 题目信息

题目信息包括：

- 题号；
- 难度；
- 主题；
- 原题链接；
- 教学重点。

### 7.2 题目重述

题目重述使用自己的话提取决定实现的契约，按题目说明：

- 输入与输出；
- 返回下标、节点、对象或结果集合的语义；
- 长度范围、数值范围、字符域或节点模型；
- 平台保证的条件；
- 输入修改方式；
- 对象状态的跨调用关系；
- 返回顺序、唯一性、去重或原地修改要求。

### 7.3 自建示例

自建示例通常选择 1 至 3 个能够暴露核心机制或实现边界的输入，例如：

- 重复值或重复字符；
- 边界下标与空结果；
- 同坐标事件；
- 对象状态变化；
- 需要回退、恢复或去重的场景；
- 能体现关键更新顺序的反例。

每个示例承担一个明确的解释任务。

## 8. C++ 多解法实现

C++ 是本仓库的主语言。C++ 实现放在题解分析之前，用一个完整代码块展示本题具有独立思想价值的代表性解法。

### 8.1 解法集合

通常选择 2 至 4 种代表性方法：

- 最直接的基准解法；
- 综合最优的主解法；
- 具有不同核心思想或实际取舍的替代解法。

解法集合聚焦算法模型、数据结构或复杂度具有实质差异的方法。仅有一种独立思路的题目保留该主解法。

### 8.2 代码组织

一个 C++ 代码块中使用不同函数表示不同解法，函数名直接表达算法，例如：

- `bruteForce`；
- `sortAndTwoPointers`；
- `onePassHash`。

LeetCode 标准入口保留题目要求的函数名，并调用最终选定的主解法。代码块包含必要的头文件、类型和辅助函数，使每种方法形成完整实现。

### 8.3 轮子与辅助函数

算法依赖且标准库没有直接提供的轮子在代码块中完整体现，例如：

- 自建哈希表、堆、队列或并查集；
- Trie 节点与资源释放；
- 二分边界、图遍历或字符串解析辅助函数；
- 平台类型之外需要定义的适配结构。

标准库提供合适容器时直接使用。辅助函数的说明集中在正确性、复杂度、边界和资源管理。

## 9. 题解正文

题解正文位于 C++ 代码之后，围绕前面已经出现的函数展开。

### 9.1 问题抽象

问题抽象指出题面背后的算法模型，并建立状态、查询和答案之间的关系。例如：

- 哈希题：当前步骤查询的前缀状态、补数或最近位置；
- 滑动窗口题：窗口有效条件、扩张条件和收缩条件；
- 图题：节点、边方向、层次、入度或访问状态；
- 动态规划题：状态语义、转移来源和遍历顺序；
- 数据结构题：对象状态和操作后维持的性质。

抽象直接解释前面 C++ 函数的设计。

### 9.2 各解法说明

按照 C++ 代码中的函数顺序解释每种代表性解法：

- 维护的状态；
- 得到答案的过程；
- 相比前一种方法减少的重复工作；
- 引入的额外空间、排序、输入处理或实现成本。

基准解法和替代解法使用紧凑篇幅讲清思想和取舍。

### 9.3 解法对比与主解法选择

用表格或紧凑段落比较代表性解法的时间、空间和实际代价，并明确九语言采用的主解法及选择原因。

比较反映真实实现，包括排序副本、哈希平均与最坏情况、递归栈、输出载荷、字符串物化、节点复用和语言适配成本。

### 9.4 主解法解释

主解法解释覆盖：

1. 关键状态的含义；
2. 算法运行步骤；
3. 查询、判断、写入、移动、回退或恢复的顺序；
4. 合法答案的覆盖与返回结果的正确性；
5. 时间和空间复杂度的来源。

状态名、函数名、判断条件和更新顺序与 C++ 主解法及九语言实现保持一致。

Easy 题把状态、过程、正确性和复杂度合并为少量段落；Medium 和 Hard 题按算法关键机制拆分。

## 10. 九语言实现

C++ 在前文完整展示。九语言固定顺序为：

1. C
2. Python
3. Java
4. Rust
5. Go
6. TypeScript
7. C#
8. Julia
9. R

九语言实现题解中最终选定的主解法，并保持同一题意、同一关键状态和同一更新顺序。平台接口采用该语言常见的 LeetCode 提交形式。

某种语言因平台接口、标准库、所有权、索引或对象模型采用不同实现时，在对应代码附近说明：

1. 适配原因；
2. 相比 C++ 主解法发生的变化；
3. 该语言实现的实际时间和空间复杂度。

语言实现重点：

- C：必要头文件、容量计算、返回数据所有权、资源释放和必需轮子；
- Python：容器语义、可变对象与递归边界；
- Java、C#：标准容器、对象模型和平台接口；
- Rust：所有权、借用、节点表示和必要的内部可变性；
- Go：切片、字符串和底层存储关系；
- TypeScript：`number` 精度、位运算和查询结果的真假判断；
- Julia、R：一基索引、范围、返回接口和可变对象表达；
- 字符串题：字节、代码单元或题目限定字符域。

## 11. 代码注释

代码块在关键位置使用中文注释，优先采用简短的中文行尾注释，使注释紧邻对应语句。

注释集中解释：

- 非显然的状态含义；
- 关键更新顺序；
- 边界哨兵与索引转换；
- 溢出、精度和字符编码限制；
- 节点复用、所有权和资源释放；
- 语言特有的容器查询语义；
- 自建轮子的冲突处理、容量和生命周期。

较长说明使用前置的一至两行中文注释。

## 12. 对象身份与修改语义

链表、树、图和对象设计题说明：

1. 节点或对象按身份区分，还是只按值区分；
2. 实现采用复用、复制、重连节点或字段修改；
3. 输入结构的最终状态和调用者可观察的修改；
4. 对象状态的构造与跨方法调用关系；
5. 返回结构的所有权、共享关系和生命周期。

正文中的对象语义与 C++ 多解法及九语言代码保持一致。

## 13. 现有文件审查与整改

公开算法题 RST 已存在时，读取全文并完成以下审查：

1. 组织为“题目信息 → 题目重述 → 自建示例 → C++ 实现 → 题解 → 九语言实现”；
2. 题目契约、平台接口、输入范围和返回语义准确；
3. C++ 在一个代码块中覆盖具有独立思想价值的代表性解法；
4. 问题抽象解释算法模型和状态关系；
5. 各解法说明、对比和主解法选择与 C++ 函数一致；
6. 主解法的状态、判断顺序、正确性和复杂度映射到代码；
7. 九语言完整实现同一主解法，并包含必要轮子和语言适配；
8. 关键位置使用简洁的中文代码注释；
9. 正文以九语言实现结束；
10. RST 标题、章节、代码块、链接、表格和数学表达完整可读。

状态 RST 已存在时，核对题号、题名、类型、原题链接和处理状态，并整理为第 4 节规定的结构。

已有正确内容继续保留并整理。整改结果直接写回目标 RST，形成该题号的最终文件。

## 14. 篇幅控制

简单题完整保留固定结构，各部分保持紧凑。

中等题完整讲清问题抽象、代表性解法、主解法状态、正确性和复杂度。

困难题围绕决定实现的关键机制展开，例如惰性删除、同坐标整体处理、状态压缩、图搜索层次、回溯恢复或数据结构不变量，并合并同义证明和重复语言说明。

每段正文解决一个具体问题：为什么这样建模、为什么这样实现、为什么选择当前主解法、为什么更新顺序正确、为什么复杂度成立，或为什么某种语言需要当前适配。

## 15. 验证方式

仓库交付分析和代码文本。用户明确指定验证或运行代码时，按指定范围执行。

## 16. 完成与接力

完成每个题号后：

1. 根据实际完成结果完整重写根 `PROGRESS.rst`；
2. 在新的 `PROGRESS.rst` 中写明下一轮题号、目标文件、工作重点和完成后入口；
3. 让 `PROGRESS.rst` 保持为面向下一次对话的当前任务快照；
4. 将目标题号 RST 与新的 `PROGRESS.rst` 组成一个提交。

下一次接手时先读新的 `PROGRESS.rst`，再按本文件规则继续。

## 17. Git 工作方式

用户要求修改仓库时，直接在 `main` 上完成。每轮将目标题号 RST 和 `PROGRESS.rst` 作为同一个原子提交写入；仓库规则变化时同步更新对应控制文件。