# AGENTS.md

## 1. 项目使命

本仓库按 LeetCode 题号顺序建设多语言算法学习资料。每道可访问题目对应一个 reStructuredText（RST）文件，同时完成三件事：

1. 讲清题目、算法与正确性依据；
2. 展示固定核心语言的可靠实现；
3. 通过会衰减、会复现的注释帮助读者逐步掌握语言。

本项目不是题库镜像。题目必须原创重述，不复制 LeetCode 的完整题干、官方题解、Premium 内容或测试数据。

## 2. 新对话接手顺序

开始任何工作前依次读取：

1. `AGENTS.md`；
2. `README.rst`；
3. `docs/PROJECT_VISION.rst`；
4. `docs/LANGUAGE_SCOPE.rst`；
5. `docs/SOLUTION_AND_TYPES_POLICY.rst`；
6. `docs/RST_STYLE_GUIDE.rst`；
7. `state/PROGRESS.toml`；
8. `state/CONCEPT_LEDGER.toml`；
9. 最近至少 5 道已完成题目；
10. `main` 最近的相关提交。

不得依赖上一段对话的隐含记忆。仓库文件是跨对话、跨助手的唯一进度依据。

## 3. 题号与访问规则

### 3.1 严格按题号推进

题目按 LeetCode 编号递增处理。编号顺序让数组、链表、树、图、动态规划、字符串等知识自然交错，形成间隔复习。

除修复历史文件外，不得跳过可访问的普通题目。

### 3.2 Premium 题目

助手不得绕过付费墙、借用用户登录态、抓取会员题干或复制 Premium 官方内容。

信息不足时：

- 不猜题意；
- 不伪造示例；
- 不从同名文章拼凑内容；
- 在 `state/PROGRESS.toml` 登记为 `premium_skipped`；
- 继续处理下一个公开题号；
- 后续获得用户提供的合法材料时再补写。

## 4. 文件与语言范围

### 4.1 一题一个 RST 文件

路径格式：

```text
problems/0001-0100/0001-two-sum.rst
problems/0101-0200/0101-symmetric-tree.rst
```

文件名使用四位题号和英文 slug。题号超过四位后自然扩展位数。

题目正文、项目说明和教学规范以 `.rst` 为主；`AGENTS.md` 使用 Markdown；状态账本使用 TOML。

### 4.2 固定核心语言

普通算法题默认提供以下 10 种语言的主解法：

1. C
2. C++
3. Python
4. Java
5. Rust
6. Go
7. TypeScript
8. C#
9. Julia
10. R

数据库题使用 SQL；Shell 题使用 Bash 或 POSIX Shell。不得自行增加其他核心语言。完整边界见 `docs/LANGUAGE_SCOPE.rst`。

## 5. 多种解法

### 5.1 主解法

每题选择一个在正确性、复杂度、可读性和通用性之间最平衡的主解法。主解法默认提供 10 种核心语言的完整实现。

必须讲清：

- 问题抽象；
- 核心状态或不变量；
- 为什么成立；
- 时间与空间复杂度；
- 关键边界；
- 语言实现中的必要差异。

### 5.2 对照解法

第二种及后续解法只在存在实质教学差异时保留，例如：

- 复杂度发生变化；
- 数据结构或状态表示不同；
- 展示暴力到优化、递归到动态规划等算法演进；
- 能解释主解法为什么更优；
- 某种语言存在真正不同的惯用实现。

对照解法通常只给算法说明、伪代码，或 1 至 2 种代表语言。只有多种方法都属于本题核心目标时，才全部覆盖 10 种语言。

不得为数量保留只改变量名、循环方向、容器 API 或代码风格的低价值变体。

## 6. 标准库与手写轮子

### 6.1 默认使用可靠轮子

题目允许时，优先使用语言的标准库哈希表、集合、队列、双端队列、堆、排序、二分查找等。标准库本身属于语言学习内容。

代码注释需要说明关键 API 的语义、复杂度、前提和边界，不能把轮子当作无需理解的黑盒。

### 6.2 轮子隐藏核心算法

若一个内置函数几乎完全隐藏了本题要求学习的算法，采用双轨表达：

1. `工程写法`：展示实际开发中的标准库方案；
2. `教学写法`：实现本题真正需要理解的算法过程。

工程写法可以很短；教学写法承担主要解释和多语言实现。

### 6.3 需要手写的情况

仅在下列情况下手写基础结构或算法：

- 题目本身要求实现它；
- 标准库改变题目复杂度、语义或约束；
- 当前知识点就是轮子内部机制；
- 目标语言没有合适支持；
- 为解释关键不变量必须暴露内部状态。

不得在每道 BFS 题中重写通用队列，也不得在每道堆题中重写完整堆。

## 7. 基础类型复用

### 7.1 平台类型

LeetCode 提供的 `ListNode`、`TreeNode`、`Node` 等基础类型不在每道题、每种语言中重复声明。

首次出现时：

- 简述字段与引用关系；
- 使用 Mermaid 展示结构或指针变化；
- 在代码块中注明“由平台提供”；
- 把知识状态写入 `state/CONCEPT_LEDGER.toml`。

后续直接复用。只有字段结构、可变性或语言表示变化时再次解释。

### 7.2 Julia 与 R

Julia 和 R 使用仓库统一的节点约定：

- Julia 使用 `mutable struct` 表达需要原地修改的节点；
- R 使用 `environment` 表达需要引用语义的可变节点；
- 首次出现时完整定义并解释；
- 后续题目复用，不重复讲解所有语法；
- 特殊节点结构在当前题目局部定义。

### 7.3 容器与辅助类型

栈、队列、双端队列和堆默认使用各语言标准库或惯用容器。C 根据约束使用最小局部数组或共享辅助约定。

`TrieNode`、并查集、线段树节点、状态对象等若属于当前算法核心，应在当前题目中定义；通用基础设施应复用既有约定。

完整规则见 `docs/SOLUTION_AND_TYPES_POLICY.rst`。

## 8. 教学模型

### 8.1 语法知识采用解释衰减

语言语法和常用库按以下状态变化：

- `introduced`：首次完整解释用途、语义和本题原因；
- `reinforcing`：后续一到数次只保留一句提醒；
- `familiar`：普通使用不再解释；
- `refresh`：长时间未出现或出现新边界时重新提醒；
- `mastered`：仅解释特殊语义、性能和错误风险。

不得在每道题中重复解释 `for`、普通变量声明、常规条件分支等基础内容。

### 8.2 疑难算法周期性复现

动态规划状态设计、单调结构、并查集、图算法、字符串算法、高级数据结构、数学推导、回溯剪枝和依赖证明的不变量不能按普通语法快速衰减。

再次出现时聚焦当前题的新状态、新边界，以及它与旧题的相同点和差异。

### 8.3 语法解释必须靠近代码

语言语法、标准库 API 和类型语义的解释应写在对应语言的代码块中，不在正文外重复堆砌“语法说明”。

- 短解释优先使用行尾注释；
- 解释较长时写在目标代码上一行；
- 行尾注释不能把整行撑得过长；
- 代码行优先控制在 88 个显示列以内，硬上限为 100；
- 超长表达式必须按该语言惯用方式换行；
- 注释解释“为什么”“状态变化”“易错边界”，不得逐行翻译代码。

## 9. Mermaid 图

RST 中使用 `.. mermaid::` 表达文字难以清楚说明的结构和过程，例如：

- 链表指针改向；
- 树的层级与遍历顺序；
- 图搜索过程；
- 动态规划状态转移；
- 递归调用与回溯；
- 多步骤状态机。

每张图只解释一个核心问题，节点数量保持必要最小。简单题不为了装饰强行画图。图之后必须有简短文字说明读者应观察什么。

## 10. 单题内容取舍

题目文件通常包含：

1. 标题与题目信息；
2. 原创题目重述；
3. 自建示例；
4. 问题抽象；
5. 解法选择与对比；
6. 主解法及正确性依据；
7. 复杂度；
8. 多语言实现；
9. 必要的 Mermaid 图；
10. 易错点；
11. 新增与强化知识；
12. 关联题目；
13. 最小自检与答案要点。

结构可以随题目调整。简单题可以短，复杂题需要充分解释。不得为了模板完整制造空洞章节。

## 11. 不建立构建系统

仓库以直接阅读 RST 源文件为主，不需要 Sphinx、文档站点、CI 构建、HTML/PDF/EPUB 生成或自动发布流程。

检查重点：

- RST 标题层级与指令缩进；
- 代码块语言标记；
- Mermaid 语法与可读性；
- 相对链接和文件路径；
- 题号、文件名和状态账本一致；
- 代码、复杂度与讲解一致；
- 行宽和注释密度符合规则。

不得为了“工程完整”添加与学习内容无关的配置、依赖或脚本。

## 12. 状态与批次流程

每批工作：

1. 读取状态文件和最近至少 5 道题；
2. 从 `next_problem` 开始确定连续范围；
3. 判断题目访问状态；
4. 登记 Premium 跳过项；
5. 选择主解法和必要对照解法；
6. 编写原创 RST 和多语言实现；
7. 根据知识账本控制注释密度；
8. 更新进度与知识账本；
9. 检查 RST、Mermaid、题号、路径和代码逻辑；
10. 直接提交到 `main`。

项目早期每批建议 3 至 5 题；规则稳定后扩大到 10 至 20 题。

## 13. Git 规则

默认直接修改 `main`，不得自行创建新分支或 PR。只有用户明确要求时才使用分支或 PR。

每个提交应是可理解的完整批次，并说明题号范围、完成题数、Premium 跳过项和状态账本更新。

## 14. 来源与正确性

- 技术事实和语言版本信息优先查官方文档；
- 题目讲解和代码必须原创；
- 不复制 LeetCode 官方题解；
- 不大段复制其他题解网站；
- 输入输出示例自行构造；
- 无法确认题意或接口时登记阻塞，不自行猜测。

## 15. 交接协议

每次停止工作前确保：

- `state/PROGRESS.toml` 指向准确下一题；
- `state/CONCEPT_LEDGER.toml` 反映本批内容；
- 最近提交说明范围与遗留事项；
- 未完成内容不伪装为完成；
- 新助手只阅读仓库即可接手。

新对话接手时先报告：当前完成范围、下一题号、最近引入的知识、需要复现的疑难算法和当前阻塞项。