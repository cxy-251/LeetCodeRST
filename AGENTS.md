# AGENTS.md

本文件规定 ChatGPT/Codex 在本仓库中生成、修改和接续 LeetCode 多语言分析题解的工作方式。仓库简介看 `README.rst`，下一次接续任务看 `PROGRESS.rst`。

## 1. 接手读取顺序

每次接手任务时，按顺序读取：

1. 根 `README.rst`；
2. 根 `AGENTS.md`；
3. 根 `PROGRESS.rst`；
4. 当前题号所属范围的 `README.rst`；
5. 当前要生成或修改的题解 RST。

最近题解用于核对文件命名、语言顺序、接口写法和说明详略。当前题按本文件规则生成。

## 2. 任务粒度

默认一个对话处理一道题。

常见任务类型：

- 单题生成：创建下一道公开算法题的完整分析题解；
- 单题修改：修改一篇已有题解中的分析、代码、接口、路径、索引或文字问题；
- 小修补丁：修正明确的小问题，例如 include、错别字、目录链接或进度记录；
- 进度同步：更新 `PROGRESS.rst` 和必要范围索引。

用户明确要求多题批次时，按题号连续处理；如果后续题开始变短、变空或只剩代码，收束到已经完整完成的连续前缀。

## 3. 目录和文件形态

题解按百题范围放置：

- `0001-0100/`
- `0101-0200/`
- `0201-0300/`

每道公开算法题使用一个 `<题号>-<slug>.rst` 文件。题目分析、说明和全部代码直接写入该文件。

Premium、Database、Shell 或暂时无法可靠确认题面的题目，在 `PROGRESS.rst` 和范围索引中登记。

## 4. 已有题解的优点

生成新题时优先继承这些已写题解的优点。

### 4.1 清楚的题目契约

好的题解会先把平台接口、输入域、返回语义和特殊边界讲清楚。`0207` 明确 `[course, prerequisite]` 建立 `prerequisite -> course`，并说明自环、重复边、孤立课程和多个分量如何处理。`0218` 明确建筑是半开区间 `[left, right)`，所以过期判断使用 `right <= x`。

### 4.2 能区分正确实现和错误实现的例子

例子不是越多越好，而是要暴露关键错误。`0076` 用 `s = "CABAA"`、`t = "AABC"` 说明只看字符种类会出错，必须覆盖重复次数。`0146` 用一组 LRU 操作序列说明成功 `get` 必须刷新最近使用顺序，否则会淘汰错键。

### 4.3 从题面转成算法模型

题解要说明“这题本质是什么”。`0128` 把最长连续序列转成“只从缺少前驱的连续段起点扩展”。`0218` 把天空线转成“扫描边界并维护活动建筑最大高度”。这种抽象比直接贴代码更有价值。

### 4.4 解法选择有取舍

优秀题解会列出主解法和替代方案，说明为什么不用暴力、排序、递归或其他结构。`0001` 对比一次遍历哈希表、暴力枚举和排序双指针；`0217` 对比两层枚举、排序检查和哈希集合扫描。

### 4.5 状态和代码变量能对应

分析要落到变量。`0050` 用 `answer × factor^exponent = base^E` 解释快速幂循环；`0076` 用 `need[c]` 和 `missing` 解释窗口何时可行；`0207` 用 `indegree`、队列和 `processed` 解释 Kahn 算法。

### 4.6 正确性理由围绕核心动作

正确性不用写成论文，但要覆盖真正关键的动作。`0128` 证明内部节点跳过安全、每段只从唯一起点扫描；`0208` 证明 `search` 必须同时检查路径存在和终止标记；`0218` 证明同坐标整体处理和惰性删除不会产生伪关键点。

### 4.7 复杂度讲真实成本

复杂度要包含实际实现成本。`0076` 区分频次数组、扫描成本和返回子串复制；`0128` 说明哈希平均复杂度、碰撞退化和 C 自建集合空间；`0218` 区分边界排序、堆操作、边界扫描和返回结果空间。

### 4.8 十语言代码前后有必要说明

代码不是孤立块。已有题解会在 C、Rust、Julia、R、TypeScript 等语言处补充必要适配说明，例如 C 资源管理、Rust 所有权、Julia/R 一基索引、TypeScript number 范围、Go 字符串或切片共享。

## 5. 已有题解的缺点

生成新题时也要避开这些问题。

### 5.1 分析可能过长

复杂题的证明、引理和语言差异容易膨胀。新题应保留关键推理，但把同类论证合并，避免同一意思在题目契约、状态、不变量、证明和语言说明中反复出现。

### 5.2 简单题不需要硬凑复杂结构

Easy 题可以紧凑。`0001` 和 `0217` 的优点是围绕一个核心状态讲清楚即可。简单题不需要为了显得完整而扩展成大段教学文章。

### 5.3 语言说明服务代码，不单独堆规则

语言差异只写当前题真正需要的部分。哈希题说明容器和键语义，字符串题说明字符单位，节点题说明对象复用和所有权。与当前题无关的语言坑不写。

### 5.4 代码应是分析的落点

分析最终要能解释代码为什么这样写。若某段分析不能帮助理解状态、边界、复杂度或语言实现，就删短。

## 6. 单题推荐结构

普通算法题按实际需要组织，标题可以调整，相邻内容可以合并。推荐顺序：

1. 题目信息：题号、难度、主题、原题链接、教学重点；
2. 题目重述或精确契约：平台接口、输入域、返回语义、特殊前提；
3. 示例与边界：选择能暴露关键错误的例子；
4. 问题抽象：把题面转成算法模型；
5. 解法选择：说明主解法和被放弃方案的取舍；
6. 核心状态与不变量：把变量含义、更新时机和代码对应起来；
7. 正确性理由：围绕关键动作说明为什么不漏、不重、不误判或为什么最优；
8. 复杂度：写清时间、工作空间、输出空间和必要语言适配成本；
9. 十语言实现：按固定顺序给出代码，并在代码前后补短说明。

## 7. 十语言顺序

普通算法题固定顺序如下：

1. C
2. C++
3. Python
4. Java
5. Rust
6. Go
7. TypeScript
8. C#
9. Julia
10. R

各语言采用符合语言习惯的写法，并尽量保持同一题意和同一主算法语义。某语言需要不同适配方式时，用短句说明差异。

## 8. 验证责任

仓库交付分析和代码文本。运行、编译、调试和判题验证由读者完成。

用户明确要求验证、审查或修 bug 时，按用户指定范围处理。

## 9. 代码生成注意点

生成代码时优先照顾这些常见问题：

- C/C++：必要头文件、数组边界、容量和资源释放；
- Rust：所有权移动、借用范围、`Option<Box<_>>` 和 `take()`；
- Go：切片和字符串共享；
- TypeScript：`number` 安全整数范围；
- Julia/R：一基索引和范围方向；
- 字符串题：字符单位与题目限定字符域；
- 图和节点题：节点身份和值的区别。

这些注意点用于提升代码可用性，题解中保持短说明。

## 10. 进度同步

完成单题生成、修改或进度任务后，根据实际改动同步：

- 当前范围目录的 `README.rst`；
- 根 `PROGRESS.rst`；
- 根 `README.rst` 中确实需要更新的总体说明。

临时分析、草稿和日志留在对话中，最终仓库只保存题解、索引和三个控制文件。

## 11. Git 工作方式

用户明确要求改仓库时，直接在 `main` 修改。

每次修改只碰用户允许的文件。若用户限定文件范围，按该范围执行。

写入后报告实际修改文件、提交 SHA 和仍需用户确认的点。