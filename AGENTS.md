# AGENTS.md

本文件规定 ChatGPT/Codex 在本仓库中生成、修改和接续 LeetCode 多语言分析题解的工作方式。仓库简介看 `README.rst`，下一次接续任务看 `PROGRESS.rst`。

## 1. 接手顺序

每次接手任务时，按顺序读取：

1. 根 `README.rst`；
2. 根 `AGENTS.md`；
3. 根 `PROGRESS.rst`；
4. 当前题号所属范围的 `README.rst`；
5. 当前要生成或修改的题解 RST；
6. 需要校准风格时，读取 1 到 3 篇相邻或同类型题解。

相邻题解只用于校准文件命名、语言顺序、RST 写法和分析详略。当前题的内容按当前题本身推导。

## 2. 任务粒度

默认一个对话处理一道题。

常见任务类型：

- 单题生成：创建下一道公开算法题的完整分析题解；
- 单题修改：修改一篇已有题解中的分析、代码、接口、路径、索引或文字问题；
- 小修补丁：修正明确的小问题，例如 include、错别字、目录链接或进度记录；
- 进度同步：更新 `PROGRESS.rst` 和必要范围索引。

用户要求多题批次时，按题号连续处理。若后续题的分析明显变短、变空或只剩代码，停止扩展批次，保留已经完整完成的连续前缀。

## 3. 目录和文件形态

题解按百题范围放置：

- `0001-0100/`
- `0101-0200/`
- `0201-0300/`

每道公开算法题使用一个 `<题号>-<slug>.rst` 文件。题目分析、说明和全部代码直接写入该文件。

Premium、Database、Shell 或暂时无法可靠确认题面的题目，在 `PROGRESS.rst` 和范围索引中登记。

## 4. 题解定位

本仓库的题解是分析型题解，不是代码片段集合。

每篇题解要完成三件事：

1. 把题目约束讲清楚；
2. 把算法为什么成立讲清楚；
3. 把十语言代码作为分析的落点给出来。

分析服务代码，代码承接分析。正文不追求论文式证明，也不退化成“几句话 + 十个代码块”。

## 5. 标准题解结构

普通算法题按下面结构组织。标题可以按题目需要调整，相邻内容可以合并，但核心信息要出现。

1. 题目信息：题号、难度、主题、原题链接、访问状态、教学重点；
2. 题目重述或精确契约：输入域、返回语义、平台接口、特殊前提和副作用；
3. 示例与边界：选择能暴露关键错误的例子，不堆普通样例；
4. 问题抽象：说明题面被转成了什么算法模型；
5. 解法选择：说明主解法，并点出被放弃方案的代价；
6. 核心状态与不变量：把变量含义、更新时机和代码对应起来；
7. 正确性理由：围绕关键动作说明为什么不漏、不重、不误判或为什么最优；
8. 复杂度：写清时间、工作空间、返回结果空间和必要语言适配成本；
9. 十语言实现：按固定顺序给出代码，并在代码前后补必要短说明。

## 6. 分析写法

题目契约要具体。写清输入长度、数值范围、字符域、节点对象、是否修改输入、返回值含义和题目保证。

例子要服务分析。优先选择能区分正确实现和常见错误的例子，例如重复字符、重复边、边界下标、空结果、同坐标事件、对象状态变化。

问题抽象要说出模型。哈希题说明“前缀状态/集合查询”，滑动窗口题说明“窗口何时可行与如何收缩”，图题说明“边方向、入度或访问状态”，数据结构题说明“对象状态和操作历史”。

解法选择要有取舍。主解法之外，只写 1 到 3 个真正相关的替代方案，说明时间、空间或接口为什么不合适。

状态分析要落到变量。正文中的状态名要能在代码里找到对应变量，变量更新时机要和代码一致。

正确性理由按题目复杂度展开：

- Easy 题用 2 到 4 段说明核心不变量和返回条件；
- Medium 题写清合法性、完整性和终止性或最优性；
- Hard 题围绕关键机制拆成少量引理，但合并重复论证。

复杂度写真实成本。除主算法时间外，说明哈希平均/最坏、排序成本、堆操作、递归栈、输出规模、字符串或数组物化，以及 C/R/Julia/TypeScript 等语言适配造成的额外成本。

## 7. 语言实现规则

普通算法题固定顺序如下：

1. C
2. C++
3. Python
4. Java
5. Rust
6. Go
7. TypeScript
8. C#
9. Julia
10. R

各语言采用符合语言习惯的写法，并保持同一题意和同一主算法语义。某语言需要不同适配方式时，用短句说明差异。

平台接口尽量贴近 LeetCode。类名、函数名、参数类型、返回类型和节点类型使用该语言常见 LeetCode 形式。

C 代码写清必要头文件、返回数组所有权、容量分配和失败时的合理行为。C++、Java、C# 使用标准容器。Rust 写清所有权模型。Julia 和 R 处理一基索引。TypeScript 注意 `number` 和 32 位位运算边界。

## 8. 篇幅控制

简单题保持紧凑，围绕一个核心状态写清即可。

中等题保留完整分析链：契约、抽象、状态、正确性、复杂度、代码。

困难题展开关键机制，例如惰性删除、同坐标整体处理、动态规划状态、图搜索层次、数据结构不变量；同类语言说明和重复证明合并。

每一段分析都要回答一个实际问题：为什么这样建模、为什么这样更新、为什么不会错、为什么复杂度成立、为什么代码需要这样写。

## 9. 验证责任

仓库交付分析和代码文本。运行、编译、调试和判题验证由读者完成。

用户明确要求验证、审查或修 bug 时，按用户指定范围处理。

## 10. 进度同步

完成单题生成、修改或进度任务后，根据实际改动同步：

- 当前范围目录的 `README.rst`；
- 根 `PROGRESS.rst`；
- 根 `README.rst` 中确实需要更新的总体说明。

临时分析、草稿和日志留在对话中，最终仓库只保存题解、索引和三个控制文件。

## 11. Git 工作方式

用户明确要求改仓库时，直接在 `main` 修改。

每次修改只碰用户允许的文件。若用户限定文件范围，按该范围执行。

写入后报告实际修改文件、提交 SHA 和仍需用户确认的点。
