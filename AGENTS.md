# AGENTS.md

本文件是 ChatGPT/Codex 处理本仓库时唯一的题解代码生成规则入口。仓库简介看 `README.rst`，当前进度看 `PROGRESS.rst`。

## 1. 控制文件边界

本仓库只使用三个控制文件：

1. `README.rst`：仓库目标和目录说明；
2. `AGENTS.md`：题解代码生成、审查和修改规则；
3. `PROGRESS.rst`：当前进度、跳过题目、当前任务和下一入口。

范围目录里的 `README.rst` 只维护题目索引，不新增全局规则。

不要创建、引用或恢复以下旧体系：

- `state/`
- `docs/`
- `archive/`
- `concepts/`
- 批次合同、概念账本、漂移日志、前向规则、质量门等拆分控制文件

如果发现旧路径残留引用，优先把它改回 `README.rst`、`AGENTS.md`、`PROGRESS.rst` 三文件模型。

## 2. 新对话读取顺序

每次接手任务时，只读取完成当前任务必要的文件：

1. 根 `README.rst`；
2. 根 `AGENTS.md`；
3. 根 `PROGRESS.rst`；
4. 当前题号所属范围的 `README.rst`；
5. 当前要生成、审查或修复的题解 RST。

不要为了确认格式而递归扫描最近题目。最近题目只能用于核对平台接口、语言顺序、文件命名和目录进度，不能强制当前题继续复杂证明、静态审查或长篇解释。

## 3. 任务粒度

默认一个对话只处理一道题。

允许的任务类型：

- 单题生成：创建下一道公开算法题的 RST，重点生成十语言代码；
- 单题修改：修改一篇已有题解中的代码、接口、路径、索引或明显文字问题；
- 小修补丁：只修明确问题，例如 include、错别字、目录链接或进度记录；
- 进度同步：只更新 `PROGRESS.rst` 和必要范围索引。

除非用户明确要求，不连续生成下一题，不顺手审查其他题，不把规则维护和题解生成混在一起。

## 4. 目录和文件形态

题解按百题范围放置：

- `0001-0100/`
- `0101-0200/`
- `0201-0300/`

每道公开算法题只能有一个 `<题号>-<slug>.rst`。全部代码直接内联在该文件中。

禁止：

- `.inc`；
- `.. include::`；
- 代码分片；
- 正文分片；
- 为 Premium、Database、Shell 或题面信息不足的题目创建空壳教程。

Premium、Database、Shell 或暂时无法可靠确认题面的题目，只在 `PROGRESS.rst` 和范围索引中登记。

## 5. 单题最低要求

普通算法题以代码为主。标题和章节可以简化，但至少保留以下内容：

1. 题号和英文题名；
2. 必要的平台接口或输入输出说明；
3. 固定十语言代码；
4. 必要时保留很短的思路、复杂度或语言差异说明。

不强制要求：

- 自建示例；
- 完整问题抽象；
- 状态不变量；
- 正确性证明；
- 人工推演；
- 静态审查记录；
- 剩余风险说明；
- 自检问题。

不要把题解写成大段教学文章。优先交付可读、可复制、可让读者自行运行验证的代码。

## 6. 十语言顺序

普通算法题固定顺序如下：

1. C
2. C++
3. Python
4. Java
5. Rust
6. Go
7. TypeScript
8. C#
9. Julia
10. R

各语言可以采用符合语言习惯的写法，但应尽量保持同一题意和同一主算法语义。若某语言必须使用不同适配方式，只需用短句说明差异。

## 7. 验证责任

仓库生成题解代码时不承担验证流程。

默认不做：

- 运行题解代码；
- 调用编译器或解释器检查题解；
- 对拍；
- 穷举；
- 属性测试；
- sanitizer；
- 目标语言最小程序验证；
- 人工推演记录；
- 静态审查记录；
- 剩余风险记录。

读者拿到代码后自行运行、调试和修正。只有用户明确要求验证、审查或修 bug 时，才进入对应任务。

## 8. 代码生成注意点

虽然仓库不要求审查记录，生成代码时仍应尽量避免明显错误：

- C/C++：必要头文件、数组边界、容量和资源释放；
- Rust：所有权移动、借用范围、`Option<Box<_>>` 和 `take()`；
- Go：切片和字符串共享；
- TypeScript：`number` 安全整数范围；
- Julia/R：一基索引和范围方向；
- 字符串题：字符单位与题目限定字符域；
- 图和节点题：节点身份和值不要混用。

这些只是写代码时的注意事项，不要求在题解中单独形成审查段落。

## 9. 进度同步

完成单题生成、修改或进度任务后，根据实际改动同步：

- 当前范围目录的 `README.rst`；
- 根 `PROGRESS.rst`；
- 根 `README.rst` 中确实需要更新的总体说明。

不要在题解任务中新增控制文件。不要把临时分析、草稿、日志、marker 提交进仓库。

## 10. Git 工作方式

用户明确要求改仓库时，直接在 `main` 修改。不要创建分支、PR、上传分片分支或临时工作流。

每次修改只碰用户允许的文件。若用户限定文件范围，不得顺手修改其他文件。

提交或写入后，报告实际修改文件、提交 SHA 和仍需用户确认的点。
