# AGENTS.md

本文件规定 ChatGPT/Codex 在本仓库中生成、审查、整改和接续 LeetCode 多语言分析题解的长期规则。仓库简介看 `README.rst`；每轮具体任务以 `PROGRESS.rst` 为准。

## 1. 接手与任务判定

每次新对话按以下顺序开始：

1. 读取根 `PROGRESS.rst`，确认目标题号、目标文件、工作重点和完成后入口；
2. 读取根 `AGENTS.md`，确定题解结构和任务闭环；
3. 在最新 `main` 上检查目标 RST 是否存在；
4. 读取目标文件全文；
5. 按目标文件状态执行本轮任务。

目标文件状态决定任务类型：

- 目标 RST 已存在：读取全文，审查并整改现有题解；
- 目标 RST 不存在：新建完整的分析型多语言题解。

`PROGRESS.rst` 决定“这轮处理哪道题”，目标文件是否存在决定“整改还是新建”，`AGENTS.md` 决定“题解怎么写”。

## 2. 任务范围

正常一轮处理一道题，并修改两个文件：

1. `PROGRESS.rst` 指定的目标题解 RST；
2. 根 `PROGRESS.rst`。

用户明确指定多题批次时，按题号连续处理，并保持每道题的分析完整度一致。批次完成后只保留完整完成的连续前缀，再把下一入口写入 `PROGRESS.rst`。

Premium、Database、Shell 或暂时无法可靠确认题面的题目，在新的 `PROGRESS.rst` 中记录处理结果并推进到下一道可处理题目。

## 3. 文件与目录

题解按百题范围放置：

- `0001-0100/`
- `0101-0200/`
- `0201-0300/`

每道公开算法题使用一个 `<题号>-<slug>.rst` 文件。题目信息、题目重述、自建示例、C++ 多解法、题解正文和九语言实现全部写入该文件。

## 4. 题解定位

本仓库产出分析型题解。每篇题解要完成三件事：

1. 用自己的话讲清题目要求、平台接口和实现边界；
2. 把题面抽象为明确的算法模型，并讲清代表性解法之间的取舍；
3. 用 C++ 展示代表性解法，再给出主解法的九语言实现。

代码先给出可落地的实现，后续分析围绕代码中的函数、状态和更新顺序展开。正文保持具体、完整和紧凑。

## 5. 固定题解结构

普通公开算法题按以下顺序组织：

1. **题目信息**；
2. **题目重述**；
3. **自建示例**；
4. **C++ 实现**；
5. **题解**；
6. **九语言实现**。

九语言实现结束后，本题正文结束。不要在末尾继续添加“易错点”“本题新增知识”“最小自检”“答案要点”“总结”等重复栏目。

标题可以根据题目微调，但整体顺序不变。简单题可以合并题解内部的小节；困难题可以围绕关键机制继续拆分。

## 6. 题目信息、题目重述与自建示例

### 6.1 题目信息

题目信息必须保留，通常包括：

- 题号；
- 难度；
- 主题；
- 原题链接；
- 教学重点。

“访问状态”只在 Premium、无法访问或题面状态特殊时保留，普通公开题无需机械填写。

### 6.2 题目重述

题目重述必须使用自己的话，不照抄或逐句翻译题面。内容要写到足以决定实现，按题目说明：

- 输入与输出；
- 返回下标、节点、对象或结果集合的语义；
- 长度范围、数值范围、字符域或节点模型；
- 哪些条件由平台保证；
- 输入能否修改；
- 对象状态是否跨调用保留；
- 返回顺序、唯一性、去重或原地修改要求。

### 6.3 自建示例

自建示例必须保留。通常选择 1 至 3 个能够暴露核心机制或常见错误的输入，例如：

- 重复值或重复字符；
- 边界下标与空结果；
- 同坐标事件；
- 对象状态变化；
- 需要回退、恢复或去重的场景；
- 能区分正确更新顺序和错误更新顺序的反例。

每个示例都要承担明确的解释任务，不为凑数量重复官方示例。

## 7. C++ 多解法实现

C++ 是本仓库的主语言。C++ 实现放在题解分析之前，用一个完整代码块展示本题具有独立思想价值的代表性解法。

### 7.1 解法数量

通常保留 2 至 4 种代表性方法：

- 最直接的基准解法；
- 综合最优的主解法；
- 具有不同核心思想或实际取舍的替代解法。

不要穷举只是循环写法、容器 API 或小优化不同的同义变体。不是所有能想到的写法都需要实现。

### 7.2 代码组织

一个 C++ 代码块中使用不同函数表示不同解法，函数名直接表达算法，例如：

- `bruteForce`；
- `sortAndTwoPointers`；
- `onePassHash`。

LeetCode 标准入口保留题目要求的函数名，并调用最终选定的主解法。代码块应包含必要的头文件、类型和辅助函数，使读者能够直接看出每种方法的完整实现。

### 7.3 轮子与辅助函数

算法真正依赖且标准库没有直接提供的轮子必须在代码块中体现，例如：

- 自建哈希表、堆、队列或并查集；
- Trie 节点与资源释放；
- 二分边界、图遍历或字符串解析辅助函数；
- 平台类型之外必须自定义的适配结构。

标准库已经提供合适容器时直接使用，不为展示轮子而重复实现标准容器。辅助函数只解释与本题正确性、复杂度、边界或资源管理有关的部分。

## 8. 题解正文

题解正文位于 C++ 代码之后，围绕前面已经出现的函数展开。

### 8.1 问题抽象

问题抽象是题解的核心。它要指出题面背后的算法模型，而不是只报出数据结构名称。例如：

- 哈希题：当前步骤要查询哪个前缀状态、补数或最近位置；
- 滑动窗口题：窗口何时有效、何时扩张、何时收缩；
- 图题：节点、边方向、层次、入度或访问状态如何定义；
- 动态规划题：状态表示什么、转移从哪里来、遍历顺序为何如此；
- 数据结构题：对象保存什么状态、操作后必须维持什么性质。

抽象必须能够解释后续 C++ 函数为什么这样设计。

### 8.2 各解法说明

按照 C++ 代码中的函数顺序解释每种代表性解法：

- 它维护什么状态；
- 它如何得到答案；
- 它解决了前一种方法的什么重复工作；
- 它引入了什么额外空间、排序、修改输入或实现复杂度。

非主解法讲清思想和取舍即可，不需要写成与主解法同等长度。

### 8.3 解法对比与主解法选择

用表格或紧凑段落比较代表性解法的时间、空间和实际代价，并明确指出九语言最终采用哪一种方法以及为什么。

比较要反映真实实现，包括排序副本、哈希平均与最坏情况、递归栈、输出载荷、字符串物化、节点复用和语言适配成本。

### 8.4 主解法解释

主解法解释要解决以下问题：

1. 关键状态分别表示什么；
2. 算法按什么步骤运行；
3. 查询、判断、写入、移动、回退或恢复的顺序为什么不能交换；
4. 为什么不会遗漏合法答案，也不会产生非法答案；
5. 时间和空间复杂度为什么成立。

状态名、函数名、判断条件和更新顺序必须与 C++ 主解法及九语言实现一致。

Easy 题可以把状态、过程、正确性和复杂度合并为少量段落；Medium 和 Hard 题再按算法关键机制拆分。不要为了模板机械建立“思路”“不变量”“正确性依据”等小标题。

## 9. 九语言实现

C++ 已在前面完整展示，不在这里重复。九语言固定顺序为：

1. C
2. Python
3. Java
4. Rust
5. Go
6. TypeScript
7. C#
8. Julia
9. R

九语言只实现题解中最终选定的主解法，不重复 C++ 中的基准解法和替代解法。

各语言使用符合语言习惯的写法，并保持同一题意、同一主算法、同一关键状态和同一更新顺序。平台接口采用该语言常见的 LeetCode 提交形式。

某种语言因平台接口、标准库、所有权、索引或对象模型需要采用不同实现时，在对应代码附近说明：

1. 为什么需要该适配；
2. 与 C++ 主解法相比改变了什么；
3. 该语言实现的实际时间和空间复杂度。

语言实现重点：

- C：必要头文件、容量计算、返回数据所有权、资源释放和必需轮子；
- Python：容器语义、可变对象与递归边界；
- Java、C#：标准容器、对象模型和平台接口；
- Rust：所有权、借用、节点表示和必要的内部可变性；
- Go：切片、字符串和底层存储关系；
- TypeScript：`number` 精度、位运算和查询结果的真假判断；
- Julia、R：一基索引、范围、返回接口和可变对象表达；
- 字符串题：字节、代码单元或题目限定字符域。

## 10. 代码注释

代码块中必须在必要位置使用中文注释，优先使用简短的中文行尾注释，使注释紧邻对应语句。

注释重点解释：

- 非显然的状态含义；
- 不能交换的更新顺序；
- 边界哨兵与索引转换；
- 溢出、精度和字符编码限制；
- 节点复用、所有权和资源释放；
- 语言特有的容器查询陷阱；
- 自建轮子的冲突处理、容量或生命周期。

不要给显然的语法、普通循环、直接赋值和标准库调用逐行加注释。不要把正文原句大段搬进代码。行尾注释过长时，改为前置的一至两行中文注释。

## 11. 对象身份与修改语义

链表、树、图和对象设计题要说明：

1. 节点或对象按身份区分，还是只按值区分；
2. 实现是复用、复制、重连节点，还是只修改字段；
3. 输入结构是否保持不变，哪些修改会被调用者观察到；
4. 对象状态如何跨构造器和方法调用保持；
5. 返回结构的所有权、共享关系和生命周期。

正文中的对象语义与 C++ 多解法及九语言代码保持一致。

## 12. 现有题解审查与整改

目标 RST 已存在时，先读取全文，再从以下方面审查：

1. 是否按照“题目信息 → 题目重述 → 自建示例 → C++ 实现 → 题解 → 九语言实现”组织；
2. 题目契约、平台接口、输入范围和返回语义是否准确；
3. C++ 是否在一个代码块中覆盖具有独立思想价值的代表性解法；
4. 问题抽象是否真正解释了算法模型，而不是只罗列术语；
5. 各解法说明、对比和主解法选择是否与 C++ 函数一致；
6. 主解法的状态、判断顺序、正确性和复杂度是否能映射到代码；
7. 九语言是否完整实现同一主解法，并包含必要轮子和语言适配；
8. 必要位置是否使用了简洁的中文代码注释；
9. 是否删除重复的“易错点”“本题新增知识”“最小自检”“答案要点”和总结栏目；
10. RST 标题、章节、代码块、链接、表格和数学表达是否完整可读。

已有正确内容继续保留并整理。发现的问题直接修正在目标 RST 中，最终文件只保留整改后的完整题解，不另建审查报告。

## 13. 篇幅控制

简单题完整保留固定结构，但各部分保持紧凑。

中等题完整讲清问题抽象、代表性解法、主解法状态、正确性和复杂度。

困难题围绕决定实现的关键机制展开，例如惰性删除、同坐标整体处理、状态压缩、图搜索层次、回溯恢复或数据结构不变量，并合并同义证明和重复语言说明。

每段正文解决一个具体问题：为什么这样建模、为什么这样实现、为什么选择当前主解法、为什么更新顺序正确、为什么复杂度成立，或为什么某种语言需要当前适配。

## 14. 验证方式

仓库交付分析和代码文本。运行、编译、调试和 LeetCode 判题由读者完成。用户明确指定验证或运行代码时，按指定范围执行。

## 15. 完成与接力

完成本轮任务后：

1. 根据实际完成结果完整重写根 `PROGRESS.rst`；
2. 在新的 `PROGRESS.rst` 中写明下一轮题号、目标文件、工作重点和完成后入口；
3. 让 `PROGRESS.rst` 始终保持为面向下一次对话的当前任务快照；
4. 将目标题解与新的 `PROGRESS.rst` 组成一个提交。

下一次对话先读新的 `PROGRESS.rst`，即可知道接下来处理哪道题；再按本文件规则判断是整改还是新建。

## 16. Git 工作方式

用户要求修改仓库时，直接在 `main` 上完成。每轮的目标题解和 `PROGRESS.rst` 作为同一个原子提交写入。规则文件只在仓库工作方式发生变化时修改。
