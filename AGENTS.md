# 根目录代理规则

本文件定义整个仓库的通用开发约束。任何代码修改前，都必须先阅读本文件。

# 允许内容

- 仓库级开发规则
- Codex 协作约束
- 目录 README 继承规则
- 与 `PROJECT_MAP.md` 配套的导航说明

# 禁止内容

- 业务实现细节
- 临时调试记录
- 与目录职责无关的产品方案
- 把 README 当作代码或配置替代品

# 修改前必须阅读

- `./AGENTS.md`
- `./PROJECT_MAP.md`
- 目标文件所在目录的 `README.md`
- 目标文件所有父目录中的 `README.md`

# 命名规则

- 根目录规则文件使用固定名称：`AGENTS.md`
- 项目映射文件使用固定名称：`PROJECT_MAP.md`
- 目录规则文件统一使用 `README.md`

# 边界说明

本文件只定义跨目录协作规则。
具体目录能放什么、不能放什么，由对应目录的 `README.md` 继续收窄。

# Codex 规则

- 在修改任何代码前，必须先阅读根目录 `AGENTS.md`。
- 在修改任何代码前，必须先阅读 `PROJECT_MAP.md`。
- 在修改任何代码前，必须阅读目标文件所在目录的 `README.md`。
- 在修改任何代码前，必须阅读目标文件所有父目录中的 `README.md`。
- 如果 `README.md` 规则与当前任务冲突，先停止并说明冲突，不要直接修改代码。
- 如果目标目录没有 `README.md`，先创建 `README.md`，再继续修改代码。
- 禁止把代码放入 `README.md` 未允许的目录。
- 禁止删除空目录中的 `README.md`。
- 不允许跨目录顺手重构；如需跨目录修改，先说明原因。
- 默认不维护以下生成或外部目录：`node_modules/`、`build/`、`dist/`、`output/`、各类 `generated/` 产物目录。
- 如果任务必须修改生成目录中的文件，先说明原因，并先为该目录补充 `README.md` 规则。
