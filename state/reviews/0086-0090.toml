[batch]
round = 2
start = 86
end = 90
status = "completed"
source = "complete_rst_content"
modify_historical_rst = false
summary = "完整读取 0086-0090 的 RST 正文、证明、复杂度和十语言代码；五题官方输入域内的核心算法正确，登记 Julia 修改可见性与 Gray Code n=0 扩展两项事实更正。"

[problems."0086"]
status = "reviewed"
file = "problems/0001-0100/0086-partition-list.rst"
positive = [
  "双链尾插同时保持小于 x 与大于等于 x 两个分区的原相对顺序。",
  "每轮先保存后继并断开当前节点，节点只进入一个结果链，最终拼接不会形成旧边回路。",
  "Rust 使用可变所有权槽位推进链尾，不复制数据节点且不需要 unsafe。",
]
observations = [
  "稳定分区不仅检查值序列，还要检查节点身份、分区内相对顺序、节点数量和无环性。",
  "逐节点断开或等价的最终尾部终止证明必须至少具备一种，不能让旧 next 边逃逸到结果拓扑。",
  "哨兵节点只保存结果入口，不属于返回节点集合。",
]
rule_ids = [
  "stable_partition_identity_and_order",
  "linked_list_topology_isolation",
]

[problems."0087"]
status = "reviewed"
file = "problems/0001-0100/0087-scramble-string.rst"
positive = [
  "状态键 first_start、second_start、length 精确覆盖两个待比较等长区间。",
  "每个切分点完整检查交换与不交换两类根转移，子状态长度严格缩小。",
  "字符频次不同安全剪枝，真假状态均进入三值缓存，避免失败状态重复搜索。",
]
observations = [
  "必要条件剪枝只能提前否定，字符多重集相同仍需检查切分树。",
  "缓存键缺少长度会混淆不同区间状态，只缓存 true 也会保留指数级失败搜索。",
  "逐区间直接相等检查与 26 项频次检查均不改变 O(n^4) 最坏时间结论。",
]
rule_ids = [
  "memo_key_covers_semantic_state",
  "cache_negative_results",
  "necessary_prune_not_sufficient",
]

[problems."0088"]
status = "reviewed_with_correction"
file = "problems/0001-0100/0088-merge-sorted-array.rst"
positive = [
  "write = first + second + 1 证明写位置始终位于 nums1 未读前缀之后。",
  "每轮把两个未合并前缀的最大值提交到最右空位，nums2 耗尽即可终止。",
  "十语言索引转换与 m=0、n=0 边界控制流正确。",
]
observations = [
  "Julia Vector 参数按共享语义传递，merge_sorted! 的元素写入对调用者可见；返回向量是接口便利，不是因为 Julia 具有 R 式复制语义。",
  "R 形式参数表现为局部绑定，函数内 nums1 写入不能作为修改调用者变量的通用接口保证，因此返回结果向量是必要适配。",
  "原地声明必须分别说明底层对象是否被调用者观察到修改，以及函数是否额外返回同一对象或结果副本。",
]
rule_ids = [
  "reverse_write_frontier_certificate",
  "mutation_visibility_by_language",
]
correction_ids = [
  "julia_merge_sorted_mutation_visibility",
]

[problems."0089"]
status = "reviewed_with_correction"
file = "problems/0001-0100/0089-gray-code.rst"
positive = [
  "公式 i XOR (i >> 1) 直接生成全部官方输入域结果，无需搜索或访问集合。",
  "相邻一位差、映射可逆、范围唯一覆盖和首尾闭合证明彼此独立且完整。",
  "输出规模 Theta(2^n)、工作空间 O(1) 与各语言位宽边界说明正确。",
]
observations = [
  "官方约束 n>=1 时循环首尾一位差证明成立。",
  "扩展 n=0 返回 [0] 时，首元素与末元素是同一值，异或为 0；若仍称其满足循环 Gray 条件，必须显式采用零维退化约定。",
  "主动支持超出官方输入域的边界，必须说明哪些原条件继续成立、哪些采用新的退化定义。",
]
rule_ids = [
  "constructive_bijection_and_cycle_certificate",
  "extension_contract_defines_degenerate_case",
]
correction_ids = [
  "gray_code_zero_dimension_extension",
]

[problems."0090"]
status = "reviewed"
file = "problems/0001-0100/0090-subsets-ii.rst"
positive = [
  "排序把相同值聚成连续段，同层只保留最左同值候选而允许下一层继续选择重复副本。",
  "规范下标表示同时证明所有多重集子集可达且不会重复提交。",
  "十语言均保存独立结果快照；C 的多缓冲分配失败路径释放已完成行和元数据。",
]
observations = [
  "去重条件必须绑定当前层 start，不能把相邻相等判断无条件应用到更深递归。",
  "枚举复杂度应区分结果行数 S、元素总载荷 P、路径工作空间和排序修改输入。",
  "输入排序副作用、独占消费和局部副本需要按语言接口分别声明。",
]
rule_ids = [
  "same_level_canonical_representative",
  "output_snapshot_and_metadata_cost",
]

[rules."stable_partition_identity_and_order"]
status = "promoted"
problems = [86]
requirement = "稳定链表分区同时证明值分类、分区内原相对顺序、原节点身份守恒、节点数量和无环性；测试不能只比较最终值数组。"

[rules."linked_list_topology_isolation"]
status = "promoted"
problems = [86]
requirement = "重建多条链时，在节点离开原拓扑时断开旧边，或提供等价的尾部终止与无环证明；任何旧 next 边都不能逃逸到最终结果。"

[rules."memo_key_covers_semantic_state"]
status = "promoted"
problems = [87]
requirement = "记忆化键必须包含决定子问题语义的全部坐标、长度、剩余额度或模式；省略维度前先证明它可由其他字段唯一恢复。"

[rules."cache_negative_results"]
status = "promoted"
problems = [87]
requirement = "可达状态的 false 与未计算必须分离；失败状态同样写入缓存，否则记忆化可能仍重复展开指数级搜索。"

[rules."reverse_write_frontier_certificate"]
status = "promoted"
problems = [88]
requirement = "逆向原地写入给出读前缀、已完成后缀和写位置关系，逐轮证明写指针严格越过所有尚未读取的同容器元素。"

[rules."mutation_visibility_by_language"]
status = "promoted"
problems = [88, 90]
requirement = "分别说明参数绑定、可变对象共享、局部重绑定、独占消费和返回副本；不能把返回容器统一解释为所有语言都具有复制语义。"

[rules."constructive_bijection_and_cycle_certificate"]
status = "promoted"
problems = [89]
requirement = "闭式构造序列分别证明范围、相邻关系、单射或可逆性、完整覆盖和循环首尾条件，不能只凭若干样例确认。"

[rules."extension_contract_defines_degenerate_case"]
status = "promoted"
problems = [89]
requirement = "主动支持官方输入域外的零维、空输入或单元素情形时，明确原条件是按字面继续检查、视为真空成立，还是采用新的退化约定。"

[rules."same_level_canonical_representative"]
status = "promoted"
problems = [90]
requirement = "重复值枚举把去重条件限定在同一递归层，定义保留的规范代表，并证明保留分支覆盖被跳过兄弟而不删除深层重复次数。"

[rules."output_snapshot_and_metadata_cost"]
status = "promoted"
problems = [90]
requirement = "枚举结果分别计入行容器数量、元素总载荷、快照复制、路径和递归栈；保存的每行不得共享后续仍会修改的可变路径。"

[next_review]
start = 91
end = 95
purpose = "Review 0091 Decode Ways through 0095 Unique Binary Search Trees II from complete RST content."
