[batch]
start = 41
end = 45
status = "completed"
source = "complete_rst_content"
modify_historical_rst = false
summary = "完整读取 0041-0045 的 RST 正文和十语言代码，提炼值派生下标守卫、永久槽位证明、聚合结果宽度、规范数字字符串、受控回退进度和区间 BFS 边界规则。"

[problems."0041"]
status = "reviewed"
file = "problems/0001-0100/0041-first-missing-positive.rst"
positive = [
  "答案范围、值到目标槽位的等价性和首个不匹配的最小性证明完整。",
  "每次交换永久填好一个目标槽位，重复值守卫阻止交换环，终止性解释清楚。",
]
observations = [
  "值直接决定下一次数组访问位置，正确性依赖先完成 1..n 值域检查，再通过短路求值访问目标槽位；后续代码不能改变条件顺序。",
  "C++ 将 size_t 长度转换为 int，Rust 将 usize 下标与 n+1 转换为 i32，安全性依赖未列出的精确数组长度上界。",
  "R 在函数内重排局部向量，可能因写时复制产生 O(n) 适配器成本，与原地 O(1) 空间接口不同。",
  "原地重排改变调用者数组顺序；Rust 消费输入，R 修改局部副本，语言接口语义需要分别说明。",
]
rule_ids = [
  "value_derived_index_guard",
  "slot_permanence_and_duplicate_termination",
  "constraint_backed_narrowing",
  "language_adapter_interface_cost",
  "input_mutation_and_ownership",
]

[problems."0042"]
status = "reviewed"
file = "problems/0001-0100/0042-trapping-rain-water.rst"
positive = [
  "较低已知最大边界确定当前位置水量的证明完整，明确说明未来信息不能改变已经结算的值。",
  "每轮移动一个端点并永久移出未处理区间，线性时间不变量清楚。",
]
observations = [
  "正文只声称题目约束下总水量可由 int 表示，没有列出 n 与最大高度并推导累计上界。",
  "C++ 将容器长度转换为 int 后减一，仍需要精确最大长度支撑。",
  "R 使用双精度数值累加以避免整数 NA，属于语言适配器中的数值表示差异。",
]
rule_ids = [
  "aggregate_accumulator_bound",
  "future_information_finality",
  "constraint_backed_narrowing",
  "language_adapter_interface_cost",
]

[problems."0043"]
status = "reviewed"
file = "problems/0001-0100/0043-multiply-strings.rst"
positive = [
  "m+n 位长度上界、相邻槽位映射和从右向左传播进位的证明完整。",
  "复杂度区分结果数组与输出字符串，算法没有把整个输入转换成大整数。",
]
observations = [
  "题目重述没有写出输入非空且除数字 0 外不含前导零；Julia findfirst 与 R which 的非零见证依赖该规范表示前提。",
  "C 的 zero、digits 和 result 分配都在检查前写入或使用，失败契约未闭合。",
  "strlen 到 int 的窄化、m+n、分配尺寸和结果长度需要由精确输入长度上界证明安全。",
  "TypeScript 的 slice、Julia 的 string. 广播与 join、R 的 utf8ToInt 和 paste0 形成不同的序列化复制与峰值空间。",
  "后处理寻找首个非零槽位时，应由零值提前返回或规范输入证明见证一定存在。",
]
rule_ids = [
  "canonical_numeric_string_contract",
  "postprocess_witness_contract",
  "allocation_failure_whole_structure",
  "capacity_expression_safety",
  "serialization_pipeline_cost",
]

[problems."0044"]
status = "reviewed"
file = "problems/0001-0100/0044-wildcard-matching.rst"
positive = [
  "最近星号回退按覆盖长度 0、1、2 递增尝试，没有跳过可行长度。",
  "正文诚实给出最坏 O(mn)，没有把常见接近线性的表现写成严格线性。",
  "字符串耗尽后只允许剩余星号的完整匹配边界处理正确。",
]
observations = [
  "ASCII 字符域直接支撑字节与代码单元索引，应在题目统一约束中集中声明，而不是只放在实现章节。",
  "Julia 把 codeunits 复制为两个 Vector，R 用 strsplit 建立两个字符向量，实际额外空间为 O(m+n)，不能沿用核心索引状态 O(1)。",
  "受控回退的终止性应显式使用 match_start 单调增加且不超过字符串长度作为进度量。",
]
rule_ids = [
  "exact_character_model",
  "normalization_overrides_core_space",
  "controlled_fallback_progress",
  "language_specific_copy_cost",
]

[problems."0045"]
status = "reviewed"
file = "problems/0001-0100/0045-jump-game-ii.rst"
positive = [
  "把连续邻接区间的并集压缩为 BFS 层边界，最少跳数由首次进入层数证明。",
  "current_end 与 farthest 分离，延迟到扫描完整层后再提交跳跃，避免局部贪心。",
]
observations = [
  "farthest 和 current_end 是覆盖边界，可能超过最后一个合法下标；正文称其为下标时应说明不能直接用于数组访问。",
  "index + nums[index] 的宽度由数组长度与最大跳数共同决定，精确约束未列出。",
  "算法进展依赖题目保证终点可达；通用版本需要在层结束时检测 farthest 是否仍等于 current_end。",
  "Rust 将非负跳数转换为 usize，安全性依赖非负前提和加法不溢出。",
]
rule_ids = [
  "implicit_interval_bfs_layer",
  "coverage_boundary_not_array_index",
  "aggregate_accumulator_bound",
  "reachability_progress_contract",
]

[rules."value_derived_index_guard"]
status = "promoted"
problems = [41]
requirement = "数组下标由输入值计算时，先完成值域检查，再依赖明确的短路求值顺序访问目标槽位；证明和代码保持同一条件顺序。"

[rules."slot_permanence_and_duplicate_termination"]
status = "promoted"
problems = [41]
requirement = "原地值槽映射证明每次交换永久完成至少一个目标槽位、正确槽位不会被后续破坏，并用目标槽位相等检查处理重复值和终止性。"

[rules."aggregate_accumulator_bound"]
status = "promoted"
problems = [42, 45]
requirement = "累计量和覆盖端点的整数宽度由元素数量、单项最大贡献或最大步长共同推导，不能只证明单次差值或单次加法安全。"

[rules."future_information_finality"]
status = "promoted"
problems = [42]
requirement = "在线结算某位置前，证明已知边界已经决定最终值，尚未扫描的信息只能保持或提高非限制侧，不能改变当前结论。"

[rules."canonical_numeric_string_contract"]
status = "promoted"
problems = [43]
requirement = "数字字符串题明确非空、字符域、零的唯一表示和前导零规则；早退、首个非零位和规范输出证明均建立在该契约上。"

[rules."postprocess_witness_contract"]
status = "promoted"
problems = [43]
requirement = "findfirst、which、unwrap 等后处理操作需要由早退分支或不变量证明见证元素必然存在，再执行解包和索引。"

[rules."serialization_pipeline_cost"]
status = "promoted"
problems = [43]
requirement = "结果数组转换为字符串时，计入切片、逐位字符串化、中间字符串集合、join/paste 和最终结果的时间及峰值空间。"

[rules."normalization_overrides_core_space"]
status = "promoted"
problems = [44]
requirement = "核心算法只保存常数索引时，若语言实现先复制 codeunits、strsplit 或字符数组，复杂度按完整实现报告为 O(m+n) 规范化空间。"

[rules."controlled_fallback_progress"]
status = "promoted"
problems = [44]
requirement = "受控回退算法给出单调进度量、最大回退次数和重扫区间上界，分别证明终止性与最坏复杂度。"

[rules."implicit_interval_bfs_layer"]
status = "promoted"
problems = [45]
requirement = "用区间边界压缩 BFS 时，证明当前层所有出边的并集连续，汇总最远端等价于生成完整下一层，并且一次边界提交对应一次层转换。"

[rules."coverage_boundary_not_array_index"]
status = "promoted"
problems = [45]
requirement = "可达范围边界可以超过最后一个有效下标；将其作为覆盖坐标使用，只有经过裁剪或范围检查后才能用于数组访问。"

[rules."reachability_progress_contract"]
status = "promoted"
problems = [45]
requirement = "依赖答案可达的贪心明确该官方前提；推广到不可达输入时，在层结束处检测边界不再推进并返回独立失败语义。"

[next_batch]
start = 46
end = 50
purpose = "完整读取 0046-0050 的 RST，完成历史范围的前向规则提炼，不修改历史题目。"
