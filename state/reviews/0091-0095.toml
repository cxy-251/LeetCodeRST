[batch]
round = 2
start = 91
end = 95
status = "completed"
source = "complete_rst_content"
modify_historical_rst = false
summary = "完整读取 0091-0095 的 RST 正文、证明、复杂度和十语言代码；0091-0094 未发现新的确定性问题，0095 核心算法正确但 R 适配器调用未定义的 tree_node 构造器。"

[problems."0091"]
status = "reviewed"
file = "problems/0001-0100/0091-decode-ways.rst"
positive = [
  "先验证全部零，再把合法 10/20 作为强制边界切分，避免非法串先积累无意义计数。",
  "无零片段只需滚动保存前两个前缀计数，单字符与合法两字符选择互斥且完整。",
  "段内计数单调不减且部分乘积不超过最终答案，固定宽中间值安全论证闭合。",
]
observations = [
  "强制使用的字符对会同时消费边界两侧字符，分段时必须把零的前一位从左片段排除。",
  "利用最终答案上界保护中间值，需要证明每个片段因子和部分乘积都受最终答案支配。",
  "Julia 与 R 的一基片段坐标已逐项对应正文半开边界，没有出现空范围误迭代。",
]
rule_ids = [
  "forced_boundary_factorization",
  "intermediate_bound_from_monotone_factors",
]

[problems."0092"]
status = "reviewed"
file = "problems/0001-0100/0092-reverse-linked-list-ii.rst"
positive = [
  "哨兵统一 left=1，current 固定为原区间首节点并最终成为反转段尾。",
  "每轮先摘除 current.next，再插入 before 之后，四条链接更新保持节点守恒与无环。",
  "Rust 采用取出区间、逐节点反转和重接后缀的所有权安全控制流，语义与复杂度一致。",
]
observations = [
  "区间重连证明必须列出 before、current、moved 和 suffix 的修改前后边，不能只展示最终序列。",
  "不同语言可以采用头插或整段反转，但都要证明只改变目标闭区间并保持区间外顺序。",
  "合法位置契约支撑全部非空解包和固定循环次数。",
]
rule_ids = [
  "linked_interval_rewire_certificate",
  "alternate_ownership_algorithm_equivalence",
]

[problems."0093"]
status = "reviewed"
file = "problems/0001-0100/0093-restore-ip-addresses.rst"
positive = [
  "剩余段数 k 与剩余字符数 r 的 k<=r<=3k 双向剪枝精确删除不可能状态。",
  "前导零只允许单字符零，增量数值首次超过 255 后可单调停止更长候选。",
  "每个合法地址由唯一四段长度向量确定，完整性与无重复证明成立。",
]
observations = [
  "固定段数回溯先检查剩余字符下界和上界，深度完成时才能由该剪枝推出字符恰好耗尽。",
  "候选提前 break 需要单调性证书：追加十进制数字不会让超过 255 的正值重新变小。",
  "C 的 81 个结果槽位来自四段各三种长度的选择向量上界，不等同于有效地址数量的精确值。",
]
rule_ids = [
  "fixed_partition_remaining_length_bounds",
  "monotone_candidate_rejection",
  "capacity_bound_from_choice_vectors",
]

[problems."0094"]
status = "reviewed"
file = "problems/0001-0100/0094-binary-tree-inorder-traversal.rst"
positive = [
  "显式栈保存左子树返回后访问根节点的递归续点，弹栈时机与中序定义一致。",
  "每个树节点只通过唯一父边到达，恰好入栈和弹栈一次。",
  "复杂度区分 O(h) 工作栈、Theta(n) 返回空间和 R 逐项扩展的 O(n^2) 累计复制。",
]
observations = [
  "一次到达一次访问依赖输入为无共享节点、无环的树；对一般有向图必须增加访问标记。",
  "工作栈上界由当前祖先链长度决定，只有退化树才达到 n。",
  "R 删除列表栈顶与扩展结果向量具有不同复制行为，正文已经分别说明。",
]
rule_ids = [
  "explicit_stack_continuation_state",
  "tree_structure_precondition_for_unique_visit",
  "height_based_stack_bound",
]

[problems."0095"]
status = "reviewed_with_correction"
file = "problems/0001-0100/0095-unique-binary-search-trees-ii.rst"
positive = [
  "根值唯一决定左右连续值域，左右结果笛卡尔积完整生成全部 BST 结构。",
  "空区间返回单个 null 作为组合单位元，使叶节点自然由空左树与空右树组成。",
  "每个最终组合深复制左右模板，保证不同返回树之间没有共享可变节点。",
]
observations = [
  "R 代码调用 tree_node(...)，但 0094 建立的构造器名称是 new_tree_node(...)，0095 本身也没有定义 tree_node；该适配器会因找不到函数而失败。",
  "深复制保证结果独立性时，复杂度必须包含最终 n*C_n 个节点以及递归过程中同时存活的临时模板。",
  "空树占位属于递归组合状态，公开 n=0 接口仍可按题目约定返回空外层列表。",
]
rule_ids = [
  "empty_structure_cartesian_identity",
  "deep_clone_result_isolation",
  "cross_file_helper_symbol_consistency",
]
correction_ids = [
  "r_unique_bst_constructor_name",
]

[rules."forced_boundary_factorization"]
status = "promoted"
problems = [91]
requirement = "当某个 token 必须与相邻 token 组成唯一结构时，先验证所有强制边界，再证明该结构消费哪些位置、阻断哪些跨界选择，以及独立片段为何可通过乘法组合。"

[rules."intermediate_bound_from_monotone_factors"]
status = "promoted"
problems = [91]
requirement = "用最终答案上界证明固定宽中间值安全时，逐项证明局部状态单调、每个因子至少为一、部分乘积不超过最终乘积；非法输入应在可能增长前被排除。"

[rules."linked_interval_rewire_certificate"]
status = "promoted"
problems = [92]
requirement = "链表区间重连列出区间前驱、固定尾、被移动节点和后缀的修改前后边，证明每轮不丢节点、不形成环，并且只改变目标区间。"

[rules."alternate_ownership_algorithm_equivalence"]
status = "promoted"
problems = [92]
requirement = "所有权语言采用不同控制流时，分别证明值与节点身份语义、修改范围、区间外顺序、时间和峰值空间等价，不能仅凭输出示例判定一致。"

[rules."fixed_partition_remaining_length_bounds"]
status = "promoted"
problems = [93]
requirement = "固定分段回溯在每个状态检查剩余字符数是否位于剩余段数乘单段最小和最大长度之间，并用该条件证明完成深度时输入恰好耗尽。"

[rules."monotone_candidate_rejection"]
status = "promoted"
problems = [93]
requirement = "候选枚举使用 break 而非 continue 时，证明后续扩展在目标度量上单调不改善；前导零、数值上界和长度上界分别给出停止依据。"

[rules."explicit_stack_continuation_state"]
status = "promoted"
problems = [94]
requirement = "把递归改为显式栈时，说明每个栈元素保存的未完成续点、何时允许提交当前节点，以及弹栈后如何进入下一子问题。"

[rules."tree_structure_precondition_for_unique_visit"]
status = "promoted"
problems = [94]
requirement = "不使用访问集合的树遍历必须声明无环、每个非根节点只有一个父节点且不存在共享子树；输入扩大为图时重新设计去重和终止机制。"

[rules."empty_structure_cartesian_identity"]
status = "promoted"
problems = [95]
requirement = "递归组合左右结构时，空区间返回包含一个空对象的选择列表，使空结构成为笛卡尔积单位元；公开空输入返回值与内部组合占位分开定义。"

[rules."deep_clone_result_isolation"]
status = "promoted"
problems = [95]
requirement = "返回多个可变树或图对象并承诺相互独立时，收集跨结果节点身份验证无共享；模板复用必须在提交前深复制，并计入全部节点与临时模板成本。"

[rules."cross_file_helper_symbol_consistency"]
status = "promoted"
problems = [94, 95]
requirement = "跨题复用平台适配器或辅助构造器时，调用符号、参数顺序和返回类型必须与定义精确一致；不能用文字声称复用而省略或改名实际 helper。"

[next_review]
start = 96
end = 100
purpose = "Review 0096 Unique Binary Search Trees through 0100 Same Tree from complete RST content."
