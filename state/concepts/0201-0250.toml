[meta]
version = 6
last_problem = 218

# teaching_state: introduced / reinforcing / familiar / refresh / mastered

[concepts]
"workflow.targeted_static_review" = { kind = "workflow", first_problem = 134, last_problem = 212, occurrences = 54, teaching_state = "mastered", difficult = false, note = "0134–0212 持续执行人工推演、证明映射、十语言静态语义和仓库一致性检查；未运行、未编译、未测试题解。" }
"invariant.mark_and_restore_board" = { kind = "invariant", first_problem = 212, last_problem = 212, occurrences = 1, teaching_state = "introduced", difficult = false, note = "0212 进入合法状态后临时标记当前格，探索四邻格，退出前恢复原字符；0200 永久清零访问状态，不属于此 concept。" }
"algorithm.range_and_common_prefix" = { kind = "algorithm", first_problem = 201, last_problem = 201, occurrences = 1, teaching_state = "introduced", difficult = false, note = "同步逻辑右移区间端点直到相等，得到最长公共二进制前缀，再补回相同数量的低位 0。" }
"proof.range_and_variable_suffix_zero" = { kind = "proof", first_problem = 201, last_problem = 201, occurrences = 1, teaching_state = "introduced", difficult = true, note = "端点跨越第一个差异位对应的块边界，变化后缀的每一位都在区间内至少出现一次 0，因此按位与全部归零。" }
"workflow.range_directory_boundary" = { kind = "workflow", first_problem = 201, last_problem = 203, occurrences = 1, teaching_state = "introduced", difficult = false, note = "0201 起题解进入 problems/0201-0300，并创建独立范围 README；0101-0200 目录不再接收新题。" }
"algorithm.floyd_cycle_detection" = { kind = "algorithm", first_problem = 141, last_problem = 202, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0141 在显式链表上维护 slow=x(k)、fast=x(2k)；0202 在平方位和函数图中采用 slow=x(t)、fast=x(2t+1)，以到达 1 或非 1 相遇区分两类终态。" }
"invariant.decimal_digit_square_sum" = { kind = "invariant", first_problem = 202, last_problem = 202, occurrences = 1, teaching_state = "introduced", difficult = false, note = "取余与整除逐个移除十进制低位，累计值始终等于已处理数字的平方和。" }
"proof.happy_number_finite_state" = { kind = "proof", first_problem = 202, last_problem = 202, occurrences = 1, teaching_state = "introduced", difficult = true, note = "d 位数的平方位和不超过 81d，四位及以上严格下降，最终进入小于 1000 的有限集合并必然到达 1 或形成环。" }
"technique.dummy_node" = { kind = "algorithm", first_problem = 2, last_problem = 203, occurrences = 7, teaching_state = "familiar", difficult = false, note = "0002–0025 用虚拟头统一结果追加、头删、归并和局部重连；0203 继续把连续头部删除统一为 previous.next 的普通删除。" }
"rust.mutable_link_slot_deletion" = { kind = "language_boundary", first_problem = 82, last_problem = 203, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0082 与 0203 都用 &mut Option<Box<ListNode>> 表示当前所有权槽位；删除时以后继替换槽位，保留时才把槽位推进到 next。" }
"interface.linked_list_node_reuse" = { kind = "interface", first_problem = 82, last_problem = 203, occurrences = 5, teaching_state = "reinforcing", difficult = false, note = "0082、0083、0086、0092 与 0203 都重连原节点而不复制保留节点；0203 进一步说明被删除节点的语言相关资源责任和新头返回。" }
"ownership.removed_list_nodes" = { kind = "ownership", first_problem = 203, last_problem = 203, occurrences = 1, teaching_state = "introduced", difficult = true, note = "C 在明确接收节点所有权的假设下重接后 free；托管语言交给运行时，Rust 通过 Box 移动自动释放被删除节点。" }
"adapter.sieve_storage_cross_language" = { kind = "language_adapter", first_problem = 204, last_problem = 204, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C/C++/Python/Rust/Go/TypeScript/C# 使用字节状态，Java 使用 boolean[]，Julia 使用 BitVector，R 使用更重的 logical 向量；全部为 O(n) 空间。" }
"algorithm.sieve_of_eratosthenes" = { kind = "algorithm", first_problem = 204, last_problem = 204, occurrences = 1, teaching_state = "introduced", difficult = false, note = "为 [0,n) 建立合数标记，只让未标记候选 p 从 p*p 开始按步长 p 划去倍数，最后统计 2..n-1 的未标记索引。" }
"invariant.sieve_candidate_state" = { kind = "invariant", first_problem = 204, last_problem = 204, occurrences = 1, teaching_state = "introduced", difficult = false, note = "处理候选 p 前，小于 p*p 的合数已标记、所有标记都是真合数、未标记 p 没有更小质因子。" }
"proof.sieve_start_at_square" = { kind = "proof", first_problem = 204, last_problem = 204, occurrences = 1, teaching_state = "introduced", difficult = true, note = "小于 p*p 的 p 倍数写成 p*k 且 k<p，已经包含更小质因子并被更早扫描标记，因此从 p*p 开始不漏合数。" }
"proof.smallest_prime_factor_completeness" = { kind = "proof", first_problem = 204, last_problem = 204, occurrences = 1, teaching_state = "introduced", difficult = true, note = "任意合数 x 的最小质因子 p 不超过 sqrt(x)，处理 p 时 x 位于从 p*p 开始的倍数序列中；质数从不可能成为该序列元素。" }
"safety.square_bound_by_division" = { kind = "numeric_safety", first_problem = 204, last_problem = 204, occurrences = 1, teaching_state = "introduced", difficult = true, note = "外层使用 p <= (n-1)/p 表达 p*p<n，在比较前避免固定宽整数平方溢出；内层按语言使用 size_t 或宽整数。" }
"algorithm.isomorphic_bidirectional_mapping" = { kind = "algorithm", first_problem = 205, last_problem = 205, occurrences = 1, teaching_state = "introduced", difficult = false, note = "同时维护 source->target 与 target->source，前者保证同一源字符映射一致，后者保证目标不被不同源字符重复占用。" }
"proof.isomorphic_bijection" = { kind = "proof", first_problem = 205, last_problem = 205, occurrences = 1, teaching_state = "introduced", difficult = true, note = "双向冲突检查维护函数与单射；每个实际目标字符都来自一个对应位置，扫描完成后映射对出现字符集合构成双射。" }
"semantics.string_character_unit" = { kind = "language_semantics", first_problem = 205, last_problem = 205, occurrences = 1, teaching_state = "introduced", difficult = true, note = "官方 ASCII 输入允许字节、UTF-16 单元和 Unicode 码点适配一致；超出 ASCII 时不得混称同一种字符边界。" }
"algorithm.reverse_list_three_links" = { kind = "algorithm", first_problem = 206, last_problem = 206, occurrences = 1, teaching_state = "introduced", difficult = false, note = "先保存 next_node，再把 current.next 指向 previous，最后整体推进 previous/current。" }
"invariant.reversed_prefix_unprocessed_suffix" = { kind = "invariant", first_problem = 206, last_problem = 206, occurrences = 1, teaching_state = "introduced", difficult = false, note = "previous 是已反转前缀新头，current 是未处理后缀首节点；两部分不交且合并覆盖全部原节点。" }
"ownership.reverse_list_links" = { kind = "ownership", first_problem = 206, last_problem = 206, occurrences = 1, teaching_state = "introduced", difficult = true, note = "C/C++ 借用并重接指针不释放；Rust 以 Option<Box<Node>> 的 take 和移动转交唯一所有权；R/Julia 修改共享可变节点。" }
"proof.reverse_list_node_conservation" = { kind = "proof", first_problem = 206, last_problem = 206, occurrences = 1, teaching_state = "introduced", difficult = true, note = "每轮恰好移动一个节点，保存后继避免丢失，新增边只指向无环前缀，最终 previous 包含全部节点逆序。" }
"adapter.c_graph_csr_failure" = { kind = "language_adapter", first_problem = 207, last_problem = 207, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 使用两遍 CSR、完整失败清理；bool 平台接口无法区分有环与资源失败，文档显式保留该风险。" }
"adapter.graph_duplicate_edges" = { kind = "language_adapter", first_problem = 207, last_problem = 207, occurrences = 1, teaching_state = "introduced", difficult = false, note = "所有实现逐条保留重复边，邻接项和入度次数一致，防止去重失衡。" }
"adapter.r_julia_course_index" = { kind = "language_adapter", first_problem = 207, last_problem = 207, occurrences = 1, teaching_state = "introduced", difficult = false, note = "R/Julia 将零基课程编号加一访问数组；R 使用 CSR 并仅在 begin<=end 时构造 seq.int。" }
"algorithm.kahn_topological_sort" = { kind = "algorithm", first_problem = 207, last_problem = 207, occurrences = 1, teaching_state = "introduced", difficult = false, note = "初始入队全部零入度课程，弹出后逐条删除出边并在后继入度首次归零时入队。" }
"invariant.remaining_indegree" = { kind = "invariant", first_problem = 207, last_problem = 207, occurrences = 1, teaching_state = "introduced", difficult = false, note = "对每个未处理课程，indegree 始终等于来自未处理课程的剩余入边数；重复边按次数同步保存和递减。" }
"model.course_prerequisite_graph" = { kind = "model", first_problem = 207, last_problem = 207, occurrences = 1, teaching_state = "introduced", difficult = false, note = "先修对 [course, prerequisite] 建立 prerequisite->course，有向入度表示尚未处理的先修边数。" }
"proof.dag_zero_indegree" = { kind = "proof", first_problem = 207, last_problem = 207, occurrences = 1, teaching_state = "introduced", difficult = true, note = "有限非空 DAG 必有零入度节点，否则沿入边无限追溯会在有限节点中重复并形成环。" }
"proof.processed_equals_acyclic" = { kind = "proof", first_problem = 207, last_problem = 207, occurrences = 1, teaching_state = "introduced", difficult = true, note = "processed==V 给出合法拓扑序；队列空且 processed<V 时剩余图无零入度，因而必含有向环。" }
"adapter.c_trie_transactional_insert" = { kind = "language_adapter", first_problem = 208, last_problem = 208, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 在主树外构造完整缺失后缀，成功后一次接入；失败释放临时链并保持对象不变，void 接口仍无法报告失败。" }
"adapter.trie_object_lifecycle" = { kind = "language_adapter", first_problem = 208, last_problem = 208, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 递归释放，C++ unique_ptr、Rust Box、Julia mutable struct、R environment 与托管对象图分别说明唯一所有权、共享变异和回收成本。" }
"model.trie_prefix_path" = { kind = "model", first_problem = 208, last_problem = 208, occurrences = 1, teaching_state = "introduced", difficult = false, note = "根到节点的路径标签表示一个前缀；固定 26 子链接共享公共前缀，节点存在与完整单词存在分离。" }
"proof.trie_operation_sequence" = { kind = "proof", first_problem = 208, last_problem = 208, occurrences = 1, teaching_state = "introduced", difficult = true, note = "对字符路径和调用序列归纳，证明插入保持旧词、加入目标词，两种查询返回充要结果。" }
"state.trie_terminal_marker" = { kind = "state", first_problem = 208, last_problem = 208, occurrences = 1, teaching_state = "introduced", difficult = false, note = "is_word 只在完整插入词终点为真，使 search 检查路径加终止标记、startsWith 只检查路径。" }
"adapter.c_transactional_word_add" = { kind = "language_adapter", first_problem = 208, last_problem = 211, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "C void 添加接口先在主树外构造完整缺失链，成功后一次接入，失败时释放临时链并保持对象不变。" }
"adapter.r_environment_trie" = { kind = "language_adapter", first_problem = 208, last_problem = 211, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "R 使用 environment 节点获得共享可变语义；模式 strsplit 产生 O(L) 字符物化。" }
"adapter.wide_window_sum" = { kind = "language_adapter", first_problem = 209, last_problem = 209, occurrences = 1, teaching_state = "introduced", difficult = false, note = "固定宽语言使用 64 位窗口和，TypeScript/R 只在官方精确整数域内承诺准确，返回长度仍为普通整数。" }
"algorithm.positive_sliding_window" = { kind = "algorithm", first_problem = 209, last_problem = 209, occurrences = 1, teaching_state = "introduced", difficult = false, note = "右端加入正数；窗口达标时持续提交长度并移动左端，和不足时只扩展右端。" }
"invariant.window_exact_sum" = { kind = "invariant", first_problem = 209, last_problem = 209, occurrences = 1, teaching_state = "introduced", difficult = false, note = "sum 始终等于 nums[left..right] 的真实元素和，best 保存所有已提交可行窗口的最短长度。" }
"proof.discarded_left_boundary" = { kind = "proof", first_problem = 209, last_problem = 209, occurrences = 1, teaching_state = "introduced", difficult = true, note = "左端被越过前对应可行窗口已经提交；未来沿用同一起点只会更长，因此不会产生新的更短答案。" }
"proof.positive_window_minimality" = { kind = "proof", first_problem = 209, last_problem = 209, occurrences = 1, teaching_state = "introduced", difficult = true, note = "全正数使左缩和严格下降；算法对每个右端枚举所有连续可行左边界，并保证任意全局最短窗口被提交。" }
"algorithm.kahn_topological_order" = { kind = "algorithm", first_problem = 210, last_problem = 210, occurrences = 1, teaching_state = "introduced", difficult = false, note = "零入度课程出队顺序直接形成拓扑序；只有输出长度等于 V 才返回完整答案。" }
"invariant.valid_topological_prefix" = { kind = "invariant", first_problem = 210, last_problem = 210, occurrences = 1, teaching_state = "introduced", difficult = false, note = "order 始终是合法拓扑前缀，每个新课程出队时没有来自未处理课程的剩余入边。" }
"output.non_unique_topological_order" = { kind = "output_contract", first_problem = 210, last_problem = 210, occurrences = 1, teaching_state = "introduced", difficult = false, note = "合法拓扑序可不唯一，应验证课程唯一性和每条依赖边相对次序，而非固定数组。" }
"output.topological_order_payload" = { kind = "resource", first_problem = 210, last_problem = 210, occurrences = 1, teaching_state = "introduced", difficult = false, note = "工作空间 O(V+E) 之外，返回顺序本身具有 O(V) 输出载荷。" }
"proof.topological_output_complete_or_empty" = { kind = "proof", first_problem = 210, last_problem = 210, occurrences = 1, teaching_state = "introduced", difficult = true, note = "DAG 零入度引理保证无环时处理全部课程；有环时丢弃部分前缀并返回空结果。" }
"algorithm.trie_dot_branch_dfs" = { kind = "algorithm", first_problem = 211, last_problem = 211, occurrences = 1, teaching_state = "introduced", difficult = false, note = "普通字母只走一个子链接；点号枚举所有非空子链接并恰好消费一个模式字符。" }
"complexity.trie_actual_visited_states" = { kind = "complexity", first_problem = 211, last_problem = 211, occurrences = 1, teaching_state = "introduced", difficult = true, note = "无点号搜索 O(L)；含点号搜索按实际访问状态 M 计，最坏受 26^L 与 Trie 节点数共同限制。" }
"model.trie_pattern_state" = { kind = "model", first_problem = 211, last_problem = 211, occurrences = 1, teaching_state = "introduced", difficult = false, note = "递归状态由当前 Trie 节点与模式位置组成，位置等于已匹配路径深度。" }
"proof.wildcard_sound_complete" = { kind = "proof", first_problem = 211, last_problem = 211, occurrences = 1, teaching_state = "introduced", difficult = true, note = "字母分支与点号分支分别证明不多匹配和不漏匹配，模式结束后以 is_word 保证完整单词。" }
"adapter.c_word_search_ownership" = { kind = "language_adapter", first_problem = 212, last_problem = 212, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 事务式构建缺失 Trie 后缀，结果字符串独立复制，返回前释放 Trie；任一失败清理结果和节点。" }
"adapter.r_board_state_environment" = { kind = "language_adapter", first_problem = 212, last_problem = 212, occurrences = 1, teaching_state = "introduced", difficult = false, note = "R 使用 environment 保存 Trie 节点、棋盘和结果计数，使递归分支共享临时标记并逐层恢复。" }
"algorithm.multiword_trie_board_dfs" = { kind = "algorithm", first_problem = 212, last_problem = 212, occurrences = 1, teaching_state = "introduced", difficult = false, note = "先构建全部单词 Trie，再从每个棋盘格启动四邻接 DFS；不存在的 Trie 前缀立即剪枝。" }
"complexity.actual_board_trie_states" = { kind = "complexity", first_problem = 212, last_problem = 212, occurrences = 1, teaching_state = "introduced", difficult = true, note = "搜索时间按实际进入状态数 Q 报告；无剪枝粗略上界为 mn·4·3^(L-1)，并与 Trie、递归栈和输出载荷分开。" }
"invariant.board_path_trie_prefix" = { kind = "invariant", first_problem = 212, last_problem = 212, occurrences = 1, teaching_state = "introduced", difficult = false, note = "每个有效递归状态的棋盘路径字符与根到当前 Trie 节点标签完全一致，路径格互不重复。" }
"model.trie_board_joint_state" = { kind = "model", first_problem = 212, last_problem = 212, occurrences = 1, teaching_state = "introduced", difficult = false, note = "递归状态同时由棋盘坐标、当前 Trie 节点和当前路径访问集合决定；实现以棋盘哨兵隐式保存路径集合。" }
"proof.terminal_payload_dedup" = { kind = "proof", first_problem = 212, last_problem = 212, occurrences = 1, teaching_state = "introduced", difficult = true, note = "首次发现后只清空终止词索引，不删除节点或子链接；同词不重复输出，共享前缀下更长单词仍可搜索。" }
"proof.trie_prefix_pruning" = { kind = "proof", first_problem = 212, last_problem = 212, occurrences = 1, teaching_state = "introduced", difficult = true, note = "当前路径追加字符后若无 Trie 子链接，则它不是任何候选词前缀，继续延伸不可能重新成为合法前缀。" }
"model.circular_path_to_two_linear_paths" = { kind = "model", first_problem = 213, last_problem = 213, occurrences = 1, teaching_state = "introduced", difficult = false, note = "环上首屋与尾屋互斥；n>=2 时分别排除尾屋和首屋，得到两个普通线性不相邻选择区间，环形最优值等于两者最优值的较大者。" }
"proof.circular_first_last_exclusion_cover" = { kind = "proof", first_problem = 213, last_problem = 213, occurrences = 1, teaching_state = "introduced", difficult = true, note = "任意环形合法方案至少不选首屋或不选尾屋，因此被两个线性子问题覆盖；两个子问题各自缺少环的一端，所有候选在原环中仍合法。" }
"adapter.shortest_palindrome_string_materialization" = { kind = "language_adapter", first_problem = 214, last_problem = 214, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 显式分配序列、size_t 前缀表和答案；Julia codeunits 是视图但拼接与答案物化；R raw、UTF-16 和字节适配只在官方 ASCII 合同下等价。" }
"optimization.longest_palindromic_prefix" = { kind = "optimization", first_problem = 214, last_problem = 214, occurrences = 1, teaching_state = "introduced", difficult = false, note = "只能在前端添加字符时，任意添加 k 个字符的回文结果都会迫使原串前 n-k 个字符成为回文；最大化可保留回文前缀等价于最小化新增字符。" }
"proof.prepend_overlap_lower_bound" = { kind = "proof", first_problem = 214, last_problem = 214, occurrences = 1, teaching_state = "introduced", difficult = true, note = "通过结果串镜像位置 k+i 与 n-1-i 的下标关系，直接推出 s[0:n-k] 回文，并得到 k>=n-L 的最优性下界。" }
"safety.unique_separator_boundary" = { kind = "safety", first_problem = 214, last_problem = 214, occurrences = 1, teaching_state = "introduced", difficult = true, note = "分隔符必须不属于输入字符域且只出现一次；否则跨边界 border 与长度上界证明失效。" }
"string.kmp_border_palindrome_equivalence" = { kind = "string", first_problem = 214, last_problem = 214, occurrences = 1, teaching_state = "introduced", difficult = false, note = "在 s + unique_separator + reverse(s) 中，最终 border 长度不超过 n；其前缀是 P，后缀是 reverse(P)，因此 border 与回文前缀双向等价。" }
"adapter.quickselect_mutation_and_indices" = { kind = "language_adapter", first_problem = 215, last_problem = 215, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C/C++/Python/Java/Go/TypeScript/C#/Julia 原地重排可见输入；Rust 按值拥有 Vec；R 重排本地向量绑定。Rust 使用有符号游标避免右边界下溢。" }
"complexity.deterministic_pivot_worst_case" = { kind = "complexity", first_problem = 215, last_problem = 215, occurrences = 1, teaching_state = "introduced", difficult = true, note = "中点元素作为确定性 pivot 时单轮 O(m)、常见输入通常接近线性，但存在连续极不平衡分区，最坏总时间 O(n^2)，不能声称保证或无条件期望 O(n)。" }
"partition.three_way_quickselect_invariant" = { kind = "invariant", first_problem = 215, last_problem = 215, occurrences = 1, teaching_state = "introduced", difficult = false, note = "维护 [left,less)<pivot、[less,scan)==pivot、[scan,greater] 未知、(greater,right]>pivot；大值交换后 scan 不前进。" }
"proof.quickselect_target_region_reduction" = { kind = "proof", first_problem = 215, last_problem = 215, occurrences = 1, teaching_state = "introduced", difficult = true, note = "分区后目标只可能位于小值侧、等值段或大值侧；未命中等值段时新区间严格缩小，重复值全部由连续等值段覆盖。" }
"selection.kth_largest_target_index" = { kind = "selection", first_problem = 215, last_problem = 215, occurrences = 1, teaching_state = "introduced", difficult = false, note = "第 k 大在升序数组中的零基下标为 n-k；Julia/R 一基位置为 n-k+1，不能把降序下标 k-1 直接用于升序分区。" }
"adapter.two_dimensional_result_ownership" = { kind = "language_adapter", first_problem = 216, last_problem = 216, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 显式拥有行指针、列长度和每行快照并在失败时完整清理；托管语言提交时复制路径，Go 必须复制切片底层内容。" }
"backtracking.strictly_increasing_candidate_start" = { kind = "algorithm", first_problem = 216, last_problem = 216, occurrences = 1, teaching_state = "introduced", difficult = false, note = "选择 value 后只从 value+1 继续，使数字不重复，并让每个无序组合只有唯一的递增生成路径。" }
"invariant.remaining_slots_and_sum" = { kind = "invariant", first_problem = 216, last_problem = 216, occurrences = 1, teaching_state = "introduced", difficult = false, note = "递归状态保持 len(path)+slots=k 与 sum(path)+remaining=n；slots=0 时只有 remaining=0 才能提交。" }
"proof.unique_combination_path" = { kind = "proof", first_problem = 216, last_problem = 216, occurrences = 1, teaching_state = "introduced", difficult = true, note = "任意合法组合排序为 c1<...<ck 后，搜索中恰有一条依次选择这些数字的路径；严格递增阻止所有排列重复。" }
"pruning.maximum_reachable_sum" = { kind = "pruning", first_problem = 216, last_problem = 216, occurrences = 1, teaching_state = "introduced", difficult = false, note = "候选数量足够时，1..9 中最大的 slots 个数字给出可实现上界；remaining 更大时状态无解。" }
"pruning.minimum_reachable_sum" = { kind = "pruning", first_problem = 216, last_problem = 216, occurrences = 1, teaching_state = "introduced", difficult = false, note = "从 start 起连续选择 slots 个数字给出任何合法补全的可实现下界；remaining 更小时状态无解。" }
"adapter.c_hash_failure_exact_fallback" = { kind = "language_adapter", first_problem = 217, last_problem = 217, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 布尔接口没有错误通道；容量溢出或分配失败时释放资源并退回无分配的二次比较，保持答案正确，仅改变失败路径性能。" }
"adapter.c_open_addressing_occupied_metadata" = { kind = "language_adapter", first_problem = 217, last_problem = 217, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 线性探测表使用 values 与独立 occupied 数组，容量至少为 2*n 的二次幂，任何 int 包括 0、INT_MIN、INT_MAX 都不与空槽冲突。" }
"adapter.r_canonical_integer_environment_key" = { kind = "language_adapter", first_problem = 217, last_problem = 217, occurrences = 1, teaching_state = "introduced", difficult = false, note = "R 使用空父环境和 inherits=FALSE，并把精确 32 位整数编码为带前缀的无小数十进制键；零统一规范化，避免父环境命中与格式碰撞。" }
"complexity.hash_average_vs_worst" = { kind = "complexity", first_problem = 217, last_problem = 217, occurrences = 1, teaching_state = "introduced", difficult = true, note = "哈希查询和插入只在平均模型下为 O(1)，总平均 O(n)；对抗碰撞、聚簇或实现退化时总最坏可达 O(n^2)，不能写成严格保证线性。" }
"invariant.seen_prefix_value_set" = { kind = "invariant", first_problem = 217, last_problem = 217, occurrences = 1, teaching_state = "introduced", difficult = false, note = "处理下标 i 前，seen 恰好保存 nums[0:i] 的值集合；命中对应更早同值元素，未命中插入后为下一位置保持不变量。" }
"proof.duplicate_second_occurrence" = { kind = "proof", first_problem = 217, last_problem = 217, occurrences = 1, teaching_state = "introduced", difficult = true, note = "选择任一重复值第二次出现的最早位置，该位置查询时更早副本必在集合中；扫描结束未命中则不存在任何 j<i 的同值对。" }
"adapter.c_skyline_owned_rows" = { kind = "language_adapter", first_problem = 218, last_problem = 218, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 显式分配边界、堆、行指针、列长度与每个二元行；失败释放部分资源，成功所有权交给调用方。" }
"geometry.skyline_half_open_buildings" = { kind = "geometry", first_problem = 218, last_problem = 218, occurrences = 1, teaching_state = "introduced", difficult = false, note = "建筑 [left,right,height] 只在 left<=x<right 时活跃；right==x 必须在读取当前高度前失效。" }
"heap.lazy_expiration_by_right_endpoint" = { kind = "data_structure", first_problem = 218, last_problem = 218, occurrences = 1, teaching_state = "introduced", difficult = false, note = "堆保存 height 与 right；过期非顶部元素暂留无害，成为顶部时持续按 right<=x 清理。" }
"invariant.active_building_maximum" = { kind = "invariant", first_problem = 218, last_problem = 218, occurrences = 1, teaching_state = "introduced", difficult = false, note = "加入全部 left<=x 建筑并清理过期顶部后，堆顶若存在既活跃又不低于任何活跃建筑，等于当前轮廓高度。" }
"output.normalized_skyline_keypoints" = { kind = "output_contract", first_problem = 218, last_problem = 218, occurrences = 1, teaching_state = "introduced", difficult = false, note = "仅在高度变化时追加 [x,height]，保证横坐标严格递增、相邻高度不同，并在最大右边界回到 0。" }
"sweep.boundary_coordinate_compression" = { kind = "algorithm", first_problem = 218, last_problem = 218, occurrences = 1, teaching_state = "introduced", difficult = false, note = "高度只可能在左右边界变化；排序去重后每个横坐标整体处理全部开始与结束，避免重复横坐标和中间伪高度。" }

# Migration corrections: restore effective states lost or malformed during flattening.
"algorithm.level_next_chain_reuse" = { kind = "algorithm", first_problem = 116, last_problem = 117, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0116 复用已建当前层 next 链，0117 以 next_head/next_tail 常数空间构造任意形状下一层。" }
"boundary.explicit_chain_tail_null" = { kind = "boundary", first_problem = 116, last_problem = 117, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0116、0117 都显式保证每层最右 next 为空，覆盖旧链接并保证后续导航终止。" }
"interface.next_node_reference_model" = { kind = "interface", first_problem = 116, last_problem = 117, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0116 建立带 next 字段的完美树节点模型，0117 推广到稀疏树并保持节点身份与左右拓扑。" }
"proof.bfs_first_discovery_shortest" = { kind = "proof", first_problem = 126, last_problem = 127, occurrences = 2, teaching_state = "reinforcing", difficult = true, note = "0126 用距离与前驱 DAG 保存全部最短路径，0127 只求距离并在首次入队时删除未访问节点。" }
"workflow.risk_triggered_sampling" = { kind = "workflow", first_problem = 138, last_problem = 149, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0138 建立风险触发抽查；0149 因几何方向规范化、哈希退化和语言键语义风险执行针对性静态审查。" }
"workflow.premium_skip_progression" = { kind = "workflow", first_problem = 156, last_problem = 186, occurrences = 8, teaching_state = "mastered", difficult = false, note = "0156–0159、0161、0163、0170、0186 按 Premium 登记并保持题号推进。" }
"domain.database_problem_registration" = { kind = "domain_policy", first_problem = 175, last_problem = 185, occurrences = 10, teaching_state = "mastered", difficult = false, note = "Database 题不符合固定十语言算法产物合同，只登记 database_skipped，不创建空壳 RST。" }
"domain.database_skipped" = { kind = "domain_policy", first_problem = 175, last_problem = 197, occurrences = 12, teaching_state = "mastered", difficult = false, note = "新增 0196–0197 Database 登记，不生成固定十语言算法 RST。" }
"domain.shell_skipped" = { kind = "domain_policy", first_problem = 192, last_problem = 195, occurrences = 4, teaching_state = "familiar", difficult = false, note = "0192–0195 属于 Shell 文本处理域，只登记状态，不生成固定十语言算法空壳。" }
"adapter.unsigned_32_bit_cross_language" = { kind = "language_adapter", first_problem = 190, last_problem = 201, occurrences = 3, teaching_state = "familiar", difficult = false, note = "0190 建立 Java 逻辑右移、TypeScript >>>0 与 R 精确 double 适配，0191 强化置位计数；0201 再用于同步右移公共前缀和恢复低位零。" }
"algorithm.house_robber_prefix_dp" = { kind = "algorithm", first_problem = 198, last_problem = 213, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0198 建立线性前缀 DP；0213 把同一 take-or-skip 递推分别应用到排除首屋和排除尾屋的两个线性区间。" }
"invariant.house_robber_rolling_pair" = { kind = "invariant", first_problem = 198, last_problem = 213, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0198 维护 previous_one=F(i)、previous_two=F(i-1)；0213 在两个线性子区间中继续保持同一滚动状态。" }

# Legacy 0214–0218 problem indexes marked the following concepts as reinforced,
# but the earlier active range ledgers contained no recoverable full records.
# Register the first verifiable authoritative state instead of inventing prior counts.
"algorithm.kmp_prefix_function" = { kind = "algorithm", first_problem = 214, last_problem = 214, occurrences = 1, teaching_state = "introduced", difficult = true, note = "在拼接串上维护最长真前后缀长度；失配时沿 prefix[j-1] 回退，匹配时递增长度，使全部回退总成本保持线性。" }
"proof.lower_bound_matched_by_construction" = { kind = "proof", first_problem = 214, last_problem = 214, occurrences = 1, teaching_state = "introduced", difficult = true, note = "先证明任意方案至少补 n-L 个字符，再由 reverse(s[L:]) 的构造恰好达到该下界。" }
"string.ascii_code_unit_contract" = { kind = "string", first_problem = 214, last_problem = 214, occurrences = 1, teaching_state = "introduced", difficult = true, note = "官方 ASCII 输入使字节、UTF-16 code unit 与 Unicode code point 边界一致；超出 ASCII 后不得继续混用这些坐标。" }
"complexity.output_payload_separation" = { kind = "complexity", first_problem = 214, last_problem = 218, occurrences = 3, teaching_state = "familiar", difficult = true, note = "0214、0216、0218 分别把字符串、二维组合行和轮廓关键点的返回载荷，与核心工作状态及临时物化成本分开报告。" }
"safety.c_size_arithmetic_and_cleanup" = { kind = "safety", first_problem = 200, last_problem = 218, occurrences = 3, teaching_state = "familiar", difficult = true, note = "0200、0214、0218 的 C 适配器在分配前检查 size_t 加乘；失败时释放已拥有对象，成功时才发布结果或返回正常值。" }
"algorithm.order_statistics" = { kind = "algorithm", first_problem = 215, last_problem = 215, occurrences = 1, teaching_state = "introduced", difficult = false, note = "只要求第 k 个排序位置时使用选择算法，不必完成全局排序；第 k 大对应升序下标 n-k。" }
"proof.strict_interval_shrink" = { kind = "proof", first_problem = 215, last_problem = 215, occurrences = 1, teaching_state = "introduced", difficult = true, note = "未命中等值段时，新闭区间严格落在小值侧或大值侧并至少删除一个位置，因此搜索必然终止。" }
"array.in_place_partition" = { kind = "array", first_problem = 215, last_problem = 215, occurrences = 1, teaching_state = "introduced", difficult = false, note = "通过交换在原数组中维护分区，不建立元素副本数组；调用契约必须说明输入顺序会被重排。" }
"semantics.duplicate_values_preserve_multiplicity" = { kind = "language_semantics", first_problem = 215, last_problem = 215, occurrences = 1, teaching_state = "introduced", difficult = true, note = "重复值分别占据排序位置；三路分区可把等值集中处理，但不得去重或改成第 k 个不同值。" }
"safety.rust_signed_partition_cursor" = { kind = "safety", first_problem = 215, last_problem = 215, occurrences = 1, teaching_state = "introduced", difficult = true, note = "Rust 的右侧分区游标使用有符号类型，转换为 usize 访问前证明非负，避免零位置递减造成下溢。" }
"backtracking.path_restore" = { kind = "backtracking", first_problem = 216, last_problem = 216, occurrences = 1, teaching_state = "introduced", difficult = false, note = "进入分支时追加选择，返回父层前撤销该选择；成功、失败与剪枝路径都必须恢复父状态。" }
"output.mutable_path_snapshot" = { kind = "output_contract", first_problem = 216, last_problem = 216, occurrences = 1, teaching_state = "introduced", difficult = false, note = "提交答案时复制当前有效路径，后续 push、pop 或兄弟分支覆盖不得修改历史结果。" }
"proof.pruning_safety" = { kind = "proof", first_problem = 216, last_problem = 216, occurrences = 1, teaching_state = "introduced", difficult = true, note = "只有可达最小和已经超过 remaining，或可达最大和仍小于 remaining 时才剪枝；两类条件分别排除全部补全。" }
"safety.c_transactional_parallel_array_growth" = { kind = "safety", first_problem = 216, last_problem = 216, occurrences = 1, teaching_state = "introduced", difficult = true, note = "C 同步扩展行指针和列长元数据时使用事务式临时缓冲；任一失败保留旧地址并完整清理已有结果。" }
"adapter.r_explicit_recursive_state" = { kind = "language_adapter", first_problem = 216, last_problem = 216, occurrences = 1, teaching_state = "introduced", difficult = false, note = "R 显式传递 start、slots、remaining 与路径状态，并用受控结果容器保存快照，避免隐式全局副作用。" }
"algorithm.query_before_insert" = { kind = "algorithm", first_problem = 217, last_problem = 217, occurrences = 1, teaching_state = "introduced", difficult = false, note = "扫描当前元素时先查询 seen，再插入当前值，使命中见证必定来自严格更早的输入前缀。" }
"proof.soundness_and_completeness" = { kind = "proof", first_problem = 217, last_problem = 217, occurrences = 1, teaching_state = "introduced", difficult = true, note = "命中时给出两个不同下标上的相等值；任一重复值的第二次出现又必然命中，分别证明可靠性与完备性。" }
"safety.capacity_arithmetic" = { kind = "safety", first_problem = 217, last_problem = 217, occurrences = 1, teaching_state = "introduced", difficult = true, note = "哈希容量的 2*n、二次幂上取整和字节数乘法都在 size_t 中先检查溢出，再执行分配。" }
"safety.signed_to_unsigned_hash_domain" = { kind = "safety", first_problem = 217, last_problem = 217, occurrences = 1, teaching_state = "introduced", difficult = true, note = "有符号整数先映射为同宽无符号位模式再参与哈希，避免负数绝对值和 INT_MIN 的未定义边界。" }
"complexity.input_read_only_vs_sorting" = { kind = "complexity", first_problem = 217, last_problem = 217, occurrences = 1, teaching_state = "introduced", difficult = true, note = "哈希扫描只读输入并使用额外集合；排序替代方案会修改或复制输入且时间为 O(n log n)，接口与复杂度必须分别说明。" }
"algorithm.sweep_line" = { kind = "algorithm", first_problem = 218, last_problem = 218, occurrences = 1, teaching_state = "introduced", difficult = false, note = "按横坐标递增处理全部边界事件，更新活动建筑集合并读取当前最大高度，只在高度变化时提交关键点。" }
"data_structure.max_heap" = { kind = "data_structure", first_problem = 218, last_problem = 218, occurrences = 1, teaching_state = "introduced", difficult = false, note = "最大堆按高度维护活动建筑极值，插入与弹出为 O(log n)，堆顶提供当前轮廓高度候选。" }
"proof.event_coordinates_suffice" = { kind = "proof", first_problem = 218, last_problem = 218, occurrences = 1, teaching_state = "introduced", difficult = true, note = "建筑活动集合只可能在 left 或 right 边界变化，两个相邻边界之间高度恒定，不会产生新的关键点。" }
"proof.lazy_deletion_extreme_query" = { kind = "proof", first_problem = 218, last_problem = 218, occurrences = 1, teaching_state = "introduced", difficult = true, note = "过期非堆顶元素暂留不会影响最大值；每次极值查询前清理全部过期堆顶即可保持答案正确。" }
"complexity.heap_push_pop_amortization" = { kind = "complexity", first_problem = 218, last_problem = 218, occurrences = 1, teaching_state = "introduced", difficult = true, note = "每栋建筑入堆一次并最终至多出堆一次，全部堆操作合计 O(n log n)，活动堆峰值 O(n)。" }
