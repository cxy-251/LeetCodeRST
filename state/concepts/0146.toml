[meta]
version = 1
range = "0146"
last_problem = 146
inherits = "state/concepts/0145.toml"

[updates]
"workflow.targeted_example_validation" = "0134 引入针对性样例验证；0146 继续执行，累计强化 13 次。"

[concepts]
"datastructure.lru_hash_doubly_linked_list" = "kind=data_structure; introduced=0146; last=0146; occurrences=1; teaching_state=explained; note=哈希表负责按键定位，双向链表维护最近到最久使用顺序，组合后 get 与 put 平均 O(1)。"
"invariant.lru_map_list_bijection" = "kind=invariant; introduced=0146; last=0146; occurrences=1; teaching_state=explained; note=每个有效键恰好对应一个链表节点；移动、更新和淘汰必须同步保持哈希记录与链表节点一一对应。"
"adapter.r_environment_cache_nodes" = "kind=language_adapter; introduced=0146; last=0146; occurrences=1; teaching_state=explained; note=R 用 environment 表达缓存和双向节点引用，并用哈希 environment 保存键到节点映射。"
