[meta]
version = 1
range = "0152"
last_problem = 152
inherits = "state/concepts/0151.toml"

[updates]
"workflow.targeted_static_review" = "0134 引入人工示例推演与逐语言静态审查；0152 继续执行，累计强化 19 次。"

[concepts]
"algorithm.max_min_product_ending_here" = "kind=algorithm; introduced=0152; last=0152; occurrences=1; teaching_state=explained; note=同时维护以当前位置结尾的最大与最小乘积，负数使两者角色交换。"
"invariant.nonempty_subarray_initialization" = "kind=invariant; introduced=0152; last=0152; occurrences=1; teaching_state=explained; note=滚动状态从首元素初始化，确保所有候选都是题目允许的非空连续子数组。"
"technique.scalar_dynamic_programming" = "kind=technique; introduced=0152; last=0152; occurrences=1; teaching_state=explained; note=完整 DP 只依赖前一位置的最大值和最小值，可压缩为常数个滚动标量。"
