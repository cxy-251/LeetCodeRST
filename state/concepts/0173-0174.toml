[meta]
version = 1
range = "0173-0174"
last_problem = 174
inherits = "state/concepts/0169-0172.toml"

[batch]
completed = 2
difficulty_budget = 6
summary = "完成 BST 惰性中序迭代器与地下城最小初始生命逆向动态规划。"

[updates]
"algorithm.lazy_bst_inorder_iterator" = "kind=algorithm; introduced=0173; last=0173; occurrences=1; teaching_state=explained; note=构造与每次 next 只展开当前子树最左路径，显式栈保存递归中序续点，不预先物化完整序列。"
"invariant.inorder_continuation_stack" = "kind=invariant; introduced=0173; last=0173; occurrences=1; teaching_state=explained; note=栈等价于暂停在 visit 前的递归调用栈；栈顶左侧工作已完成，是下一中序节点。"
"proof.bst_iterator_no_skip_no_duplicate" = "kind=proof; introduced=0173; last=0173; occurrences=1; teaching_state=explained; note=根和每个父节点右子树各形成唯一入口，树节点最多压栈一次并由一次 next 弹出，BST 中序保证升序。"
"complexity.bst_iterator_amortized_next" = "kind=complexity; introduced=0173; last=0173; occurrences=1; teaching_state=explained; note=单次 next 最坏沿左链 O(h)，全遍历每节点入栈出栈各一次，总 O(n)，故均摊 O(1)，峰值空间 O(h)。"
"resource.c_iterator_dynamic_stack" = "kind=resource; introduced=0173; last=0173; occurrences=1; teaching_state=explained; note=C 用几何扩容节点指针栈，realloc 临时指针防丢失，构造失败清理，free 只释放迭代器资源；平台 next 无独立 OOM 通道。"
"adapter.mutable_iterator_state" = "kind=language_adapter; introduced=0173; last=0173; occurrences=1; teaching_state=explained; note=Julia 用 mutable struct，R 用 environment 和环境栈帧，Rust 缩短 RefCell 借用后再修改迭代器。"
"algorithm.reverse_minimum_health_dp" = "kind=algorithm; introduced=0174; last=0174; occurrences=1; teaching_state=explained; note=need(i,j) 定义为进入格前完成剩余路径的最小生命，逆向转移 max(1,min(down,right)-cell)。"
"proof.minimum_health_necessity_sufficiency" = "kind=proof; introduced=0174; last=0174; occurrences=1; teaching_state=explained; note=低于状态值时无法满足生命下界或任一后继需求；等于状态值并选择较小需求后继时可由归纳策略存活。"
"invariant.dungeon_rolling_row" = "kind=invariant; introduced=0174; last=0174; occurrences=1; teaching_state=explained; note=右向左更新前 dp[j] 是下方状态，dp[j+1] 已是当前行右侧状态，更新后 dp[j] 成为当前格状态。"
"boundary.single_virtual_princess_successor" = "kind=boundary; introduced=0174; last=0174; occurrences=1; teaching_state=explained; note=仅公主格下方设置需求 1 的虚拟后继，其他边界为 INF，防止路径从普通边界逃出网格。"
"numeric.dungeon_wide_need_state" = "kind=numeric; introduced=0174; last=0174; occurrences=1; teaching_state=explained; note=固定宽语言用 64 位需求和远离上界的 INF；扫描保证至少一个有限后继，哨兵不参与减法。"
"workflow.targeted_static_review" = "kind=workflow; introduced=0167; last=0174; occurrences=7; teaching_state=explained; note=本批两题仅执行人工状态推演、证明映射、十语言静态语义和仓库一致性检查，未运行、未编译、未测试题解。"
