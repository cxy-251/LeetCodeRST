[meta]
version = 1
range = "0189-0197"
last_problem = 197
inherits = "state/concepts/0188.toml"

[batch]
completed = 3
shell_skipped = [192, 193, 194, 195]
database_skipped = [196, 197]
difficulty_budget = 4
scope_shrunk = true
summary = "完成数组右旋与两道 32 位位运算题，登记 Shell 与 Database 域题；0198 独立后移。"

[updates]
"algorithm.rotate_array_three_reversals" = "kind=algorithm; introduced=0189; last=0189; occurrences=1; teaching_state=explained; note=把原数组分成 A 与末尾 B，利用 rev(A+B)=rev(B)+rev(A)，再分别恢复 B 与 A 得到右旋。"
"invariant.reverse_closed_interval" = "kind=invariant; introduced=0189; last=0189; occurrences=1; teaching_state=explained; note=闭区间反转中外侧对称位置已就位、未处理元素保持在 left..right，循环内缩直至完成。"
"adapter.array_mutation_visibility" = "kind=language_adapter; introduced=0189; last=0189; occurrences=1; teaching_state=explained; note=C 至 Julia 修改调用者可见数组；R 普通向量局部绑定需返回结果，不能宣称调用者绑定原地改变。"
"algorithm.fixed_width_bit_reversal" = "kind=algorithm; introduced=0190; last=0190; occurrences=1; teaching_state=explained; note=固定执行 32 轮，每轮把输入最低位送入左移后的答案，前导零也占据反转位置。"
"invariant.reverse_bits_prefix" = "kind=invariant; introduced=0190; last=0190; occurrences=1; teaching_state=explained; note=t 轮后答案低 t 位从高到低是原输入 b0..b(t-1)，输入为原位模式逻辑右移 t 位。"
"adapter.unsigned_32_bit_cross_language" = "kind=language_adapter; introduced=0190; last=0191; occurrences=2; teaching_state=explained; note=Java 用 32 位补码与逻辑右移，TypeScript 每轮 >>>0，R 用精确 double 的除二取余适配完整无符号域。"
"algorithm.kernighan_clear_lowest_one" = "kind=algorithm; introduced=0191; last=0191; occurrences=1; teaching_state=explained; note=n&(n-1) 每轮恰好清除最低有效 1，循环次数等于汉明重量。"
"proof.lowest_set_bit_elimination" = "kind=proof; introduced=0191; last=0191; occurrences=1; teaching_state=explained; note=n-1 只翻转最低 1 及其低位；按位与保留高位并清除该最低 1。"
"domain.shell_skipped" = "kind=domain_policy; introduced=0192; last=0195; occurrences=4; teaching_state=registered; note=0192–0195 属于 Shell 文本处理域，只登记状态，不生成固定十语言算法空壳。"
"domain.database_skipped" = "kind=domain_policy; introduced=0175; last=0197; occurrences=12; teaching_state=registered; note=新增 0196–0197 Database 登记，不生成固定十语言算法 RST。"
"workflow.targeted_static_review" = "kind=workflow; introduced=0167; last=0191; occurrences=13; teaching_state=explained; note=本批三题仅执行人工推演、证明映射、十语言静态语义和仓库一致性检查；未运行、未编译、未测试题解。"
