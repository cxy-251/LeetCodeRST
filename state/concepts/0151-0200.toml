[meta]
version = 8
last_problem = 200

# teaching_state: introduced / reinforcing / familiar / refresh / mastered

[concepts]
"invariant.level_frontier_snapshot" = { kind = "invariant", first_problem = 102, last_problem = 199, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "每轮开始时队列快照恰好包含当前层全部节点，新入队孩子只属于下一层。" }
"invariant.mark_before_push" = { kind = "invariant", first_problem = 130, last_problem = 200, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "邻居在压入队列或栈前立即标记，防止被多个相邻格重复发现。" }
"workflow.targeted_static_review" = { kind = "workflow", first_problem = 134, last_problem = 200, occurrences = 42, teaching_state = "reinforcing", difficult = false, note = "0151–0200 继续执行人工推演、证明映射、逐语言静态语义和仓库一致性检查；未运行、未编译、未测试题解。" }
"adapter.c_reverse_words_owned_result" = { kind = "language_adapter", first_problem = 151, last_problem = 151, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 按 n+1 分配返回缓冲，r<=n 保证写入与终止符有界；失败返回 NULL，成功结果由调用者释放。" }
"algorithm.reverse_word_scan_from_right" = { kind = "algorithm", first_problem = 151, last_problem = 151, occurrences = 1, teaching_state = "introduced", difficult = false, note = "以半开右边界 end 从右向左交替跳过空格、定位 [start,end) 单词并正向写出；每轮越过一个完整单词。" }
"cost.string_slice_and_join_materialization" = { kind = "complexity", first_problem = 151, last_problem = 151, occurrences = 1, teaching_state = "introduced", difficult = true, note = "统一区分 O(n) 扫描、O(r) 返回；各语言另计切片复制、单词容器、构建器容量、输入副本与 join 峰值共存。" }
"invariant.reverse_words_unprocessed_prefix" = { kind = "invariant", first_problem = 151, last_problem = 151, occurrences = 1, teaching_state = "introduced", difficult = false, note = "循环入口时 [0,end) 是唯一未处理前缀，输出已包含其右侧全部单词的逆序，且每个原词恰写一次。" }
"invariant.single_separator_output" = { kind = "invariant", first_problem = 151, last_problem = 151, occurrences = 1, teaching_state = "introduced", difficult = false, note = "首词前不写分隔符，后续提交前恰写一个空格；输出无首尾空格且相邻词间始终恰有一个空格。" }
"proof.reverse_words_exact_word_coverage" = { kind = "proof", first_problem = 151, last_problem = 151, occurrences = 1, teaching_state = "introduced", difficult = true, note = "跳过的字符全为空格，随后取得未处理前缀最右最大非空格片段；严格左移保证不重不漏和终止。" }
"semantics.ascii_word_scan_units" = { kind = "language_semantics", first_problem = 151, last_problem = 151, occurrences = 1, teaching_state = "introduced", difficult = true, note = "ASCII 合同允许字节或代码单元扫描；该证明不能无条件推广到任意 Unicode 用户感知字符。" }
"algorithm.max_min_product_ending_here" = { kind = "algorithm", first_problem = 152, last_problem = 152, occurrences = 1, teaching_state = "introduced", difficult = false, note = "当前位置只比较 value、old_max*value、old_min*value 三个候选；乘负数反转极值角色，乘零令扩展候选合并为零。" }
"invariant.maximum_minimum_product_endpoints" = { kind = "invariant", first_problem = 152, last_problem = 152, occurrences = 1, teaching_state = "introduced", difficult = false, note = "max_ending/min_ending 是所有恰以当前位置结尾的非空子数组乘积两端极值，answer 覆盖全部已处理右端点。" }
"invariant.nonempty_subarray_initialization" = { kind = "invariant", first_problem = 152, last_problem = 152, occurrences = 1, teaching_state = "introduced", difficult = false, note = "三个状态从 nums[0] 初始化，避免用 0 或 1 引入不存在的空子数组候选。" }
"numeric.actual_subarray_product_witness" = { kind = "numeric", first_problem = 152, last_problem = 152, occurrences = 1, teaching_state = "introduced", difficult = true, note = "每个 DP 候选都有真实连续子数组见证，因此官方任意子数组乘积适合 32 位的保证覆盖最大、最小和扩展候选。" }
"proof.product_subarray_right_endpoint_partition" = { kind = "proof", first_problem = 152, last_problem = 152, occurrences = 1, teaching_state = "introduced", difficult = true, note = "每个以 i 结尾的子数组要么单取 i，要么唯一延长一个以 i-1 结尾的子数组；乘以固定数的单调或反单调性使旧两端充分。" }
"semantics.same_old_state_parallel_update" = { kind = "language_semantics", first_problem = 152, last_problem = 152, occurrences = 1, teaching_state = "introduced", difficult = true, note = "新最大与新最小必须来自同一组三候选；先覆盖最大值再计算最小值会在一轮内重复使用当前元素。" }
"technique.scalar_dynamic_programming" = { kind = "technique", first_problem = 152, last_problem = 152, occurrences = 1, teaching_state = "introduced", difficult = false, note = "完整 DP 只依赖前一位置双极值，可压缩为常数标量；覆盖前必须先保存同一旧状态的两个扩展候选。" }
"complexity.binary_candidate_length_halving" = { kind = "complexity", first_problem = 153, last_problem = 153, occurrences = 1, teaching_state = "introduced", difficult = true, note = "旧闭区间长度 s>=2 时任一分支新长度至多 ceil(s/2)，由长度递推而非二分外形证明最坏 O(log n)。" }
"invariant.minimum_index_closed_interval" = { kind = "invariant", first_problem = 153, last_problem = 153, occurrences = 1, teaching_state = "introduced", difficult = false, note = "闭区间始终包含唯一最小值下标；大于分支保留 [mid+1,right]，小于分支保留 [left,mid]。" }
"proof.rotated_two_segment_exclusion" = { kind = "proof", first_problem = 153, last_problem = 153, occurrences = 1, teaching_state = "introduced", difficult = true, note = "旋转后由较大值段与从最小值开始的较小值段组成；同段严格递增与跨段值序共同证明两个分支的整段排除。" }
"semantics.closed_interval_lower_midpoint" = { kind = "language_semantics", first_problem = 153, last_problem = 153, occurrences = 1, teaching_state = "introduced", difficult = true, note = "left<right 时下中点满足 left<=mid<right，支撑比较不同下标、严格收缩和各语言边界安全。" }
"technique.binary_search_keep_mid_candidate" = { kind = "technique", first_problem = 153, last_problem = 153, occurrences = 1, teaching_state = "introduced", difficult = false, note = "小于右端只排除中点右侧，中点可能正是唯一最小值，因此必须 right=mid 而不能 mid-1。" }
"algorithm.rotated_array_minimum_right_boundary" = { kind = "algorithm", first_problem = 153, last_problem = 154, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "互异版本按中点与右端严格比较保留含最小值的一侧；重复值版本在等值无法判向时只删除有中点同值副本的右端。" }
"complexity.duplicate_binary_search_linear_worst_case" = { kind = "complexity", first_problem = 154, last_problem = 154, occurrences = 1, teaching_state = "introduced", difficult = true, note = "每轮至少删一项给出 O(n) 上界；全相等长度 n 输入执行 n-1 个等值分支，给出 Omega(n)，最坏为 Theta(n)。" }
"invariant.minimum_value_witness_interval" = { kind = "invariant", first_problem = 154, last_problem = 154, occurrences = 1, teaching_state = "introduced", difficult = false, note = "候选区间始终至少含一个值等于全局最小值的位置；允许删除某个最小下标，不要求保留固定答案身份。" }
"proof.opposite_direction_equal_comparison_counterexamples" = { kind = "proof", first_problem = 154, last_problem = 154, occurrences = 1, teaching_state = "introduced", difficult = true, note = "两个中点等于右端但最小值分处左右的输入证明等值比较确实无法固定判向。" }
"semantics.unsigned_right_decrement_guard" = { kind = "language_semantics", first_problem = 154, last_problem = 154, occurrences = 1, teaching_state = "introduced", difficult = true, note = "right 减一只发生在 left<right 的循环内，保证 Rust usize 及一基 Julia/R 边界不会下溢。" }
"technique.equal_right_endpoint_disambiguation" = { kind = "technique", first_problem = 154, last_problem = 154, occurrences = 1, teaching_state = "introduced", difficult = false, note = "nums[mid]==nums[right] 时令 right-=1；若右端是最小值，仍留在新区间的 mid 提供替代见证。" }
"adapter.c_min_stack_resource_atomicity" = { kind = "language_adapter", first_problem = 155, last_problem = 173, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0155 与 0173 都用 realloc 临时指针和先扩容后改状态；固定平台方法缺少资源错误通道，正常功能合同以分配成功为前提。" }
"complexity.amortized_dynamic_array_push" = { kind = "complexity", first_problem = 155, last_problem = 173, occurrences = 2, teaching_state = "reinforcing", difficult = true, note = "0155 区分动态数组 push 的单次扩容与摊还成本；0173 的 C 节点栈同样几何扩容，总复制受峰值树高约束。" }
"data_structure.augmented_stack" = { kind = "data_structure", first_problem = 155, last_problem = 155, occurrences = 1, teaching_state = "introduced", difficult = false, note = "每层保存 (value,prefix_min)，把昂贵的最小值查询提前到 push 时增量计算，并随 pop 回滚。" }
"invariant.prefix_minimum_per_entry" = { kind = "invariant", first_problem = 155, last_problem = 155, occurrences = 1, teaching_state = "introduced", difficult = false, note = "第 i 层 minimum 等于当前栈底到该层全部值的最小值；末层同时证实 top 与 getMin，重复最小值逐层保留。" }
"proof.min_stack_operation_sequence_induction" = { kind = "proof", first_problem = 155, last_problem = 155, occurrences = 1, teaching_state = "introduced", difficult = true, note = "构造建立空状态，push 建立新层证书，pop 删除完整末层并恢复旧证书，查询只读不变量；对任意合法操作序列归纳。" }
"semantics.mutable_stack_object_identity" = { kind = "language_semantics", first_problem = 155, last_problem = 173, occurrences = 2, teaching_state = "reinforcing", difficult = true, note = "0155 与 0173 的方法都必须修改同一持久栈对象；Go 用指针接收者，Julia 用 mutable struct，R 用 environment 保存跨调用状态。" }
"workflow.premium_skip_progression" = { kind = "workflow", first_problem = 156, last_problem = 186, occurrences = 8, teaching_state = "introduced", difficult = false, note = "0156–0159、0161、0163、0170、0186 按 Premium 登记并保持题号推进。" }
"adapter.rust_shared_tail_rc" = { kind = "adapter", first_problem = 160, last_problem = 160, occurrences = 1, teaching_state = "introduced", difficult = false, note = "Box 不能被两个头共同拥有；教学适配器用 Rc 表达共享尾链、ptr_eq 比较身份，clone 只调整引用计数。" }
"contract.linked_list_node_identity" = { kind = "contract", first_problem = 160, last_problem = 160, occurrences = 1, teaching_state = "introduced", difficult = false, note = "相交按节点对象身份判断；值相同或内容相同的独立节点不相交，共享节点后因 next 唯一而共享整条后缀。" }
"invariant.concatenated_pointer_identity_sequences" = { kind = "invariant", first_problem = 160, last_problem = 160, occurrences = 1, teaching_state = "introduced", difficult = false, note = "更新 t 次后两个指针分别位于各自组合身份序列第 t 个状态；空哨兵是代码真实经过的状态。" }
"proof.intersection_alignment_with_null_sentinel" = { kind = "proof", first_problem = 160, last_problem = 160, occurrences = 1, teaching_state = "introduced", difficult = true, note = "独有前缀 a,b、共享后缀 c 时，两指针至迟在 a+b+c+1 次更新后到首个共享节点；无交点时在 m+n+1 次后同为空。" }
"semantics.cross_language_reference_identity" = { kind = "language_semantics", first_problem = 160, last_problem = 160, occurrences = 1, teaching_state = "introduced", difficult = true, note = "Python is、C# ReferenceEquals、Julia ===、R 环境 identical 与各原生指针比较都必须绕开值或结构相等。" }
"technique.two_head_pointer_switching" = { kind = "technique", first_problem = 160, last_problem = 160, occurrences = 1, teaching_state = "introduced", difficult = false, note = "两个指针分别枚举 A,null,B 与 B,null,A，同组成不同顺序让第二段自动抵消独有前缀长度差。" }
"algorithm.binary_search_adjacent_slope" = { kind = "algorithm", first_problem = 162, last_problem = 162, occurrences = 1, teaching_state = "introduced", difficult = false, note = "比较 nums[mid] 与右邻；上升时保留 [mid+1,right]，下降时保留 [left,mid]，不寻找全局最大值。" }
"complexity.peak_candidate_halving" = { kind = "complexity", first_problem = 162, last_problem = 162, occurrences = 1, teaching_state = "introduced", difficult = true, note = "长度 s>=2 的候选在任一分支后至多为 ceil(s/2)，正式支撑最坏 O(log n)。" }
"contract.virtual_negative_infinity_boundaries" = { kind = "contract", first_problem = 162, last_problem = 162, occurrences = 1, teaching_state = "introduced", difficult = false, note = "数组外侧数学上视为负无穷，使首尾可成峰；代码不以合法 INT_MIN 物化哨兵。" }
"invariant.peak_exists_in_candidate_interval" = { kind = "invariant", first_problem = 162, last_problem = 162, occurrences = 1, teaching_state = "introduced", difficult = false, note = "候选左端要么在边界要么大于左外邻，右端对称大于右外邻；双边界证书保证闭区间内至少有峰。" }
"proof.peak_boundary_certificate_preservation" = { kind = "proof", first_problem = 162, last_problem = 162, occurrences = 1, teaching_state = "introduced", difficult = true, note = "上升分支为新左端建立左证书，下降分支为新右端建立右证书；收缩到单点时两证书直接给出严格峰。" }
"semantics.adjacent_read_and_zero_based_return" = { kind = "language_semantics", first_problem = 162, last_problem = 162, occurrences = 1, teaching_state = "introduced", difficult = true, note = "left<right 与下中点保证 mid+1<=right；Julia/R 内部一基，返回时必须减一得到题目零基下标。" }
"adapter.c_gap_bucket_allocation_failure" = { kind = "language_adapter", first_problem = 164, last_problem = 164, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C calloc 数量由 B<=n 支撑，成功路径 free；失败只能返回与合法结果不可区分的 0，明确为接口限制。" }
"algorithm.pigeonhole_gap_buckets" = { kind = "algorithm", first_problem = 164, last_problem = 164, occurrences = 1, teaching_state = "introduced", difficult = false, note = "以 ceil((high-low)/(n-1)) 作为桶宽，每桶只存实际最小/最大，按桶号扫描相邻非空桶边界。" }
"complexity.bucket_count_bounded_by_input" = { kind = "complexity", first_problem = 164, last_problem = 164, occurrences = 1, teaching_state = "introduced", difficult = true, note = "w>=R/(n-1) 推出 floor(R/w)+1<=n，桶空间随元素数线性而非随 10^9 值域增长。" }
"invariant.bucket_internal_gap_bound" = { kind = "invariant", first_problem = 164, last_problem = 164, occurrences = 1, teaching_state = "introduced", difficult = false, note = "同桶整数落在宽 w 的半开区间，任意差至多 w-1；最大排序间距至少 w，故目标必跨桶。" }
"numeric.widened_bucket_arithmetic" = { kind = "numeric", first_problem = 164, last_problem = 164, occurrences = 1, teaching_state = "introduced", difficult = true, note = "值域、上取整分子、桶号分子和跨桶差均在运算前提升到 64 位，最终答案不超过 10^9。" }
"proof.adjacent_nonempty_bucket_sorted_neighbors" = { kind = "proof", first_problem = 164, last_problem = 164, occurrences = 1, teaching_state = "introduced", difficult = true, note = "相邻非空桶之间没有输入值，前桶最大值与后桶最小值因此在全局排序序列中真正相邻。" }
"algorithm.version_significant_span_compare" = { kind = "algorithm", first_problem = 165, last_problem = 165, occurrences = 1, teaching_state = "introduced", difficult = false, note = "每段定位 end、跳零得 significant，以有效位数和等长 ASCII 字典序比较，不解析任意长整数。" }
"complexity.version_interface_materialization" = { kind = "complexity", first_problem = 165, last_problem = 165, occurrences = 1, teaching_state = "introduced", difficult = true, note = "核心扫描 O(n+m) 时间/O(1) 索引；另计 C++ 按值副本、Java/TS 短命段对象、Rust 所有权与 R 输入物化。" }
"invariant.missing_revision_equals_zero" = { kind = "invariant", first_problem = 165, last_problem = 165, occurrences = 1, teaching_state = "introduced", difficult = false, note = "全零段与已结束侧的缺失修订号都规范为有效长度 0；较短版本结束后仍逐段核对非零尾部。" }
"invariant.version_scanner_progress" = { kind = "invariant", first_problem = 165, last_problem = 165, occurrences = 1, teaching_state = "introduced", difficult = false, note = "循环继续时至少一侧未结束，该侧跨过一个合法非空段并严格前进；结束侧可停留为缺失零段而不死循环。" }
"proof.decimal_span_numeric_order" = { kind = "proof", first_problem = 165, last_problem = 165, occurrences = 1, teaching_state = "introduced", difficult = true, note = "无前导零十进制串先由位数定序，等长时首个不同高位定序；首次不同修订号决定版本关系。" }
"string.ascii_segment_scanning" = { kind = "string", first_problem = 165, last_problem = 187, occurrences = 4, teaching_state = "familiar", difficult = false, note = "0165、0171、0179 与 0187 的 ASCII 合同支撑字节或代码单元扫描；0187 的 Julia codeunits 为包装，R charToRaw 物化 O(L) 输入字节。" }
"algorithm.long_division_remainder_position_map" = { kind = "algorithm", first_problem = 166, last_problem = 166, occurrences = 1, teaching_state = "introduced", difficult = false, note = "整数部分后以当前非零余数为状态，在乘十前记录 remainder->next_digit_position；余数重复时在首次位置插入左括号。" }
"complexity.output_sensitive_recurring_decimal" = { kind = "complexity", first_problem = 166, last_problem = 166, occurrences = 1, teaching_state = "introduced", difficult = true, note = "以生成位数 L 和不同余数 r 报告期望 O(L) 时间、O(L+r) 空间、一次括号插入 O(L)；哈希极端冲突最坏可达 O(L^2)。" }
"invariant.exact_fraction_decimal_prefix" = { kind = "invariant", first_problem = 166, last_problem = 166, occurrences = 1, teaching_state = "introduced", difficult = false, note = "生成 k 位后保持 N/D=q+sum(d_i/10^i)+r_k/(D*10^k)，且 0<=r_k<D；每轮 10r=dD+r' 精确延伸一位。" }
"language.javascript_r_exact_integer_range" = { kind = "language_adapter", first_problem = 166, last_problem = 166, occurrences = 1, teaching_state = "introduced", difficult = false, note = "TypeScript/R 的最大 remainder*10 约 2.15e10，低于 2^53；不使用 JS 32 位位运算，R 用固定槽与 environment 避免逐位向量重建。" }
"numeric.int_min_absolute_value_widening" = { kind = "syntax", first_problem = 29, last_problem = 166, occurrences = 3, teaching_state = "familiar", difficult = true, note = "0029、0050 与 0166 都先提升到 64 位，再处理最小 32 位负整数的绝对值或相反数；0166 同时覆盖分子、分母与负零。" }
"proof.remainder_finite_or_cycle_dichotomy" = { kind = "proof", first_problem = 166, last_problem = 166, occurrences = 1, teaching_state = "introduced", difficult = true, note = "余数为零恰好有限终止；否则非零余数来自有限集合且转移确定，首次重复状态给出函数图的环入口和循环部分。" }
"resource.c_transactional_builder_and_remainder_hash" = { kind = "language_adapter", first_problem = 166, last_problem = 166, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 动态字符串检查终止符容量和 realloc，开放寻址表保持二次幂容量并失败清理；成功返回堆字符串所有权。" }
"adapter.one_based_index_contract" = { kind = "language_adapter", first_problem = 167, last_problem = 167, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C/C++/Python/Java/Rust/Go/TS/C# 内部零基返回前加一；Julia/R 内部已一基，不能重复加一。" }
"algorithm.sorted_anchor_two_pointer_sum" = { kind = "algorithm", first_problem = 15, last_problem = 167, occurrences = 3, teaching_state = "familiar", difficult = true, note = "0015–0016 在固定锚点后的有序后缀使用双指针；0167 直接在完整非递减数组两端排除，不再需要外层锚点。" }
"invariant.two_sum_solution_interval" = { kind = "invariant", first_problem = 167, last_problem = 167, occurrences = 1, teaching_state = "introduced", difficult = false, note = "尚未返回时唯一解始终位于 [left,right]；区间外端点均已由单调不等式证明不可能参与解。" }
"proof.monotone_sum_pointer_elimination" = { kind = "proof", first_problem = 15, last_problem = 167, occurrences = 3, teaching_state = "familiar", difficult = true, note = "0015–0016 与 0167 都用有序端点界证明：和过小时排除左端，和过大时排除右端；重复值不破坏非严格不等式。" }
"resource.c_fixed_pair_return" = { kind = "resource", first_problem = 1, last_problem = 167, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0001 与 0167 的 C 实现都只在两个 int 成功分配并写入后设置 returnSize=2；失败保持长度 0，成功返回所有权交给调用者。" }
"algorithm.bijective_base26_decrement" = { kind = "algorithm", first_problem = 168, last_problem = 168, occurrences = 1, teaching_state = "introduced", difficult = false, note = "每轮先 current-1，再以 26 取余映射 A..Z，高位商继续编码，最后反转低位优先字符。" }
"complexity.output_space_exclusion" = { kind = "complexity", first_problem = 52, last_problem = 187, occurrences = 36, teaching_state = "mastered", difficult = false, note = "0052–0187 持续区分工作状态、适配器与返回载荷；0187 另报 1 MiB 固定状态表、R 输入物化、Theta(R) 结果和托管子串存储风险。" }
"invariant.column_title_place_value" = { kind = "invariant", first_problem = 168, last_problem = 168, occurrences = 1, teaching_state = "introduced", difficult = false, note = "N=current*26^k+sum(x_i*26^i)，其中已生成 x_i 属于 1..26；每轮由 current=26q+x_k 保持。" }
"numeric.column_title_32bit_length" = { kind = "numeric", first_problem = 168, last_problem = 168, occurrences = 1, teaching_state = "introduced", difficult = true, note = "2^31-1 对应 FXSHRXW，当前输入域标题最多 7 位；C/C#/R 固定缓冲容量由该上界支撑。" }
"proof.bijective_base26_uniqueness" = { kind = "proof", first_problem = 168, last_problem = 168, occurrences = 1, teaching_state = "introduced", difficult = true, note = "(current-1) 的欧几里得商余唯一决定最低位与剩余高位，递归应用推出标题表示唯一。" }
"algorithm.boyer_moore_majority_vote" = { kind = "algorithm", first_problem = 169, last_problem = 169, occurrences = 1, teaching_state = "introduced", difficult = false, note = "用 candidate 与 balance 压缩异值成对抵消；题目保证严格多数存在时最终候选可直接返回。" }
"contract.majority_verification_precondition" = { kind = "contract", first_problem = 169, last_problem = 169, occurrences = 1, teaching_state = "introduced", difficult = false, note = "官方保证多数存在，故省略第二遍；扩展到任意输入时必须验证最终候选频次。" }
"invariant.majority_unpaired_partition" = { kind = "invariant", first_problem = 169, last_problem = 169, occurrences = 1, teaching_state = "introduced", difficult = false, note = "任意已处理前缀可划分为若干异值对和 balance 个 candidate；balance 不是候选真实频次。" }
"proof.strict_majority_survives_cancellation" = { kind = "proof", first_problem = 169, last_problem = 169, occurrences = 1, teaching_state = "introduced", difficult = true, note = "删除任意异值对后 2m>n 的严格多数关系保持，因此真正多数不可能被全部抵消。" }
"algorithm.bijective_base26_decode" = { kind = "algorithm", first_problem = 171, last_problem = 171, occurrences = 1, teaching_state = "introduced", difficult = false, note = "按 A=1..Z=26 从左到右执行 answer=answer*26+digit；解码侧不使用 0168 的先减一。" }
"invariant.column_number_prefix_fold" = { kind = "invariant", first_problem = 171, last_problem = 171, occurrences = 1, teaching_state = "introduced", difficult = false, note = "处理前 k 位后累计值恰等于该标题前缀的位权和，读下一位时整体乘 26 再加数位。" }
"proof.column_title_encode_decode_inverse" = { kind = "proof", first_problem = 171, last_problem = 171, occurrences = 1, teaching_state = "introduced", difficult = true, note = "0168 的 current=26q+digit 分解与 0171 的 value=26value+digit 逐层互逆。" }
"algorithm.factorial_trailing_zero_layer_sum" = { kind = "algorithm", first_problem = 172, last_problem = 172, occurrences = 1, teaching_state = "introduced", difficult = false, note = "v5(n!)=sum floor(n/5^k)，反复把 current 除以 5 并累加，避免计算阶乘或显式幂。" }
"complexity.logarithmic_integer_division_loop" = { kind = "complexity", first_problem = 172, last_problem = 172, occurrences = 1, teaching_state = "introduced", difficult = true, note = "状态每轮除以 5，时间 O(log_5 n)、空间 O(1)，没有阶乘中间值或字符串输出。" }
"invariant.factor_five_quotient_sum" = { kind = "invariant", first_problem = 172, last_problem = 172, occurrences = 1, teaching_state = "introduced", difficult = false, note = "t 轮后 current=floor(n/5^t)，answer 为前 t 层 floor(n/5^k) 之和。" }
"proof.prime_factor_layer_counting" = { kind = "proof", first_problem = 172, last_problem = 172, occurrences = 1, teaching_state = "introduced", difficult = true, note = "5^k 的倍数恰贡献第 k 个因子 5；分层计数使每个整数按其 5-adic 指数被统计恰当次数。" }
"algorithm.binary_tree_inorder_explicit_stack" = { kind = "algorithm", first_problem = 94, last_problem = 173, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0094 用显式栈一次性模拟左根右控制流；0173 把同一左链展开保存为跨 next 调用的惰性对象状态。" }
"proof.inorder_stack_continuation_invariant" = { kind = "proof", first_problem = 94, last_problem = 173, occurrences = 2, teaching_state = "reinforcing", difficult = true, note = "0094 与 0173 的栈都保存左递归完成后尚待 visit 的祖先续点；0173 进一步用该不变量证明栈顶是下一最小节点及 hasNext 等价性。" }
"interface.binary_tree_node_reference_model" = { kind = "interface", first_problem = 94, last_problem = 173, occurrences = 21, teaching_state = "mastered", difficult = false, note = "0094 建立跨语言 TreeNode 引用模型，0101–0116 持续强化；0173 的迭代器保存节点引用且要求树在对象生命周期内不变。" }
"ownership.read_only_tree_reference_stack" = { kind = "ownership", first_problem = 144, last_problem = 173, occurrences = 2, teaching_state = "reinforcing", difficult = true, note = "0144 与 0173 的显式栈都只保存并读取树节点引用；Rust Rc 克隆不复制子树，C/C++ 栈不拥有节点。" }
"algorithm.lazy_bst_inorder_iterator" = { kind = "algorithm", first_problem = 173, last_problem = 173, occurrences = 1, teaching_state = "introduced", difficult = false, note = "构造与每次 next 只展开当前子树最左路径，显式栈保存递归中序续点，不预先物化完整序列。" }
"complexity.bst_iterator_amortized_next" = { kind = "complexity", first_problem = 173, last_problem = 173, occurrences = 1, teaching_state = "introduced", difficult = true, note = "单次 next 最坏沿左链 O(h)，全遍历每节点入栈出栈各一次，总 O(n)，故均摊 O(1)，峰值空间 O(h)。" }
"proof.bst_iterator_no_skip_no_duplicate" = { kind = "proof", first_problem = 173, last_problem = 173, occurrences = 1, teaching_state = "introduced", difficult = true, note = "根和每个父节点右子树各形成唯一入口，树节点最多压栈一次并由一次 next 弹出，BST 中序保证升序。" }
"algorithm.reverse_minimum_health_dp" = { kind = "algorithm", first_problem = 174, last_problem = 174, occurrences = 1, teaching_state = "introduced", difficult = false, note = "need(i,j) 定义为进入格前完成剩余路径的最小生命，逆向转移 max(1,min(down,right)-cell)。" }
"boundary.single_virtual_princess_successor" = { kind = "boundary", first_problem = 174, last_problem = 174, occurrences = 1, teaching_state = "introduced", difficult = false, note = "仅公主格下方设置需求 1 的虚拟后继，其他边界为 INF，防止路径从普通边界逃出网格。" }
"invariant.dungeon_rolling_row" = { kind = "invariant", first_problem = 174, last_problem = 174, occurrences = 1, teaching_state = "introduced", difficult = false, note = "右向左更新前 dp[j] 是下方状态，dp[j+1] 已是当前行右侧状态，更新后 dp[j] 成为当前格状态。" }
"algorithm.reverse_rolling_update" = { kind = "concept", first_problem = 115, last_problem = 174, occurrences = 3, teaching_state = "familiar", difficult = false, note = "0115 与 0119 逆序覆盖以保护上一轮左邻状态；0174 从右向左更新，使 dp[j] 保留旧下方而 dp[j+1] 已是新右侧状态。" }
"language.julia_r_descending_iteration" = { kind = "language_boundary", first_problem = 115, last_problem = 174, occurrences = 4, teaching_state = "familiar", difficult = false, note = "0115–0120 与 0174 的 Julia/R 逆序 DP 都使用显式递减范围；0174 另核对一基矩阵端点与 R 的 by=-1L。" }
"technique.infinity_sentinel" = { kind = "algorithm", first_problem = 4, last_problem = 174, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0004 用无穷统一数组分割边界；0174 用正无穷标记不可达后继，并证明每格减法前必选到有限需求。" }
"numeric.path_sum_widen_before_subtract" = { kind = "numeric", first_problem = 112, last_problem = 174, occurrences = 3, teaching_state = "familiar", difficult = true, note = "0112–0113 在扣除节点值前扩宽；0174 在 64 位需求中扣除格值，并以答案上界 399001 支撑最终平台整数转换。" }
"proof.minimum_health_necessity_sufficiency" = { kind = "proof", first_problem = 174, last_problem = 174, occurrences = 1, teaching_state = "introduced", difficult = true, note = "低于状态值时无法满足生命下界或任一后继需求；等于状态值并选择较小需求后继时可由归纳策略存活。" }
"domain.database_problem_registration" = { kind = "domain_policy", first_problem = 175, last_problem = 185, occurrences = 10, teaching_state = "introduced", difficult = false, note = "Database 题不符合固定十语言算法产物合同，只登记 database_skipped，不创建空壳 RST。" }
"domain.database_skipped" = { kind = "domain_policy", first_problem = 175, last_problem = 197, occurrences = 12, teaching_state = "introduced", difficult = false, note = "新增 0196–0197 Database 登记，不生成固定十语言算法 RST。" }
"algorithm.largest_number_concat_sort" = { kind = "algorithm", first_problem = 179, last_problem = 179, occurrences = 1, teaching_state = "introduced", difficult = false, note = "数字转十进制字符串后，以 ab>ba 决定 a 在 b 前，排序并拼接；全零结果归一化为单个 0。" }
"boundary.largest_number_all_zero_normalization" = { kind = "boundary", first_problem = 179, last_problem = 179, occurrences = 1, teaching_state = "introduced", difficult = false, note = "排序后首项为 0 时所有输入均为 0，规范返回单个字符串 0。" }
"implementation.virtual_concatenation_compare" = { kind = "implementation", first_problem = 179, last_problem = 179, occurrences = 1, teaching_state = "introduced", difficult = false, note = "比较器按 m+n 个虚拟字符扫描 a+b 与 b+a，不分配临时拼接串；C qsort 比较器无失败路径。" }
"proof.concat_comparator_strict_weak_order" = { kind = "proof", first_problem = 179, last_problem = 179, occurrences = 1, teaching_state = "introduced", difficult = true, note = "ab 与 ba 的顺序等价于无限周期串 a^ω 与 b^ω 的字典序，继承传递性；ab=ba 的等价项任意顺序不改变结果。" }
"proof.largest_number_adjacent_exchange" = { kind = "proof", first_problem = 179, last_problem = 179, occurrences = 1, teaching_state = "introduced", difficult = true, note = "相邻块 ab 与 ba 具有相同外部前后缀，交换逆序对会增大完整拼接；任意排列可通过相邻交换转为排序排列。" }
"adapter.r_fixed_decimal_integer_format" = { kind = "language_adapter", first_problem = 179, last_problem = 179, occurrences = 1, teaching_state = "introduced", difficult = true, note = "R 用 sprintf(\"%.0f\", value) 把 0..10^9 精确整数固定写成纯十进制串，避免 as.character 的科学计数法破坏比较字符域。" }
"interface.c_heap_owned_string_result" = { kind = "interface", first_problem = 43, last_problem = 179, occurrences = 3, teaching_state = "familiar", difficult = false, note = "0043、0049 与 0179 都返回调用者负责释放的 C 堆字符串；0179 还事务式清理数字中间串并为答案保留终止符。" }
"adapter.go_repeated_dna_clone" = { kind = "language_adapter", first_problem = 187, last_problem = 187, occurrences = 1, teaching_state = "introduced", difficult = false, note = "Go 使用 strings.Clone 复制 10 字符结果，避免切片长期保留完整输入底层存储。" }
"algorithm.dna_two_bit_rolling_window" = { kind = "algorithm", first_problem = 187, last_problem = 187, occurrences = 1, teaching_state = "introduced", difficult = false, note = "A/C/G/T 映射为 0..3，每轮左移 2 位并用 20 位掩码保留最近 10 个字符。" }
"invariant.dna_fixed_window_code" = { kind = "invariant", first_problem = 187, last_problem = 187, occurrences = 1, teaching_state = "introduced", difficult = false, note = "处理下标 i 后 code 是最近 min(i+1,10) 个 DNA 字符的定长四进制编码；长度 10 时与窗口一一对应。" }
"c.transactional_ip_string_results" = { kind = "language_boundary", first_problem = 93, last_problem = 187, occurrences = 2, teaching_state = "reinforcing", difficult = false, note = "0093 与 0187 的 C 返回值都由 char** 和独立堆字符串组成，按成功数量清理；0187 还事务式释放动态外层数组与固定状态表。" }
"state.dna_second_occurrence_emission" = { kind = "state", first_problem = 187, last_problem = 187, occurrences = 1, teaching_state = "introduced", difficult = false, note = "2^20 状态表使用 0/1/2 表示未见、见一次、已输出，只在第二次出现时追加结果。" }
"algorithm.stock_k_transactions_exact_state" = { kind = "algorithm", first_problem = 188, last_problem = 188, occurrences = 1, teaching_state = "introduced", difficult = false, note = "cash[t]/hold[t] 分别表示恰好完成 t 次卖出后的空仓/持仓最大现金；买入保持 t，卖出把 t-1 推进到 t。" }
"boundary.stock_unlimited_transaction_degeneracy" = { kind = "boundary", first_problem = 188, last_problem = 188, occurrences = 1, teaching_state = "introduced", difficult = false, note = "长度 n 最多完成 floor(n/2) 笔交易；k 达到该上限时约束失效，转为累加全部正相邻差。" }
"invariant.stock_exact_cash_hold_prefix" = { kind = "invariant", first_problem = 188, last_problem = 188, occurrences = 1, teaching_state = "introduced", difficult = false, note = "每个日期前缀后，cash/hold 覆盖对应精确卖出次数和持仓状态的全部合法策略，并保留最大现金。" }
"numeric.stock_safe_negative_infinity" = { kind = "numeric", first_problem = 188, last_problem = 188, occurrences = 1, teaching_state = "introduced", difficult = true, note = "不可达状态使用远离类型下界的安全负无穷，固定宽语言只在来源可达时执行价格加减，避免最小整数溢出。" }
"proof.stock_descending_inplace_update" = { kind = "proof", first_problem = 188, last_problem = 188, occurrences = 1, teaching_state = "introduced", difficult = true, note = "交易数递减时 hold[t] 读取未改写的旧 cash[t]，cash[t] 读取尚未轮到的旧 hold[t-1]，等价于前一日双数组。" }
"proof.stock_positive_difference_optimality" = { kind = "proof", first_problem = 188, last_problem = 188, occurrences = 1, teaching_state = "introduced", difficult = true, note = "任意买卖区间利润是相邻差之和，不超过其中正差之和；逐个执行正差的一日交易达到该上界。" }
"resource.c_stock_state_allocation" = { kind = "resource", first_problem = 188, last_problem = 188, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 分配 cash 与 hold 两组 64 位状态，部分失败释放已分配对象；-1 作为合法非负答案域外的资源失败标记。" }
"adapter.array_mutation_visibility" = { kind = "language_adapter", first_problem = 189, last_problem = 189, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C 至 Julia 修改调用者可见数组；R 普通向量局部绑定需返回结果，不能宣称调用者绑定原地改变。" }
"algorithm.rotate_array_three_reversals" = { kind = "algorithm", first_problem = 189, last_problem = 189, occurrences = 1, teaching_state = "introduced", difficult = false, note = "把原数组分成 A 与末尾 B，利用 rev(A+B)=rev(B)+rev(A)，再分别恢复 B 与 A 得到右旋。" }
"invariant.reverse_closed_interval" = { kind = "invariant", first_problem = 189, last_problem = 189, occurrences = 1, teaching_state = "introduced", difficult = false, note = "闭区间反转中外侧对称位置已就位、未处理元素保持在 left..right，循环内缩直至完成。" }
"algorithm.fixed_width_bit_reversal" = { kind = "algorithm", first_problem = 190, last_problem = 190, occurrences = 1, teaching_state = "introduced", difficult = false, note = "固定执行 32 轮，每轮把输入最低位送入左移后的答案，前导零也占据反转位置。" }
"invariant.reverse_bits_prefix" = { kind = "invariant", first_problem = 190, last_problem = 190, occurrences = 1, teaching_state = "introduced", difficult = false, note = "t 轮后答案低 t 位从高到低是原输入 b0..b(t-1)，输入为原位模式逻辑右移 t 位。" }
"adapter.unsigned_32_bit_cross_language" = { kind = "language_adapter", first_problem = 190, last_problem = 191, occurrences = 2, teaching_state = "introduced", difficult = false, note = "Java 用 32 位补码与逻辑右移，TypeScript 每轮 >>>0，R 用精确 double 的除二取余适配完整无符号域。" }
"algorithm.kernighan_clear_lowest_one" = { kind = "algorithm", first_problem = 191, last_problem = 191, occurrences = 1, teaching_state = "introduced", difficult = false, note = "n&(n-1) 每轮恰好清除最低有效 1，循环次数等于汉明重量。" }
"proof.lowest_set_bit_elimination" = { kind = "proof", first_problem = 191, last_problem = 191, occurrences = 1, teaching_state = "introduced", difficult = true, note = "n-1 只翻转最低 1 及其低位；按位与保留高位并清除该最低 1。" }
"domain.shell_skipped" = { kind = "domain_policy", first_problem = 192, last_problem = 195, occurrences = 4, teaching_state = "introduced", difficult = false, note = "0192–0195 属于 Shell 文本处理域，只登记状态，不生成固定十语言算法空壳。" }
"algorithm.house_robber_prefix_dp" = { kind = "algorithm", first_problem = 198, last_problem = 198, occurrences = 1, teaching_state = "introduced", difficult = false, note = "F(i) 表示前 i 间房屋最大金额，递推 max(F(i-1),F(i-2)+nums[i-1])。" }
"boundary.house_robber_empty_strategy" = { kind = "boundary", first_problem = 198, last_problem = 198, occurrences = 1, teaching_state = "introduced", difficult = false, note = "金额非负使空方案提供 0 下界；实现额外兼容空数组、单房、双房和全零。" }
"invariant.house_robber_rolling_pair" = { kind = "invariant", first_problem = 198, last_problem = 198, occurrences = 1, teaching_state = "introduced", difficult = false, note = "处理前 i 间后 previous_one=F(i)、previous_two=F(i-1)，先算 current 再整体推进避免覆盖旧依赖。" }
"numeric.house_robber_wide_accumulator" = { kind = "numeric", first_problem = 198, last_problem = 198, occurrences = 1, teaching_state = "introduced", difficult = true, note = "固定宽语言用 64 位中间状态，最终按官方约束转换到平台返回类型。" }
"proof.house_robber_last_house_partition" = { kind = "proof", first_problem = 198, last_problem = 198, occurrences = 1, teaching_state = "introduced", difficult = true, note = "所有方案按最后一间不选或选择完备且互斥；选择时必须连接到 i-2 前缀。" }
"state.house_robber_prefix_optimum" = { kind = "state", first_problem = 198, last_problem = 198, occurrences = 1, teaching_state = "introduced", difficult = false, note = "状态只保留前缀最优金额，具体选择路径在相同前缀下被更大金额支配。" }
"adapter.circular_node_queue" = { kind = "language_adapter", first_problem = 199, last_problem = 199, occurrences = 1, teaching_state = "introduced", difficult = false, note = "C/Go/TypeScript/Julia 使用循环队列保持活动存储与树宽同阶；R 用环境槽位并在弹出后删除。" }
"algorithm.level_bfs_right_view" = { kind = "algorithm", first_problem = 199, last_problem = 199, occurrences = 1, teaching_state = "introduced", difficult = false, note = "冻结当前层大小，按左后右从左到右处理，本层最后出队节点即右侧可见节点。" }
"proof.rightmost_node_visibility" = { kind = "proof", first_problem = 199, last_problem = 199, occurrences = 1, teaching_state = "introduced", difficult = true, note = "同层队列按从左到右排列，最后节点是最靠右非空节点，每层恰好提交一个值。" }
"adapter.mutable_grid_cross_language" = { kind = "language_adapter", first_problem = 200, last_problem = 200, occurrences = 1, teaching_state = "introduced", difficult = false, note = "多数语言修改调用者可见网格；Rust 消费拥有值，R 局部矩阵修改可能写时复制，需分别报告。" }
"algorithm.grid_component_flood_fill" = { kind = "algorithm", first_problem = 200, last_problem = 200, occurrences = 1, teaching_state = "introduced", difficult = false, note = "外层扫描首次发现陆地时计数一次，并用迭代栈清除整个四邻接连通分量。" }
"proof.component_discovery_bijection" = { kind = "proof", first_problem = 200, last_problem = 200, occurrences = 1, teaching_state = "introduced", difficult = true, note = "每个岛屿的最早扫描陆地恰好触发一次计数，洪泛完整清除该分量，后续同岛格不再计数。" }
