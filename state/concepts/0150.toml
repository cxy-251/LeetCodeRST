[meta]
version = 1
range = "0150"
last_problem = 150
inherits = "state/concepts/0149.toml"

[updates]
"workflow.targeted_example_validation" = "0134 引入针对性样例验证；0150 继续执行，累计强化 17 次。"

[concepts]
"algorithm.reverse_polish_stack_evaluation" = "kind=algorithm; introduced=0150; last=0150; occurrences=1; teaching_state=explained; note=数字入栈，运算符按右值后左值的顺序弹出两个操作数并把结果压回。"
"semantics.integer_division_toward_zero" = "kind=language_semantics; introduced=0150; last=0150; occurrences=1; teaching_state=explained; note=跨语言统一向零截断；Python 与 R 不能直接使用向负无穷取整的整除结果。"
"invariant.rpn_stack_completed_subexpressions" = "kind=invariant; introduced=0150; last=0150; occurrences=1; teaching_state=explained; note=处理任意前缀后，栈保存已完成且尚未被后续运算符消费的子表达式值。"
