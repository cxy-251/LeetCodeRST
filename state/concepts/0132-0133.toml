[meta]
version = 1
range = "0132-0133"
last_problem = 133
inherits = "state/concepts/0129-0131.toml"

[updates]
"algorithm.palindrome_interval_dp" = "0131 用回文区间表支持枚举全部分割；0132 复用同一状态计算最少切割，累计强化 2 次。"

[concepts]
"algorithm.palindrome_min_cut_prefix_dp" = "kind=algorithm; introduced=0132; last=0132; occurrences=1; teaching_state=explained; note=以最后一个回文片段为分界，将最少切割转化为前缀最优值转移。"
"proof.last_palindrome_segment_decomposition" = "kind=proof; introduced=0132; last=0132; occurrences=1; teaching_state=explained; note=任意分割都有唯一最后片段；枚举其起点并组合最优前缀即可覆盖全局最优解。"
"boundary.cut_count_not_partition_count" = "kind=boundary; introduced=0132; last=0132; occurrences=1; teaching_state=explained; note=k 个回文片段只需要 k-1 刀；整串回文答案为 0。"
"algorithm.graph_clone_memo_before_recursion" = "kind=algorithm; introduced=0133; last=0133; occurrences=1; teaching_state=explained; note=创建空副本后立即登记，再递归邻居，才能在有环图中终止并复用同一副本。"
"proof.graph_clone_identity_bijection" = "kind=proof; introduced=0133; last=0133; occurrences=1; teaching_state=explained; note=原节点身份到唯一副本的映射建立节点双射，并逐邻接项复制全部边。"
"boundary.graph_identity_not_business_value" = "kind=boundary; introduced=0133; last=0133; occurrences=1; teaching_state=explained; note=通用克隆映射应以对象身份为键；C 与 R 仅因题目保证 val 唯一才使用值索引适配。"
