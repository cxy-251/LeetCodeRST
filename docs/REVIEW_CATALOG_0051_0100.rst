0051–0100 第二轮审查资料目录
===========================

用途
----

本文件记录 ``0051`` 至 ``0100`` 第二轮逐题审查的覆盖进度、证据位置和规则归并方式。审查读取每题
完整 RST、证明、复杂度和十语言代码，默认不修改历史题目正文。

当前覆盖
--------

已完成：

* ``0051–0055``：``state/reviews/0051-0055.toml`` 与
  ``docs/review-findings/0051-0055.rst``；
* ``0056–0060``：``state/reviews/0056-0060.toml`` 与
  ``docs/review-findings/0056-0060.rst``；
* ``0061–0065``：``state/reviews/0061-0065.toml`` 与
  ``docs/review-findings/0061-0065.rst``；
* ``0066–0070``：``state/reviews/0066-0070.toml`` 与
  ``docs/review-findings/0066-0070.rst``；
* ``0071–0075``：``state/reviews/0071-0075.toml`` 与
  ``docs/review-findings/0071-0075.rst``；
* ``0076–0080``：``state/reviews/0076-0080.toml`` 与
  ``docs/review-findings/0076-0080.rst``；
* ``0081–0085``：``state/reviews/0081-0085.toml`` 与
  ``docs/review-findings/0081-0085.rst``。

下一批：

* ``0086–0090``。

覆盖状态和事实更正以 ``state/REVIEW_INDEX.toml`` 为准。

资料分层
--------

被审查正文
~~~~~~~~~~

``problems/0001-0100/0051`` 至 ``0100`` 是本轮审查对象。发现确定性问题时先写入审查证据和
``REVIEW_INDEX`` 更正；只有用户明确指定修复某题，才修改该题正文。

结构化记录
~~~~~~~~~~

``state/reviews/0051-0055.toml``、``state/reviews/0056-0060.toml``、
``state/reviews/0061-0065.toml``、``state/reviews/0066-0070.toml``、
``state/reviews/0071-0075.toml``、``state/reviews/0076-0080.toml``、
``state/reviews/0081-0085.toml`` 及后续五题批次保存：

* 每题正向样本；
* 确定性问题与验证缺口；
* 可复用规则编号；
* 批次范围和下一步。

详细证据
~~~~~~~~

``docs/review-findings/0051-0055.rst``、``docs/review-findings/0056-0060.rst``、
``docs/review-findings/0061-0065.rst``、``docs/review-findings/0066-0070.rst``、
``docs/review-findings/0071-0075.rst``、``docs/review-findings/0076-0080.rst``、
``docs/review-findings/0081-0085.rst`` 及后续批次保存完整文字分析、代码语义依据和跨题归纳。

规则归并
~~~~~~~~

第二轮批次中的 ``rules`` 是带来源的已提升候选。全部 ``0051–0100`` 审查完成后，再把稳定规则整理为
下一阶段的完整前向执行规则；在完成归并前，不启动 ``0101`` 之后的题目生成。

当前事实更正
------------

R 递归闭包状态
~~~~~~~~~~~~~~

普通 ``x[i] <- value`` 复杂赋值会把结果绑定到当前函数调用帧。递归子调用的 enclosure 是函数定义
环境，不是调用者帧；需要共享并撤销状态时，使用显式参数、``environment`` 或经审计的 ``<<-``。

R ``seq.int`` 方向
~~~~~~~~~~~~~~~~~~

``seq.int(from, to, by)`` 在 ``to-from`` 与 ``by`` 符号相反时会报错。构造反向范围前必须验证端点
顺序，不能依赖 C 风格循环条件把非法范围变成零次迭代。

Julia ``codeunits`` 成本
~~~~~~~~~~~~~~~~~~~~~~~~

``codeunits(s)`` 默认返回轻量 ``CodeUnits`` 包装视图，不物化长度 ``n`` 的副本。Julia 扫描该视图的
辅助空间是 ``O(1)``；R ``utf8ToInt(s)`` 仍会物化 ``O(n)`` 码点向量。

R 路径栈累计复制
~~~~~~~~~~~~~~~~

``0071`` 的 R 实现反复执行 ``stack <- c(stack, segment)`` 和负下标删除。每次都会重建当前字符向量，
因此大量短组件或交替入栈出栈时，累计复制最坏为平方级，不能沿用通用 ``O(n)`` 时间结论。

编辑距离适配器空间
~~~~~~~~~~~~~~~~~~

``0072`` 的一行 DP 核心工作数组是 ``O(min(m,n))``。C++ 按值复制两个输入字符串，Julia
``collect(codeunits(...))`` 与 R ``utf8ToInt`` 物化两个输入数组，使这些适配器的额外和峰值空间达到
``O(m+n)``。

最小窗口 Go 子串存储
~~~~~~~~~~~~~~~~~~~~

``0076`` 的 Go 实现直接返回 ``s[start:end]``。该值可以共享原字符串的底层存储，并可能保留完整输入；
只有显式 ``strings.Clone`` 才保证新分配，因此不能统一写成复制 ``O(answer_length)``。

最小窗口 R 输入物化
~~~~~~~~~~~~~~~~~~~

``0076`` 的 R 实现分别对 ``s`` 与 ``t`` 调用 ``charToRaw``，随后再转换为整数向量。固定 128 项
``need`` 只说明滑动窗口核心状态为常数；适配器额外和峰值空间为 ``O(|s|+|t|)``。

本批稳定模式
------------

* 重复值降低比较信息时区分通常和最坏复杂度；
* 链表断链、所有权转移和节点回收责任分别说明；
* 连续值段先定义保留数量，再设计前驱推进；
* 单调栈弹栈提供左右阻断边界证书；
* 虚拟哨兵只触发结算，不越界访问或修改输入；
* 高维降维证明编码不变量、完整覆盖和反向合法性；
* 峰值工作空间与每轮重新分配分别报告。

规则优先级
----------

#. ``state/PROGRESS.toml``：当前阶段和下一批；
#. ``state/REVIEW_INDEX.toml``：覆盖与事实更正；
#. 已完成阶段的完整前向规则；
#. 专项策略和模板；
#. 本轮结构化记录与详细证据；
#. 提交说明、PR 描述和对话报告。

执行方式
--------

审查由用户在当前对话中明确触发。每个五题批次完成内容读取、证据整理、状态更新和一致性检查后，
作为一个原子提交非强制快进到 ``main``。
