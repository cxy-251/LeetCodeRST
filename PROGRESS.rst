下一次接续任务
============

长期目标
--------

从第 ``0001`` 题开始，按题号顺序整理仓库中的全部 LeetCode 题目。当前阶段只修改每个题目 RST 的前三个小节：

#. ``题目信息``；
#. ``题目重述``；
#. ``自建示例``。

每道公开题必须先阅读 LeetCode 当前原题，逐项确认输入、输出、限制、保证条件和特殊语义，再与仓库内容对照。每题原则上建立 ``2`` 至 ``3`` 个独立设计的示例：至少一个体现核心机制，至少一个明确覆盖原题允许的边界、空结果、无解、重复值、并列选择、最小规模、对象初始状态或特殊返回值。不得复制或机械改写官方示例、说明和提示；所有输出和解释必须独立复算。``C++ 实现`` 及其后的全部内容保持原样。Premium、Database、Shell 状态页不凭记忆补写。禁止新建任何文件或目录。

可信完成范围
------------

``0700`` 至 ``1000`` 已按上述标准重新逐题审核完成：

* ``0700`` 至 ``0900`` 的严格复核结果继续有效；
* ``0901`` 至 ``1000`` 已按题号逐题打开仓库 RST，并实际对照 LeetCode 当前原题核对输入输出、参数含义、下标、约束、顺序、唯一性、对象状态、无解条件和特殊返回语义；本范围没有 Premium 状态页；
* 本批 ``100`` 道题原文件均只有一个自建示例，现已全部补足为 ``2`` 个独立示例，并保证至少一个明确体现边界、空结果、无解、最小规模、并列规则或对象初始状态；所有输出和解释均已独立复算；
* 已替换直接复用或高度同构官方示例的题目，包括 ``0924``、``0928``、``0932``、``0935``、``0940``、``0955``、``0958``、``0963``、``0968``、``0970`` 至 ``0972``、``0975``、``0991``、``0995``、``0997`` 等；
* 主要契约修正包括：``0911`` 补充候选人编号范围；``0912`` 恢复禁止内置排序、``O(n log n)`` 时间及尽量少用额外空间的要求；``0913`` 明确完整游戏状态重复时判平局；``0916`` 补充两个输入数组内部单词互不重复；``0946`` 补充元素范围；``0975`` 将数组最小长度从错误的 ``2`` 修正为 ``1``；``0984`` 补充 ``1 <= a + b``；``0999`` 补充固定 ``8 x 8`` 尺寸和合法字符约束；
* 本批实际修改 ``0901`` 至 ``1000`` 的全部 ``100`` 个题目文件以及本进度文件；没有创建、删除或重命名文件，没有修改任何题目的 ``C++ 实现`` 及其后内容，也没有修改 ``1001``；
* ``1001`` 及其后的既有内容尚未纳入本次可信复核范围，不能视为已确认。

规则同步状态
------------

``AGENTS.md`` 已经包含公开算法题至少两个原创示例、至少一个覆盖边界或特殊语义、禁止复制或机械改写官方示例等要求，本批核对后保持原样；本文件已同步记录真实完成范围。

当前目标
--------

下一入口为 ``1001. Grid Illumination``。

* 目标文件：``1001-1100/1001-grid-illumination.rst``；
* 当前状态：文件存在，但尚未纳入严格复核，不得把现有内容视为已确认；
* 重点核对：重复灯坐标只算一盏灯；每盏灯照亮同行、同列和两条对角线；每次查询必须先记录照明状态，再关闭查询格及周围八格内的灯；
* 开始前必须实际读取 LeetCode ``1001`` 当前原题，并只修改前三节；
* ``0700`` 至 ``1000`` 无需为了制造提交再次改写，除非后续发现可复现的新问题。