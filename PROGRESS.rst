下一次接续任务
============

当前目标
--------

处理 ``0004. Median of Two Sorted Arrays``。

* 目标文件：``0001-0100/0004-median-of-two-sorted-arrays.rst``；
* 文件类型：公开算法题完整题解；
* 当前状态：目标文件已存在，执行全文审查与整改；
* 完成后下一入口：``0005. Longest Palindromic Substring``。

本轮工作
--------

#. 读取 ``AGENTS.md`` 和第四题全文；
#. 核对两个有序数组、允许单侧为空、总长度非零、返回中位数和目标复杂度；
#. 设计能够展示奇偶总长度、空数组、分割位于端点和跨数组取边界值的自建示例；
#. 在 C++ 代码块中实现完整合并、双指针走到中位位置和较短数组二分分割三种代表性解法，并由标准入口调用二分主解法；
#. 从合并后中间位置推导左右两部分的元素数量，再建立两个数组分割位置 ``i``、``j`` 的约束关系；
#. 说明为什么始终在较短数组上二分，并推导 ``j = (m + n + 1) / 2 - i`` 的边界范围；
#. 使用跟踪表展示 ``low``、``high``、``i``、``j`` 和四个分割边界值的变化；
#. 说明 ``a_left > b_right`` 时为什么必须减小 ``i``，``b_left > a_right`` 时为什么必须增大 ``i``；
#. 使用具体机制标题解释合法分割为何完整划分合并序列，以及奇偶总长度时如何由边界值确定中位数；
#. 说明空侧分割使用哨兵值的含义、整数求和转浮点时的溢出处理和各语言索引适配；
#. 从合并元素数、双指针步数和二分区间长度推导三种方法的实际时间与空间复杂度；
#. 用 C、Python、Java、Rust、Go、TypeScript、C#、Julia 和 R 实现选定主解法；
#. 在关键代码位置使用简洁的中文注释，并让题解正文在 R 实现结束。

本轮完成条件
------------

#. 第四题完整呈现“合并中位数 → 左右分割 → 分割数量约束 → 二分方向 → 合法边界 → 奇偶结果 → 复杂度”的推导链；
#. C++、题解正文和九语言实现使用一致的分割位置、哨兵语义和二分边界；
#. 完整重写本文件，使下一轮直接接续 ``0005. Longest Palindromic Substring``；
#. 将第四题与新的 ``PROGRESS.rst`` 组成一个原子提交。
