下一次接续任务
============

当前目标
--------

处理 ``0010. Regular Expression Matching``。

* 目标文件：``0001-0100/0010-regular-expression-matching.rst``；
* 文件类型：公开算法题完整题解；
* 当前状态：目标文件已存在，执行全文审查与整改；
* 完成后下一入口：``0011. Container With Most Water``。

本轮工作
--------

#. 读取 ``AGENTS.md`` 和第十题全文；
#. 核对完整匹配语义、普通字符、点号 ``.``、星号 ``*``、空字符串、空模式以及模式合法性保证；
#. 设计能够展示普通字符失败、点号单字符匹配、星号匹配零次、一次、多次和完整匹配失败的自建示例；
#. 在 C++ 代码块中实现直接递归、记忆化后缀搜索和自底向上动态规划三种代表性解法，并由标准入口调用动态规划主解法；
#. 定义 ``match(i, j)`` 或 ``dp[i][j]`` 表示字符串后缀 ``s[i:]`` 与模式后缀 ``p[j:]`` 能否完整匹配，并说明边界状态；
#. 从模式当前位置是否带星号推导两类转移：跳过 ``element*`` 表示出现零次，消费一个匹配字符并停留在当前模式位置表示继续重复；
#. 解释普通字符和点号分支为什么必须同时消费一个字符串字符与一个模式元素；
#. 使用递归树局部展开或 DP 小表展示 ``s = "aab"``、``p = "c*a*b"`` 中零次分支、多次分支和状态复用；
#. 使用具体机制标题解释星号两条分支为何覆盖全部重复次数、记忆化为何消除重复后缀、表格填充顺序为何满足依赖以及最终状态为何代表完整匹配；
#. 从字符串长度、模式长度、状态数量、递归分支和每状态常数工作推导三种方法的实际时间与空间复杂度；
#. 用 C、Python、Java、Rust、Go、TypeScript、C#、Julia 和 R 实现动态规划主解法，并统一后缀状态、边界初始化和星号转移；
#. 在关键代码位置使用简洁的中文注释，并让题解正文在 R 实现结束。

本轮完成条件
------------

#. 第十题完整呈现“完整匹配契约 → 后缀状态 → 普通字符与点号 → 星号零次或多次 → 递归重复状态 → 记忆化 → 动态规划填表 → 状态演化 → 覆盖依据 → 复杂度”的推导链；
#. C++、题解正文和九语言实现使用一致的状态含义、首字符匹配条件、星号分支和边界状态；
#. 完整重写本文件，使下一轮直接接续 ``0011. Container With Most Water``；
#. 将第十题与新的 ``PROGRESS.rst`` 组成一个原子提交。
