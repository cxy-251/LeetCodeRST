下一次接续任务
==============

项目总目标
----------

完成仓库中的全部 ``3999`` 道已有 LeetCode 题目，为每道题整理一份独立、完整、可单独阅读的 C++ 题解，内容包括题目信息与分析、自建示例、C++ 代表性代码、C++ 代码分析、正确性与复杂度。文件中已有的多语言实现保留但不作为后续审核和完成判断的要求。每道题必须只依赖当前原题和当前文件，不得依赖其他题目的正文。

执行边界
--------

只修改已有文件，不新增、删除、重命名或移动文件和目录。每道题从文件开头到第一个多语言实现标题之前都属于审核范围，通常为“九语言实现”；已有“十语言实现”等同义标题时同样适用。按需要修改题目分析、示例、C++ 代码和 C++ 代码分析；该标题及其后已有多语言实现保留但不审核、不修改。Premium、Database、Shell 或题面无法可靠确认的题目保持状态页，不猜测题面和解法。不运行代码测试，只做必要的静态检查。

交付批次
--------

当前批次为 ``0201–0300``。每完成一个 100 题批次并通过静态检查后统一提交并推送一次；批次中途不推送，最后不足 100 道的尾批次按实际已有题目处理。

当前可信范围
------------

已按新范围重新确认 ``0001`` 至 ``0200``；``0101–0200`` 批次已完成静态审核并准备统一推送。

本轮已根据用户最新要求重设执行规则：每题只审核文件开头到第一个多语言实现标题之前的正文，包含题目信息、题目重述、自建示例、C++ 实现和题解；该标题及其后多语言实现全部跳过。历史提交和旧进度中的完成声明不自动计入新的可信范围。只有按新范围重新读取、核对并完成的题目，才可记录为可信完成。

当前目标
--------

从 ``0201. Bitwise AND of Numbers Range`` 开始，按新范围完成 ``0201–0300`` 批次复审。

* 当前批次：``0201–0300``；
* 当前动作：逐题读取文件开头至第一个多语言实现标题之前，核对题意、示例、C++ 实现、正确性和复杂度；
* 当前目标文件：``0201-0300/0201-bitwise-and-of-numbers-range.rst``；
* 第一个多语言实现标题及其后已有多语言实现不作为本轮工作范围。

完成记录
--------

本轮已完成 ``0001``：核对当前题面和约束，补充“数组不保证有序、返回下标顺序不受限”的独立契约说明；现有题目分析、示例、C++ 实现、代码分析和多语言实现已检查，无需运行代码测试。

本轮已完成 ``0002``：核对逆序数位、节点范围、单节点零和无前导零约束；补充数字零的独立边界示例；现有进位状态、最终新增节点、输入节点语义、C++ 实现、代码分析和多语言实现已检查，无需运行代码测试。

本轮已完成 ``0003``：核对连续子串、字符范围、空字符串和多解语义；补充多个等长答案的契约说明，以及包含空格和符号的独立示例；现有滑动窗口状态、位置跳跃、C++ 实现、代码分析和多语言实现已检查，无需运行代码测试。

本轮已完成 ``0004``：核对数组有序性、中位数奇偶定义、单侧空数组、总长度范围和对数时间要求；补充“两个数组不能同时为空”的独立边界说明；现有合并、走到中位和二分分割方案、C++ 实现、代码分析和多语言实现已检查，无需运行代码测试。

本轮已完成 ``0005``：核对回文定义、连续子串、非空输入、字符范围和多种等长答案；补充返回结果至少包含一个字符的契约说明；现有枚举、动态规划、中心扩展、C++ 实现、代码分析和多语言实现已检查，无需运行代码测试。

本轮已完成 ``0006``：核对锯齿路径、周期长度、边界行、单行和行数超过字符数语义；补充输出不包含排版空格、每个输入字符只保留一次的契约说明；现有路径模拟、周期索引、C++ 实现、代码分析和多语言实现已检查，无需运行代码测试。

本轮已完成 ``0007``：核对 32 位输入范围、负号、向零截断、前导零和溢出返回 ``0``；补充零值和 ``INT_MIN`` 反转溢出的独立示例；现有字符串对照、宽整数对照、纯 32 位主解法、C++ 实现、代码分析和多语言实现已检查，无需运行代码测试。

本轮已完成 ``0008``：核对空格、可选符号、连续数字前缀、首个非法字符、空输入和 32 位钳制；补充负向溢出与空字符串的独立示例；现有阶段扫描、有限状态机、C++ 实现、代码分析和多语言实现已检查，无需运行代码测试。

本轮已完成 ``0009``：核对十进制回文、负数、非零末尾零、零值和不使用字符串的半数反转语义；补充零值独立示例；现有字符串对照、完整反转、半数反转、C++ 实现、代码分析和多语言实现已检查，无需运行代码测试。

本轮已完成 ``0010``：核对整串匹配、``.`` 单字符语义、``*`` 对前置元素的零次或多次修饰、输入字符集、长度和模式合法性；补充 ``*`` 不能脱离前置元素独立匹配的契约说明；现有后缀状态、星号两分支、动态规划、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0011``：核对竖线不可倾斜、短板决定高度、宽度决定面积、数组范围和双指针移动不变量；修正“最优边界避开最高柱”的自建示例，使数据与说明一致；现有暴力对照、双指针证明、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0012``：核对输入范围、七种基本符号、六种允许的减法组合、按十进制位转换和规范表示；补充 ``49`` 不能跨位写成 ``IL`` 的独立示例；现有按位查表、降序贪心、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0013``：核对七种符号、六种减法组合、合法输入保证、长度和数值范围；补充非法罗马串不属于本题输入契约的说明；现有相邻比较、从右扫描、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0014``：核对公共前缀必须从首字符连续开始、空前缀、空字符串、单元素数组和字符范围；补充单元素数组直接返回自身的边界说明；现有纵向扫描、横向收缩、排序首尾对照、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0015``：核对三项下标两两不同、和值为零、数值三元组去重、输出顺序不限定和整数范围；修正去重章节标题，使其准确对应首值、左值、右值三处跳过逻辑；现有排序双指针、单调性证明、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0016``：核对三项异下标、绝对距离定义、唯一最近和值、输入范围和精确命中下界；补充返回和值而非下标、三元组或距离的输出契约；现有枚举对照、排序双指针、提前返回、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0017``：核对当前题面的非空数字串约束、``2`` 到 ``9`` 的电话映射、每位恰选一个字母和答案顺序不限定；修正历史空输入约束为当前 ``[1, 4]``，并保留空串防御性说明；补充正式四字母按键示例，并修正 C++ 移动语义所需头文件；现有笛卡尔积、回溯、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0018``：核对四项下标两两不同、目标和值、唯一四元组、长度与数值范围和 64 位求和；修正 C 语言结果数组的固定容量为动态扩容，避免合法输出较多时的缓冲区越界；现有排序双指针、两层去重、范围剪枝、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0019``：核对非空链表、节点数量与节点值范围、``1 <= n <= length``、删除头节点和单节点结果；现有两遍扫描、栈、哨兵双指针、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0020``：核对三类括号字符集、同类型闭合、嵌套顺序、闭括号前置和扫描后栈为空等契约；现有反复删除对照、栈扫描、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0021``：核对两条非递减链表、节点总数范围、节点值范围、空链表、相等值和原节点拼接要求；修正约束为两表节点总数上限，并补充不复制节点的输出契约；现有递归、迭代、复制对照、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0022``：核对恰好 ``n`` 对括号、前缀余额约束、完整闭合、结果去重和 ``n`` 范围；补齐 C++ 全量枚举对照的预分配包装，避免辅助实现从空路径提前结束；现有全量枚举、回溯、动态规划、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0023``：核对 ``k``、单链表长度、总节点数、节点值范围、空数组与空链表；补充结果复用并重连原节点、不复制值节点的输出语义；现有顺序归并、分治、最小堆、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0024``：核对相邻节点成对交换、仅重连节点而不修改值、空链表、单节点和奇数尾节点；补充 C++ 对照实现的 ``std::swap`` 头文件；现有值交换对照、递归、迭代、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0025``：核对完整 ``k`` 组反转、不足 ``k`` 个尾段保持原序、节点值不变、``k`` 与链表长度范围；补充 C++ 数组对照实现的 ``std::reverse`` 头文件；现有数组、递归、迭代、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0026``：核对非递减输入、每个值只保留一次、原地覆盖、返回有效前缀长度和后缀不作要求；现有直接删除、额外数组对照、双指针、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0027``：核对原地移除、目标值过滤、前 ``k`` 个位置、后缀不作要求、顺序可改变和空数组范围；现有直接删除、稳定压缩、尾部交换、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0028``：核对连续子串、首次零基下标、未找到返回 ``-1``、小写字符和当前非空 ``needle`` 约束；补充空模式仅为约束外防御性分支的说明；现有标准库、朴素扫描、KMP、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0029``：核对 32 位有符号范围、向零截断、禁止乘除取模、``INT_MIN / -1`` 钳制和负数域倍增；修正所有语言实现中使用除法计算半最小值的违规写法，改为常量或移位边界；现有逐次减法、逐轮倍增、倍增表、C++ 实现和九语言实现已检查，无需运行代码测试。

本轮已完成 ``0030``：核对等长单词、全部词频恰好使用一次、重复词、可重叠答案、零基起始下标和固定偏移滑动窗口；C++ 基准方法、滑动窗口状态、收缩不变量、正确性与复杂度已审核，无需运行代码测试。已有多语言实现不纳入后续审核门槛。

本轮已完成 ``0031``：核对排列的字典序定义、重复值、原地修改、常量额外空间和最大排列回绕；收敛 C++ 区域为单一的枢轴—后继—反转后缀实现，补足其不变量、正确性与复杂度分析，无需运行代码测试。已有多语言实现不纳入后续审核门槛。

本轮规则文件已更新：``AGENTS.md`` 改为只要求独立 C++ 题解，已有多语言实现保留但不作为审核门槛；``README.rst`` 明确多语言内容保留且不作为后续必需项，缺失题号不创建新文件。下一轮从 ``0032`` 继续，不根据旧范围声明完成状态。

本轮已再次明确范围并重置可信游标：从 ``0001`` 重新检查到“九语言实现”之前；旧的 ``0001`` 至 ``0031`` 记录仅保留为历史说明，不能替代本次按新范围的复审。

按新范围重新复审已完成 ``0001``：核对 Two Sum 的输入范围、唯一解、不同下标、重复值和任意返回顺序；重新检查“九语言实现”之前的题目信息、题目重述、自建示例、C++ 实现和题解，并移除正文对多语言实现的依赖，不运行代码测试。

按新范围重新复审已完成 ``0002``：核对逆序数位、节点范围、零值表示、连续进位和输入链表语义；重新检查“九语言实现”之前的题目信息、题目重述、自建示例、C++ 实现和题解，并移除正文对多语言实现的依赖，不运行代码测试。

按新范围重新复审已完成 ``0001–0031``：逐题重新读取并核对“九语言实现”之前的题目信息、题目重述、自建示例、C++ 实现和题解；补正其中对多语言实现的正文依赖，确认现有 C++ 入口、证明和复杂度与题意一致，不运行代码测试。

按新范围重新复审已完成 ``0032–0040``：逐题重新读取并核对有效括号、旋转数组二分、边界二分、数独、游程编码和组合搜索等题目的“九语言实现”之前正文；修正 0036 C++ 辅助实现头文件、0037 C++ 辅助实现头文件和 0039 示例标题，不运行代码测试。

按新范围重新复审已完成 ``0041–0065``：逐题重新读取并核对原地数组、贪心、字符串、矩阵、链表、动态规划和数值解析题目的“九语言实现”之前正文；修正 0042 储水示例、0045 跳跃示例与不可达状态保护、0054 矩阵状态表、0056 C++ 辅助头文件、0058 C++ 语言表述，并移除 0061 正文对其他语言实现的依赖，不运行代码测试。

按新范围重新复审已完成 ``0066–0085``：逐题重新读取并核对进位、二进制加法、文本排版、二分、路径栈、编辑距离、矩阵、回溯、链表和单调栈题目的“九语言实现”之前正文；修正 0072 自建编辑距离示例、0074 二分状态表、0078 C++ 头文件、0079 搜索路径状态表、0082 节点资源表述和 0085 矩阵状态表，不运行代码测试。

按新范围重新复审已完成 ``0086–0100``：逐题重新读取并核对稳定分区、区间动态规划、原地归并、Gray Code、子集去重、解码、链表区间反转、IP 分段、树遍历、BST 构造与校验、字符串交错和同树判断等题目的“九语言实现”之前正文；修正 0086 分区状态表并移除其他语言表述、0087/0090/0097/0100 C++ 头文件、0088 归并状态表、0097 自建反例和 0098 说明，不运行代码测试。

按新范围重新复审已完成 ``0101–0200``：逐题读取本批次全部 100 个已有题号文件，审核第一个多语言实现标题之前的题目信息、题目重述或契约、自建示例、C++ 正文、正确性和复杂度；无缺失或重复题号，不运行代码测试。实际修改题目文件为 ``0101–0120``、``0125–0127``、``0129``、``0137``、``0140``、``0149–0151``、``0172–0174``、``0179``、``0187–0191``、``0198–0200``，并同步更新 ``AGENTS.md``、``README.rst`` 和本进度文件。

本批次重点修正：同步 ``0101–0120`` 的自建示例状态表与输入，补齐 C++ 使用的 ``<utility>``、``<cstdint>``、``<cstdlib>`` 和移动语义说明；修正 ``0110`` 失衡节点高度表、``0125``、``0150`` 等正文中的非 C++ 语言依赖；补充 ``0140`` 的 ``<utility>``；统一 ``0149``、``0151`` 的 C++ 头文件；清理可用题解前置正文中的多语言实现描述并保留 C++ 语义。``0156–0159``、``0161``、``0163``、``0170``、``0186`` 继续作为 Premium 状态页，``0175–0185``、``0196–0197`` 继续作为 Database 状态页，``0192–0195`` 继续作为 Shell 状态页；``0172–0174``、``0179``、``0187–0191``、``0198–0200`` 的现有“十语言实现”标题按同义多语言边界处理。

批次静态门已完成：题号文件计数为 100，逐题号文件唯一；``git diff --check`` 通过；前置正文未保留对后置多语言实现的依赖；未运行、未编译、未对拍。下一目标为 ``0201–0300``，从 ``0201-bitwise-and-of-numbers-range.rst`` 开始。
