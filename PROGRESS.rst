下一次接续任务
============

长期目标
--------

从第 ``0001`` 题开始，按题号顺序整理仓库中的全部 LeetCode 题目。当前阶段只修改每个题目 RST 的前三个小节：

#. ``题目信息``；
#. ``题目重述``；
#. ``自建示例``。

每道公开题必须先阅读 LeetCode 当前原题，逐项确认输入、输出、限制、保证条件和特殊语义，再与仓库内容对照。每题建立 ``2`` 至 ``3`` 个独立设计的示例：至少一个体现核心机制，至少一个明确覆盖原题允许的边界、空结果、无解、重复值、并列选择、最小规模或对象状态边界。不得复制或机械改写官方示例、说明和提示。``C++ 实现`` 及其后的全部内容保持原样。Premium、Database、Shell 状态页不凭记忆补写。禁止新建任何文件或目录。

可信完成范围
------------

``0700`` 至 ``0850`` 已按新标准逐题复核完成：

* ``0700`` 至 ``0840`` 已完成原题契约核对与示例整改；
* ``0841`` 至 ``0850``：共 ``10`` 道公开算法题，均已对照 LeetCode 当前原题核对；
* 本段修改了 ``8`` 道题：``0841`` 修正房间数量应至少为 2、每个房间钥匙互不重复以及总钥匙数范围；``0843`` 增加单候选交互边界；``0844`` 增加双方最终文本均为空的边界；``0845`` 增加峰顶平台导致无山脉的边界；``0847`` 增加单节点图边界；``0848`` 增加 z 循环到 a 的边界；``0849`` 增加最佳空座位位于末端的场景；``0850`` 增加单个单位矩形边界；
* ``0842`` 与 ``0846`` 原有两个示例已覆盖核心机制与合法边界，核对后保持原样；
* 没有创建、删除或重命名任何文件；未修改 ``C++ 实现`` 及其后的内容；
* ``0851`` 至 ``1100`` 尚未按这一新示例标准重新复核。

当前目标
--------

处理 ``0851. Loud and Rich``。

* 目标文件：``0801-0900/0851-loud-and-rich.rst``；
* 当前状态：公开 Medium 算法题，现有契约基本完整，但只有一个自建示例；
* 重点核对：``richer[a,b]`` 表示 a 比 b 富有；候选关系包含本人并沿有向关系传递；所有 quiet 值互不相同；对每个人返回候选中 quiet 最小者的编号；
* 示例要求：保留财富关系传递的核心场景，再补一个空 richer 或单人边界；
* 完成后下一入口：``0852. Peak Index in a Mountain Array``。

本题工作
--------

#. 打开并阅读 LeetCode ``0851`` 当前原题，不以现有 RST、标题、代码或记忆代替题面；
#. 对照检查 richer 方向、传递关系、候选包含本人、quiet 唯一性与输出含义；
#. 建立或复核 ``2`` 至 ``3`` 个独立示例，至少一个明确覆盖合法边界条件；
#. 独立复算每个示例的输入、输出和解释；
#. ``C++ 实现`` 及其后的内容保持原样；
#. 完成后继续核对 ``0852``，并更新可信完成范围和下一入口；
#. 不创建任何新文件或目录。