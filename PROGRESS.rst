下一次接续任务
============

长期目标
--------

从第 ``0001`` 题开始，按题号顺序整理仓库中的全部 LeetCode 题目。当前阶段只修改每个题目 RST 的前三个小节：

#. ``题目信息``；
#. ``题目重述``；
#. ``自建示例``。

每道公开题必须先阅读 LeetCode 当前原题，逐项确认输入、输出、限制、保证条件和特殊语义，再与仓库内容对照。现有内容正确时保留，不为制造改动而重写；发现偏差时只修正前三节。``C++ 实现`` 及其后的全部内容保持原样。Premium、Database、Shell 状态页不凭记忆补写。禁止新建任何文件或目录。

当前审计状态
------------

按用户指定，需要从 ``0700`` 开始重新完成严格逐题复核。目前不能把 ``0700`` 至 ``0800`` 标记为已经可信完成：此前的检查没有为每道公开题独立读取并核对 LeetCode 当前原题，且后续审计已经发现遗漏。

目前已确认并修正：

* ``0753``：补回“返回长度最短的字符串”这一核心契约，并修正示例解释；
* ``0763``：原示例由 LeetCode 官方提示中的字符串机械扩展，不符合“自建示例”要求，已替换为独立示例；
* ``0790``：原示例虽然输出 ``24`` 正确，但解释没有说明结果为何成立，已替换为可以完整验证的二列棋盘示例；
* ``0799``：原示例直接取自官方题面关于倒入四杯香槟的说明场景，不符合“自建示例”要求，已替换为独立场景。

``0700`` 至 ``0800`` 的其他文件只能视为待严格复核，不能因暂未发现错误而宣称全部准确。``0801`` 及之后暂不推进。没有创建、删除或重命名任何文件。

当前目标
--------

从 ``0700. Search in a Binary Search Tree`` 重新开始严格复核。

* 目标文件：``0601-0700/0700-search-in-a-binary-search-tree.rst``；
* 每道公开题必须实际读取 LeetCode 当前原题，并核对题目契约、约束和自建示例来源；
* 示例不仅要计算正确，还必须独立设计，不能复制官方示例、官方说明或提示中的场景，也不能只机械替换字符或数字；
* 每题完成后再推进下一题，直到重新核对至 ``0800``；
* Premium 状态页只确认类型并保持原样；
* ``C++ 实现`` 及其后的内容保持原样；
* 不创建任何新文件或目录。
