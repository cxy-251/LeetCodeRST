下一次接续任务
============

任务状态
--------

本仓库的前三节整理任务已于 ``2026-07-28`` 重置可信状态。

此前提交、旧版 ``PROGRESS.rst`` 或文件现状中关于 ``0700`` 至 ``2000`` 等范围已经完成的声明，只保留为历史记录，不再作为当前可信完成依据。

从本次重置开始，只有严格按照根目录 ``AGENTS.md`` 逐题读取当前原题、核对题目契约、重写或确认前三节，并在本文件中明确记录的题号，才属于新的可信完成范围。

当前可信范围
------------

暂无。

当前没有任何题号被计入重置后的可信完成范围。已经存在完整前三节的文件也必须重新逐题核对，不能因为内容看似完整而直接跳过。

当前目标
--------

从 ``0001. Two Sum`` 重新开始。

* 目标文件：``0001-0100/0001-two-sum.rst``；
* 文件类型：公开 Algorithms 题；
* 当前动作：读取当前 LeetCode 原题，重新核对并按 ``AGENTS.md`` 整理前三节；
* 允许修改：``题目信息``、``题目重述``、``自建示例``；
* 禁止修改：文件主标题、``C++ 实现`` 及其后的全部内容；
* 禁止操作：新增、删除、重命名或移动任何文件和目录。

本题重点核对
------------

处理 ``0001`` 时必须准确确认：

* 返回的是两个不同位置的零基下标；
* 同一个元素不能使用两次；
* 平台对有效答案数量的保证；
* 两个下标的返回顺序是否受限；
* 数组长度、元素值和目标值的合法范围；
* 自建示例必须独立设计，至少一个体现不同位置上的重复数值或其他合法边界。

完成条件
--------

``0001`` 只有同时满足以下条件，才可以加入新的可信范围：

#. 已实际读取并核对当前原题；
#. ``题目信息`` 五个字段准确；
#. ``题目重述`` 完整表达输入、输出、约束和特殊语义；
#. 至少两个原创自建示例，其中至少一个覆盖合法边界或易错条件；
#. 所有示例的输入、输出和解释已经独立复算；
#. diff 只涉及目标文件前三节和本进度文件；
#. 文件主标题与 ``C++ 实现`` 及其后的内容完全未变；
#. 没有新增、删除、重命名或移动任何文件。

完成后接力
----------

完成 ``0001`` 后：

* 在本文件中把新的可信范围记录为 ``0001``；
* 记录本次实际修改内容和检查结果；
* 将任务指针推进到第一道尚未按新 ``AGENTS.md`` 完成的题目；
* 下一目标通常为 ``0002. Add Two Numbers``，但必须先确认对应文件已经存在；
* 目标 RST 与更新后的 ``PROGRESS.rst`` 一起直接提交到 ``main``；
* 后续继续按题号顺序逐题推进，不得根据旧完成声明跳到 ``2001``。