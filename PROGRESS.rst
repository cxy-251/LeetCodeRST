下一次接续任务
============

当前目标
--------

处理 ``0006. Zigzag Conversion``。

* 目标文件：``0001-0100/0006-zigzag-conversion.rst``；
* 文件类型：公开算法题完整题解；
* 当前状态：目标文件已存在，执行全文审查与整改；
* 完成后下一入口：``0007. Reverse Integer``。

本轮工作
--------

#. 读取 ``AGENTS.md`` 和第六题全文；
#. 核对 Z 字形路径、按行读取顺序、``numRows = 1``、行数不小于字符串长度以及平台字符串单位；
#. 设计能够展示完整周期、不完整尾部周期、中间行双字符和首尾行转向点的自建示例；
#. 在 C++ 代码块中实现按路径写入行缓冲区和按周期索引直接读取两种代表性解法，并由标准入口调用周期索引主解法；
#. 从逐字符路径模拟推导行号的下降与上升变化，再推导一个完整周期长度 ``2 * numRows - 2``；
#. 推导每一行的竖列下标 ``row + k * cycle``，以及中间行斜线下标 ``index + cycle - 2 * row``；
#. 使用跟踪表展示输入下标、所属行、路径方向、周期位置和最终逐行读取顺序；
#. 说明首行与末行为什么每周期只出现一次，中间行为什么按“竖列字符、斜线字符”顺序出现；
#. 使用具体机制标题解释周期索引为何覆盖每个输入字符一次，以及逐行追加为何保持图形中的从左到右顺序；
#. 从路径模拟次数、行缓冲区总字符数和周期索引访问次数推导两种方法的实际时间与空间复杂度；
#. 用 C、Python、Java、Rust、Go、TypeScript、C#、Julia 和 R 实现周期索引主解法；
#. 在关键代码位置使用简洁的中文注释，并让题解正文在 R 实现结束。

本轮完成条件
------------

#. 第六题完整呈现“路径模拟 → 行号变化 → 周期结构 → 竖列与斜线索引 → 状态演化 → 覆盖与顺序依据 → 复杂度”的推导链；
#. C++、题解正文和九语言实现使用一致的周期长度、行号、竖列位置和斜线位置；
#. 完整重写本文件，使下一轮直接接续 ``0007. Reverse Integer``；
#. 将第六题与新的 ``PROGRESS.rst`` 组成一个原子提交。
