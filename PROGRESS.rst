下一次接续任务
============

当前目标
--------

处理 ``0008. String to Integer (atoi)``。

* 目标文件：``0001-0100/0008-string-to-integer-atoi.rst``；
* 文件类型：公开算法题完整题解；
* 当前状态：目标文件已存在，执行全文审查与整改；
* 完成后下一入口：``0009. Palindrome Number``。

本轮工作
--------

#. 读取 ``AGENTS.md`` 和第八题全文；
#. 核对前导空格、可选正负号、连续数字、首个无效字符停止、无数字返回 0 和 32 位钳制规则；
#. 设计能够展示空格与符号、符号后无数字、数字后停止、正向溢出、负向溢出和前导零的自建示例；
#. 在 C++ 代码块中实现显式阶段扫描和有限状态机两种代表性解法，并由标准入口调用显式阶段扫描主解法；
#. 从解析规则推导“跳过空格 → 读取符号 → 累积数字 → 遇到其他字符停止”的单向状态变化；
#. 推导 ``value = value * 10 + digit`` 的压入前边界判断，以及正负号如何决定最终钳制方向；
#. 使用跟踪表展示输入位置、当前字符、解析阶段、符号、累计值和停止或钳制动作；
#. 说明为什么扫描停止后不能继续寻找数字，以及为什么只要读取过至少一个数字就能形成合法结果；
#. 使用具体机制标题解释各阶段为何只向前推进、边界检查为何覆盖全部溢出情况、停止位置为何确定最终解析前缀；
#. 从字符串扫描次数、状态数量和常数变量推导两种方法的实际时间与空间复杂度；
#. 用 C、Python、Java、Rust、Go、TypeScript、C#、Julia 和 R 实现选定主解法，并对字符判断、数值类型和钳制语义作语言适配；
#. 在关键代码位置使用简洁的中文注释，并让题解正文在 R 实现结束。

本轮完成条件
------------

#. 第八题完整呈现“输入前缀规则 → 阶段扫描 → 符号确定 → 十进制累积 → 停止条件 → 边界钳制 → 状态演化 → 完整性依据 → 复杂度”的推导链；
#. C++、题解正文和九语言实现使用一致的扫描阶段、数字累积顺序、停止位置和 32 位边界；
#. 完整重写本文件，使下一轮直接接续 ``0009. Palindrome Number``；
#. 将第八题与新的 ``PROGRESS.rst`` 组成一个原子提交。
