下一次接续任务
============

长期目标
--------

从第 ``0001`` 题开始，按题号顺序整理仓库中的全部 LeetCode 题目。当前阶段只修改每个题目 RST 的前三个小节：

#. ``题目信息``；
#. ``题目重述``；
#. ``自建示例``。

每道公开题必须先阅读 LeetCode 当前原题，逐项确认输入、输出、限制、保证条件和特殊语义，再与仓库内容对照。每题建立 ``2`` 至 ``3`` 个独立设计的示例：至少一个体现核心机制，至少一个明确覆盖原题允许的边界、空结果、无解、重复值、并列选择、最小规模或对象状态边界。不得复制或机械改写官方示例、说明和提示。``C++ 实现`` 及其后的全部内容保持原样。Premium、Database、Shell 状态页不凭记忆补写。禁止新建任何文件或目录。

可信完成范围
------------

``0700`` 至 ``0860`` 已按新标准逐题复核完成：

* ``0700`` 至 ``0850`` 已完成原题契约核对与示例整改；
* ``0851`` 至 ``0860``：共 ``10`` 道公开算法题，均已对照 LeetCode 当前原题核对；
* 本段修改了 ``9`` 道题：``0851`` 补充空 richer 边界并写明关系唯一性；``0852`` 补回 ``O(log n)`` 时间要求并增加最短山脉边界；``0853`` 补充单车边界及数组长度契约；``0854`` 把字符范围修正为 ``a`` 至 ``f`` 并增加零交换边界；``0855`` 删除直接复制官方操作序列的示例，改为两个独立状态场景；``0856`` 增加顶层并列计分；``0857`` 增加只雇一人的边界；``0858`` 增加命中接收器 0 的场景；``0859`` 增加交换两个不同字符的核心正例；
* ``0860`` 原有成功找零与首位顾客无法找零两个示例已覆盖核心机制与边界，核对后保持原样；
* 没有创建、删除或重命名任何文件；未修改 ``C++ 实现`` 及其后的内容；
* ``0861`` 至 ``1100`` 尚未按这一新示例标准重新复核。

当前目标
--------

处理 ``0861. Score After Flipping Matrix``。

* 目标文件：``0801-0900/0861-score-after-flipping-matrix.rst``；
* 当前状态：公开 Medium 算法题，现有契约基本完整，但只有一个自建示例；
* 重点核对：可以重复翻转任意整行或整列；每行按最左位为最高位解释为二进制数；返回所有行数值之和的最大值；
* 示例要求：保留行列共同翻转的核心场景，再补一个单格、单行或已经达到最大分数的合法边界；
* 完成后下一入口：``0862. Shortest Subarray with Sum at Least K``。

本题工作
--------

#. 打开并阅读 LeetCode ``0861`` 当前原题，不以现有 RST、标题、代码或记忆代替题面；
#. 对照检查矩阵尺寸、翻转范围、二进制位序、重复操作许可和返回值；
#. 建立或复核 ``2`` 至 ``3`` 个独立示例，至少一个明确覆盖合法边界条件；
#. 独立复算每个示例的输入、输出和解释；
#. ``C++ 实现`` 及其后的内容保持原样；
#. 完成后继续核对 ``0862``，并更新可信完成范围和下一入口；
#. 不创建任何新文件或目录。