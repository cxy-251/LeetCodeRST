下一次接续任务
============

长期目标
--------

从第 ``0001`` 题开始，按题号顺序整理仓库中的全部 LeetCode 题目。当前阶段只修改每个题目 RST 的前三个小节：

#. ``题目信息``；
#. ``题目重述``；
#. ``自建示例``。

每道公开题必须先阅读 LeetCode 当前原题，逐项确认输入、输出、限制、保证条件和特殊语义，再与仓库内容对照。每题原则上建立 ``2`` 至 ``3`` 个独立设计的示例：至少一个体现核心机制，至少一个明确覆盖原题允许的边界、空结果、无解、重复值、并列选择、最小规模、对象初始状态或特殊返回值。不得复制或机械改写官方示例、说明和提示；所有输出和解释必须独立复算。``C++ 实现`` 及其后的全部内容保持原样。Premium、Database、Shell 状态页不凭记忆补写。禁止新建任何文件或目录。

可信完成范围
------------

``0700`` 至 ``0900`` 已按上述标准重新逐题审核完成：

* ``0700`` 至 ``0800`` 的严格复核结果继续有效；
* ``0801`` 至 ``0900`` 已按题号逐题打开仓库 RST，并实际对照 LeetCode 当前原题核对输入输出、参数含义、下标、约束、顺序、唯一性、对象状态、无解条件和特殊返回语义；本范围没有 Premium 状态页；
* 每道公开算法题现有 ``2`` 至 ``3`` 个独立自建示例，其中至少一个明确体现边界或特殊语义，所有输出和解释均已独立复算；
* 本批实际修改 ``40`` 个题目文件：``0803``、``0808``、``0809``、``0827``、``0847`` 至 ``0849``、``0858`` 至 ``0860``，以及 ``0871`` 至 ``0900``；其余内容正确的题目保持原样；
* 主要示例问题包括直接复用官方输入、截短或调换官方示例、保持官方操作序列、只机械替换数值，以及公开题只有一个示例；
* 主要契约修正包括：``0874`` 补充障碍可位于原点且机器人起步时忽略、离开后不可再进入；``0879`` 补充 ``minProfit = 0`` 时空子集计入；``0888`` 补充双方原糖果总数不同并修正数组约束字段；``0889`` 按当前题面修正节点值范围；``0891`` 将 ``nums[i]`` 上限从错误的 ``10^9`` 修正为 ``10^5``；``0897`` 删除未由当前题面单独保证的节点值互异条件，并按中序结果表述；
* ``0861`` 至 ``0870`` 已重新逐题复核，旧整改均成立，没有为了制造提交再次改写；
* 没有创建、删除或重命名文件，没有修改任何题目的 ``C++ 实现`` 及其后内容，也没有修改 ``0901``；
* ``0901`` 及其后的既有内容尚未纳入本次可信复核范围，不能视为已确认。

规则同步状态
------------

``AGENTS.md`` 已经包含公开算法题至少两个原创示例、至少一个覆盖边界或特殊语义、禁止复制或机械改写官方示例等要求，本批核对后保持原样；本文件已同步记录真实完成范围。

当前目标
--------

下一入口为 ``0901. Online Stock Span``。

* 目标文件：``0901-1000/0901-online-stock-span.rst``；
* 当前状态：文件存在，但尚未纳入严格复核，不得把现有内容视为已确认；
* 重点核对：对象状态跨多次 ``next(price)`` 调用保留；跨度包含当天，并只向前覆盖连续且价格小于或等于当天价格的日期；返回值至少为 ``1``；
* 开始前必须实际读取 LeetCode ``0901`` 当前原题，并只修改前三节；
* ``0700`` 至 ``0900`` 无需为了制造提交再次改写，除非后续发现可复现的新问题。