下一次接续任务
============

当前目标
--------

处理 ``0007. Reverse Integer``。

* 目标文件：``0001-0100/0007-reverse-integer.rst``；
* 文件类型：公开算法题完整题解；
* 当前状态：目标文件已存在，执行全文审查与整改；
* 完成后下一入口：``0008. String to Integer (atoi)``。

本轮工作
--------

#. 读取 ``AGENTS.md`` 和第七题全文；
#. 核对 32 位有符号整数范围、十进制数字反转、负号语义、前导零消失和越界返回 0；
#. 设计能够展示正数末尾零、负数余数、正向上边界、负向下边界和中途溢出的自建示例；
#. 在 C++ 代码块中实现字符串转换、宽整数累积和纯 32 位逐位处理三种代表性解法，并由标准入口调用压入前判溢出的主解法；
#. 从十进制位权推导“弹出最低位、删除最低位、把数字压入结果”的状态变化；
#. 说明向零截断除法与负数余数如何让正负数使用同一循环，并比较各语言的除法和取余语义；
#. 推导 ``reversed * 10 + digit`` 在正负边界上的压入前判断，解释商边界与最后一位边界的来源；
#. 使用跟踪表展示 ``x``、``digit``、更新前结果、边界检查和更新后结果的逐步变化；
#. 使用具体机制标题解释每轮为何恰好移动一个十进制位、压入前检查为何覆盖全部溢出情况，以及循环结束为何得到完整反转；
#. 从十进制数字数量、字符串构造、宽整数累积和常数状态推导三种方法的实际时间与空间复杂度；
#. 用 C、Python、Java、Rust、Go、TypeScript、C#、Julia 和 R 实现选定主解法，并明确各语言固定宽度与除法适配；
#. 在关键代码位置使用简洁的中文注释，并让题解正文在 R 实现结束。

本轮完成条件
------------

#. 第七题完整呈现“十进制位权 → 逐位弹出 → 向零截断 → 结果压入 → 边界推导 → 状态演化 → 完整性依据 → 复杂度”的推导链；
#. C++、题解正文和九语言实现使用一致的数字提取、结果更新和溢出判断顺序；
#. 完整重写本文件，使下一轮直接接续 ``0008. String to Integer (atoi)``；
#. 将第七题与新的 ``PROGRESS.rst`` 组成一个原子提交。
