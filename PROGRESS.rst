下一次接续任务
============

长期目标
--------

从第 ``0001`` 题开始，按题号顺序整理仓库中的全部 LeetCode 题目。当前阶段只修改每个题目 RST 的前三个小节：

#. ``题目信息``；
#. ``题目重述``；
#. ``自建示例``。

每道公开题必须先阅读 LeetCode 当前原题，逐项确认输入、输出、限制、保证条件和特殊语义，再与仓库内容对照。每题建立 ``2`` 至 ``3`` 个独立设计的示例：至少一个体现核心机制，至少一个明确覆盖原题允许的边界、空结果、无解、重复值、并列选择、最小规模或对象状态边界。不得复制或机械改写官方示例、说明和提示。``C++ 实现`` 及其后的全部内容保持原样。Premium、Database、Shell 状态页不凭记忆补写。禁止新建任何文件或目录。

可信完成范围
------------

``0700`` 至 ``0800`` 已按新标准重新逐题复核完成：

* 共 ``82`` 道公开算法题，均已对照 LeetCode 原题核对契约、约束、特殊语义和示例结果；
* 每道公开题现在都包含 ``2`` 至 ``3`` 个自建示例，至少一个示例明确写出合法边界条件或特殊结果；
* 共 ``19`` 个 Premium 状态页，已确认类型并保持原样；
* ``0753`` 补回了“返回长度最短的字符串”这一核心契约，并将示例改为可完整验证最短性的独立覆盖序列；
* ``0763``、``0790``、``0799`` 中来源过近或解释不足的示例已经替换；
* 本次针对示例数量与边界覆盖又修改了 ``27`` 道公开题，并同步更新 ``AGENTS.md``，把每题至少两个示例写入长期规则；
* 没有创建、删除或重命名任何文件，未修改 Premium 状态页；
* ``0801`` 至 ``1100`` 尚未按这一新示例标准重新复核。

当前目标
--------

处理 ``0801. Minimum Swaps To Make Sequences Increasing``。

* 目标文件：``0801-0900/0801-minimum-swaps-to-make-sequences-increasing.rst``；
* 当前状态：公开 Hard 算法题，现有前三节需重新依据 LeetCode 原题与新示例规则核对；
* 重点核对：两个数组长度相同；一次操作只能交换同一下标的两个元素；最终两个数组都必须严格递增；题目保证存在可行方案；返回最少交换次数；
* 示例要求：保留或建立一个需要交换的核心场景，并建立一个合法边界场景，例如无需交换、最短数组或存在多种交换选择但最少次数确定；
* 完成后下一入口：``0802. Find Eventual Safe States``。

本题工作
--------

#. 打开并阅读 LeetCode ``0801`` 当前原题，不以现有 RST、标题、代码或记忆代替题面；
#. 对照检查题号、难度、数组范围、同下标交换规则、严格递增要求、可行性保证和返回值；
#. 建立或复核 ``2`` 至 ``3`` 个独立示例，至少一个明确覆盖合法边界条件；
#. 独立复算每个示例的输入、输出和解释；
#. ``C++ 实现`` 及其后的内容保持原样；
#. 完成后继续核对 ``0802``，并更新可信完成范围和下一入口；
#. 不创建任何新文件或目录。
